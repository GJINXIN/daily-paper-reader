---
title: "MemCollab: Cross-Agent Memory Collaboration via Contrastive Trajectory Distillation"
title_zh: MemCollab：通过对比轨迹蒸馏实现的跨智能体记忆协作
authors: "Yurui Chang, Yiran Wu, Qingyun Wu, Lu Lin"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23234v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 基于大语言模型的智能体及跨智能体记忆协作
tldr: 针对大语言模型智能体内存机制通常与特定模型耦合、难以在异构智能体间共享的问题，本文提出了 MemCollab 框架。该框架通过对比不同智能体在同一任务上的推理轨迹，利用对比轨迹蒸馏技术提取出与智能体无关的抽象推理约束，从而构建通用的协作内存。实验证明，MemCollab 在数学推理和代码生成任务中显著提升了不同模型间的协作效率和准确性，实现了跨模型的推理资源共享。
selection_source: fresh_fetch
motivation: 现有的智能体内存机制往往与特定模型的推理风格深度绑定，导致在异构智能体间直接共享内存时会因模型偏见而导致性能下降。
method: 提出 MemCollab 框架，通过对比不同智能体的推理轨迹来蒸馏出任务级的不变量，并结合任务感知检索机制实现跨智能体的内存协作。
result: 在数学推理和代码生成基准测试中，MemCollab 显著提高了多种异构智能体（包括跨模型系列）的推理准确率和推理效率。
conclusion: 协作构建的内存可以作为异构大语言模型智能体之间的共享推理资源，有效打破了内存与特定模型之间的耦合。
---

## 摘要
基于大语言模型（LLM）的智能体依赖记忆机制来复用过去解决问题经验中的知识。现有方法通常以每个智能体为单位构建记忆，将存储的知识与单个模型的推理风格紧密耦合。在异构智能体的现代部署中，一个自然的问题随之而来：单个记忆系统能否在不同模型之间共享？我们发现，在智能体之间简单地迁移记忆往往会降低性能，因为此类记忆将任务相关知识与智能体特定的偏置纠缠在一起。为了应对这一挑战，我们提出了 MemCollab，这是一个协作记忆框架，通过对比不同智能体在同一任务上生成的推理轨迹来构建与智能体无关的记忆。这种对比过程蒸馏出了抽象的推理约束，这些约束捕捉了共享的任务级不变性，同时抑制了智能体特定的伪影。我们进一步引入了一种任务感知检索机制，该机制根据任务类别调节记忆访问，确保在推理时仅使用相关的约束。在数学推理和代码生成基准测试上的实验表明，MemCollab 在包括跨模型家族设置在内的多种智能体中，一致地提高了准确率和推理效率。我们的结果表明，协作构建的记忆可以作为多种基于 LLM 的智能体的共享推理资源。

## Abstract
Large language model (LLM)-based agents rely on memory mechanisms to reuse knowledge from past problem-solving experiences. Existing approaches typically construct memory in a per-agent manner, tightly coupling stored knowledge to a single model's reasoning style. In modern deployments with heterogeneous agents, a natural question arises: can a single memory system be shared across different models? We found that naively transferring memory between agents often degrades performance, as such memory entangles task-relevant knowledge with agent-specific biases. To address this challenge, we propose MemCollab, a collaborative memory framework that constructs agent-agnostic memory by contrasting reasoning trajectories generated by different agents on the same task. This contrastive process distills abstract reasoning constraints that capture shared task-level invariants while suppressing agent-specific artifacts. We further introduce a task-aware retrieval mechanism that conditions memory access on task category, ensuring that only relevant constraints are used at inference time. Experiments on mathematical reasoning and code generation benchmarks demonstrate that MemCollab consistently improves both accuracy and inference-time efficiency across diverse agents, including cross-modal-family settings. Our results show that the collaboratively constructed memory can function as a shared reasoning resource for diverse LLM-based agents.

---

## 论文详细总结（自动生成）

以下是对论文《MemCollab: Cross-Agent Memory Collaboration via Contrastive Trajectory Distillation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体记忆机制通常是“单智能体”模式，即记忆是从特定模型的推理轨迹中提取并供其自身使用的。这种方式导致记忆与特定模型的推理风格、偏好和偏见深度耦合。
*   **研究动机**：在现代应用中，通常会部署多个不同规模或架构的异构智能体（如 7B 模型与 32B 模型协作）。如果直接将一个模型的记忆迁移给另一个模型，往往会导致性能下降。
*   **整体含义**：论文旨在探索是否能构建一个**智能体无关（Agent-agnostic）**的共享记忆系统，通过对比不同智能体在同一任务上的表现，提取出通用的推理规律，从而支持异构智能体间的知识共享。

### 2. 方法论
MemCollab 框架分为两个主要阶段：**对比驱动的记忆构建**和**任务感知的记忆检索与推理**。

*   **对比轨迹蒸馏（Contrastive Trajectory Distillation）**：
    *   **轨迹配对**：让两个异构智能体（如一强一弱）解决同一个任务，收集它们的推理轨迹。
    *   **偏好选择**：根据结果正确性区分“优选轨迹”（$\tau^+$）和“非优选轨迹”（$\tau^-$）。
    *   **对比分析**：利用大模型对比这两条轨迹，识别出导致成功的“推理不变量”（Invariants）和导致失败的“违规模式”（Violation patterns）。
    *   **记忆条目生成**：将对比结果转化为抽象的推理约束，格式为：`enforce [推理不变量]; avoid [违规模式]`。这种抽象化处理去除了模型特定的风格，保留了任务级的逻辑。
*   **任务感知检索（Task-Aware Retrieval）**：
    *   **任务分类**：使用 LLM 作为分类器，将任务按类别（如代数、数论）和子类别进行标记。
    *   **两阶段检索**：推理时，先根据任务类别过滤记忆库，再通过相似度匹配检索最相关的 Top-p 条约束。
*   **记忆增强推理**：智能体在推理时将检索到的抽象约束作为 Prompt 的一部分，引导其避开常见错误并遵循正确逻辑。

### 3. 实验设计
*   **数据集**：
    *   **数学推理**：MATH500、GSM8K。
    *   **代码生成**：MBPP、HumanEval。
*   **基准模型（Backbones）**：Qwen2.5-7B/32B-Instruct、LLaMA-3-8B-Instruct。
*   **对比方法**：
    *   **Vanilla**：无记忆增强的基准。
    *   **Buffer of Thoughts (BoT)**：基于推理模板的记忆方法。
    *   **Dynamic Cheatsheet (DC)**：测试时学习的自适应记忆。
    *   **Naive Transfer**：直接使用单一模型（如 32B）生成的记忆。
    *   **Self-Contrast**：模型自身生成的多个轨迹进行对比。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX 6000 GPU**。
*   **软件框架**：基于 `smolagents` 的 `CodeAgent` 实现。
*   **训练/推理细节**：推理时设置温度为 0，Top-p 为 0.8。记忆库构建涉及对 1000 个训练样本进行轨迹生成和对比蒸馏。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了测试，涵盖了数学和代码两大复杂推理领域。
*   **实验维度**：
    *   **同家族跨规模**：Qwen2.5-7B 与 32B 之间的协作。
    *   **跨家族协作**：Llama-3-8B 与 Qwen2.5-32B 之间的协作。
    *   **消融实验**：测试了检索数量（Top-k）的影响、不同检索策略（嵌入 vs. 分类）的对比。
    *   **效率分析**：统计了推理轮数的变化。
*   **充分性评价**：实验设计较为全面，不仅验证了准确率的提升，还通过跨家族实验证明了方法的通用性，对比基线涵盖了最新的相关研究，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：MemCollab 显著优于直接迁移记忆的方法。例如，Qwen2.5-7B 在 MATH500 上的准确率从 52.2% 提升至 67.0%。
*   **双向受益**：不仅弱模型（7B/8B）能通过共享记忆变强，强模型（32B）在引入协作记忆后性能也有进一步提升。
*   **推理效率**：使用协作记忆后，智能体解决问题所需的平均推理轮数显著减少（如在代码任务中减少了一半以上），说明记忆引导减少了盲目的试错。
*   **跨架构有效性**：即使在 Llama 和 Qwen 这种不同架构的模型间，对比蒸馏出的记忆依然具有高度的可迁移性。

### 7. 优点
*   **解耦偏见**：通过对比学习成功将“任务逻辑”与“模型风格”解耦，解决了异构智能体记忆共享的根本痛点。
*   **任务感知**：引入任务分类过滤机制，有效降低了检索噪声，提高了推理时的信噪比。
*   **通用性强**：不依赖于特定的模型架构，适用于现代多模型混合部署的场景。

### 8. 不足与局限
*   **依赖强模型蒸馏**：记忆的构建和任务分类仍高度依赖于一个性能较强的 Backbone 模型（如 32B 或更高），对于完全没有强模型参与的场景可能受限。
*   **领域局限**：目前实验主要集中在数学和代码等逻辑高度结构化的领域，在创意写作或开放式对话等非结构化任务中的效果尚待验证。
*   **分类体系固定**：任务分类依赖于预定义的标签体系，对于模糊或跨学科的新任务，分类准确性可能会影响检索效果。

（完）
