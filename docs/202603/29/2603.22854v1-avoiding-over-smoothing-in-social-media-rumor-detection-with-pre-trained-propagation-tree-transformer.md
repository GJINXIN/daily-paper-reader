---
title: Avoiding Over-smoothing in Social Media Rumor Detection with Pre-trained Propagation Tree Transformer
title_zh: 利用预训练传播树 Transformer 避免社交媒体谣言检测中的过度平滑问题
authors: "Chaoqun Cui, Caiyan Jia"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22854v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 利用传播树进行社交媒体谣言检测
tldr: 针对社交媒体谣言检测中图神经网络（GNN）存在的过度平滑和长程依赖捕捉能力弱的问题，本文提出了一种基于纯Transformer架构的预训练传播树模型（P2T3）。该方法通过提取对话链并利用Token级嵌入引入结构偏置，在大规模无标签数据上进行预训练。实验证明P2T3在多个基准数据集上超越了现有最优方法，且在少样本场景下表现优异，为谣言检测提供了新的范式。
selection_source: fresh_fetch
motivation: 现有的基于GNN的谣言检测方法受限于传播树结构的过度平滑问题，且难以捕捉长距离的节点依赖关系。
method: 提出P2T3模型，通过提取传播树中的对话链，结合Token级嵌入注入连接信息，并利用大规模无标签数据进行预训练。
result: P2T3在多个公开基准数据集上达到了SOTA性能，并在少样本学习任务中展现出强大的泛化能力。
conclusion: 该研究证明了纯Transformer架构能有效克服GNN在谣言检测中的局限性，为未来社交媒体研究提供了统一的大模型方案。
---

## 摘要
用于谣言检测的深度学习技术通常利用图神经网络（GNNs）来分析帖子之间的关系。然而，这些方法在处理谣言传播结构时，由于过度平滑（over-smoothing）问题而表现不佳，导致性能下降。我们对该问题的调查显示，过度平滑与谣言传播树的结构特征本质相关，其中大多数节点都是 1 级节点。此外，GNN 难以捕捉这些树中的长距离依赖关系。为了规避这些挑战，我们提出了一种基于纯 Transformer 架构的预训练传播树 Transformer（P2T3）方法。该方法沿着回复的传播方向从树结构中提取所有对话链，利用 token 级嵌入注入连接信息并引入必要的归纳偏置，并在大规模无标签数据集上进行预训练。实验表明，P2T3 在多个基准数据集上超越了以往的最先进方法，并在少样本条件下表现良好。P2T3 不仅避免了 GNN 固有的过度平滑问题，还可能为未来的社交媒体研究提供大模型或统一的多模态方案。

## Abstract
Deep learning techniques for rumor detection typically utilize Graph Neural Networks (GNNs) to analyze post relations. These methods, however, falter due to over-smoothing issues when processing rumor propagation structures, leading to declining performance. Our investigation into this issue reveals that over-smoothing is intrinsically tied to the structural characteristics of rumor propagation trees, in which the majority of nodes are 1-level nodes. Furthermore, GNNs struggle to capture long-range dependencies within these trees. To circumvent these challenges, we propose a Pre-Trained Propagation Tree Transformer (P2T3) method based on pure Transformer architecture. It extracts all conversation chains from a tree structure following the propagation direction of replies, utilizes token-wise embedding to infuse connection information and introduces necessary inductive bias, and pre-trains on large-scale unlabeled datasets. Experiments indicate that P2T3 surpasses previous state-of-the-art methods in multiple benchmark datasets and performs well under few-shot conditions. P2T3 not only avoids the over-smoothing issue inherent in GNNs but also potentially offers a large model or unified multi-modal scheme for future social media research.

---

## 论文详细总结（自动生成）

### 论文总结：利用预训练传播树 Transformer 避免社交媒体谣言检测中的过度平滑问题

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对社交媒体谣言检测中的**传播结构建模**问题展开研究。目前主流方法多采用图神经网络（GNN）处理谣言传播树（RPT），但存在两大核心缺陷：
*   **过度平滑（Over-smoothing）：** 论文通过实证研究发现，RPT 结构极度不平衡，绝大多数节点（约 60%-80%）是直接回复源帖的 1 级节点。在无向图设置下，仅需 2-3 层 GNN 聚合，节点特征就会趋同，导致模型加深时性能反而下降。
*   **长程依赖捕捉能力弱：** GNN 侧重于局部邻域聚合，难以有效捕捉从源帖到深层叶子节点的完整对话逻辑链条。
**研究动机：** 探索一种能够克服 GNN 结构限制、支持大规模预训练且能捕捉深层交互信息的模型架构。

#### 2. 方法论：P2T3 模型
论文提出了 **Pre-trained Propagation Tree Transformer (P2T3)**，核心思想是将树状结构转化为序列化的对话链，并利用 Transformer 的全局注意力机制。

*   **对话链提取：** 从 RPT 中提取所有从 1 级节点到叶子节点的路径，形成多个对话链序列。
*   **Token 级嵌入（Token-wise Embedding）：** 为了在 Transformer 中保留图结构信息，引入了三种嵌入：
    *   **链标识符（Chain Identifier）：** 使用正交向量区分不同的对话链，确保模型识别 token 所属的路径。
    *   **深度嵌入（Depth Embedding）：** 采用类似正弦/余弦的编码，标记节点在原始树中的层级深度。
    *   **类型嵌入（Type Embedding）：** 区分源帖、深层对话和浅层对话。
*   **训练策略：**
    *   **预训练：** 在大规模无标签数据上，通过最大化源帖表示与各对话链首个节点表示之间的**互信息（Mutual Information）**进行自监督学习。
    *   **微调：** 在标注数据上使用交叉熵损失进行分类任务训练。

#### 3. 实验设计
*   **数据集：** 使用了四个主流基准数据集：Weibo、DRWeibo、Twitter15 和 Twitter16。
*   **无标签数据集：** 作者自行构建并发布了两个大规模数据集 **UWeibo** (209,549 条) 和 **UTwitter** (204,922 条)。
*   **Benchmark 与对比方法：**
    *   **Transformer 类：** PLAN, HD-TRANS。
    *   **GNN 类：** BiGCN, ClaHi-GAT, GACL, DDGCN, RAGCL（当前 SOTA）。
*   **实验场景：** 包括全量监督学习、消融实验、模型深度敏感性分析、少样本（Few-shot）性能测试以及案例分析。

#### 4. 资源与算力
*   **硬件：** 使用了单张 **Nvidia GeForce RTX 3090 GPU**。
*   **软件：** 基于 PyTorch 实现。
*   **训练细节：** 学习率为 5e-5，Batch Size 为 32，Transformer 编码器为 3 层。所有模型统一训练 100 个 epoch 以确保收敛，取最后 10 个 epoch 的平均值作为最终结果。

#### 5. 实验数量与充分性
论文的实验设计非常详尽且具有说服力：
*   **实证调查：** 在正式提出方法前，先通过实验验证了 GNN 在 RPT 上的过度平滑现象（对比了 UD-GCN, TD-GCN 等不同方向的效果）。
*   **消融实验：** 分别验证了三种 Token 嵌入和预训练模块的贡献，证明了链标识符（Chain Identifier）对性能提升最为关键。
*   **深度实验：** 对比了 P2T3 与 GNN 随层数增加的性能变化，证明了 P2T3 具有更强的扩展性（Scaling capability）。
*   **公平性：** 所有基准模型均在相同数据下重新实现或运行，并提供了开源代码和数据集。

#### 6. 主要结论与发现
*   **克服平滑：** P2T3 成功解决了 GNN 在处理 RPT 时的过度平滑问题，模型性能随深度增加而提升并趋于稳定。
*   **SOTA 性能：** 在所有四个数据集上，P2T3 的准确率和 F1 值均显著优于之前的 SOTA 方法（如 RAGCL）。
*   **预训练红利：** 相比 GNN，Transformer 架构能更有效地从大规模无标签数据中学习判别性特征。
*   **少样本优势：** 在仅有极少量标注样本（如 10-50 个）的情况下，经过预训练的 P2T3 表现远超传统模型。

#### 7. 优点
*   **结构创新：** 巧妙地将非平衡树结构转化为对话链序列，既保留了时序逻辑，又利用了 Transformer 处理长程依赖的能力。
*   **理论支撑：** 对 GNN 在谣言检测中的失效原因（1 级节点占比过高导致的频谱低通滤波效应）进行了深入的理论分析。
*   **资源贡献：** 提供了两个高质量的大规模无标签社交媒体数据集，对半监督谣言检测研究有重要价值。

#### 8. 不足与局限
*   **序列长度压力：** 将树拆解为多条对话链并拼接，可能会导致输入序列过长，增加 Transformer 的计算复杂度（$O(n^2)$）。
*   **多模态缺失：** 虽然论文提到了未来方向，但目前的 P2T3 主要基于文本和结构，尚未深度整合社交媒体中常见的图像和视频信息。
*   **实时性挑战：** 预训练模型虽然效果好，但在极早期检测（Early Detection）场景下，当对话链尚未充分形成时，其优势可能受限。

（完）
