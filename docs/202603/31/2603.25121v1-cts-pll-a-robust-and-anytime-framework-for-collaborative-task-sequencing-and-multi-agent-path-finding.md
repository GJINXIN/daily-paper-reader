---
title: "CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding"
title_zh: CTS-PLL：一种用于协作任务排序与多智能体路径规划的鲁棒且随时的框架
authors: "Junkai Jiang, Yitao Xu, Ruochen Li, Shaobing Xu, Jianqiang Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25121v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 多智能体路径规划与任务排序
tldr: 针对协作任务排序与多智能体路径规划（CTS-MAPF）中的组合复杂性难题，本文提出CTS-PLL分层框架。该框架引入了基于完备规划方法的死锁检测与释放机制，并结合大邻域搜索（LNS）实现解的随时优化。实验证明，该方法在稠密环境下具有极高的鲁棒性，能显著提升任务成功率和解质量，并已在真实机器人实验中得到验证。
selection_source: fresh_fetch
motivation: 旨在解决CTS-MAPF问题在复杂稠密环境下因组合爆炸导致的规划失败和解质量低下的挑战。
method: 提出了一种结合死锁检测释放机制与大邻域搜索（LNS）优化的分层规划框架。
result: 在稀疏和稠密基准测试中，该方法在成功率和解质量上均优于现有算法，且具备良好的运行效率。
conclusion: CTS-PLL是一个鲁棒且高效的随时规划框架，为解决实际场景中的多智能体协作任务提供了可行方案。
---

## 摘要
协作任务排序与多智能体路径规划（CTS-MAPF）问题要求智能体在避免碰撞的同时完成一系列任务，由于其组合复杂性，这带来了巨大的挑战。本研究引入了 CTS-PLL，这是一种分层框架，它通过两项关键增强扩展了基于配置的 CTS-MAPF 规划范式：一种利用完备规划方法进行局部重规划的死锁智能体检测与释放机制，以及一种基于大邻域搜索（LNS）的随时优化程序。这些改进确保了在密集环境中的鲁棒性，并能够持续提升解的质量。在稀疏和密集基准测试上的广泛评估表明，与现有方法相比，CTS-PLL 实现了更高的成功率和解质量，同时保持了具有竞争力的运行效率。真实世界的机器人实验进一步证明了该方法在实践中的可行性。

## Abstract
The Collaborative Task Sequencing and Multi-Agent Path Finding (CTS-MAPF) problem requires agents to accomplish sequences of tasks while avoiding collisions, posing significant challenges due to its combinatorial complexity. This work introduces CTS-PLL, a hierarchical framework that extends the configuration-based CTS-MAPF planning paradigm with two key enhancements: a lock agents detection and release mechanism leveraging a complete planning method for local re-planning, and an anytime refinement procedure based on Large Neighborhood Search (LNS). These additions ensure robustness in dense environments and enable continuous improvement of solution quality. Extensive evaluations across sparse and dense benchmarks demonstrate that CTS-PLL achieves higher success rates and solution quality compared with existing methods, while maintaining competitive runtime efficiency. Real-world robot experiments further demonstrate the feasibility of the approach in practice.

---

## 论文详细总结（自动生成）

以下是对论文《CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：解决**协作任务排序与多智能体路径规划（CTS-MAPF）**问题。该问题要求多个智能体在避免碰撞的同时，按照特定顺序完成一系列协作任务，并最终到达目标点。
*   **研究动机**：
    *   **组合复杂性**：任务排序与路径规划的耦合导致搜索空间呈指数级增长。
    *   **现有方法局限**：基于冲突搜索（CBS）的方法在密集环境下扩展性差；基于配置（PIBT）的方法虽然高效，但由于缺乏完备性，容易陷入死锁（Deadlock）或活锁（Livelock），且难以持续优化解的质量。
    *   **实际需求**：仓储物流、机器人车队等场景需要既能快速生成可行解，又能随时间推移不断优化解质量的“随时（Anytime）”算法。

### 2. 方法论
CTS-PLL 采用分层架构，在 CTS-PIBT 的基础上引入了两个核心增强模块：
*   **死锁检测与释放模块（Lock Detect & Release）**：
    *   **检测**：当 Extended-PIBT 无法在规定步数内完成任务时，识别出处于停滞状态的智能体集合（包括未完成任务的活跃智能体和阻碍路径的被动智能体）。
    *   **释放**：利用具备完备性的 MAPF 算法（如 **LaCAM**）进行局部重规划。通过对停滞智能体的位置进行随机扰动（Permutation）作为局部目标，打破对称性并引导智能体跳出死锁区域。
*   **基于大邻域搜索（LNS）的随时优化模块**：
    *   在获得初始可行解后，通过“破坏-修复”循环不断降低总流失时间（Flowtime）。
    *   **邻域选择策略**：采用随机采样、基于交集的采样（针对拥堵点）和随机游走采样（针对延迟智能体）三种策略，并利用指数移动平均值动态调整各策略的权重。
    *   **修复**：使用优先级规划（PP）对选定子集进行重新路径规划。
*   **整体流程**：首先生成 Top-K 任务序列，对每个序列运行带死锁释放的路径规划，随后在剩余时间内通过 LNS 持续优化。

### 3. 实验设计
*   **实验场景**：使用标准 CTS-MAPF 数据集中的四种网格地图：`empty`（空旷）、`random`（随机障碍）、`room`（房间）和 `maze`（迷宫）。
*   **实验设置**：
    *   **稀疏场景**：5-20 个智能体，10-50 个任务。
    *   **稠密场景**：在最复杂的 `maze` 地图上设置 50 个智能体，60-180 个任务。
*   **对比方法（Benchmark）**：
    *   **CTS-CBS / CBSS**：基于冲突搜索的组合优化方法。
    *   **CTS-PIBT**：现有的高效配置类基准方法。
    *   **CTS-PIBT-anytime**：CTS-PIBT 的随时优化变体。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-12700KF CPU，32GB RAM，运行于 Linux 系统。
*   **算力说明**：论文未提及使用 GPU，该算法主要依赖 CPU 进行启发式搜索和逻辑计算。
*   **时间限制**：稀疏场景限时 60s（对比方法 180s），稠密场景限时 180s。

### 5. 实验数量与充分性
*   **实验规模**：针对每种（智能体数量，任务数量）组合，均生成了 **50 组随机实例**进行测试。
*   **消融实验**：设计了三个版本（v1：仅死锁释放；v2：死锁释放+多序列搜索；v3：全功能版含 LNS）来验证各模块的贡献。
*   **充分性评价**：实验覆盖了从简单到极高难度的多种拓扑结构，且包含了消融实验和**真实物理机器人（toio 机器人）验证**，实验设计客观、对比公平，样本量足以支撑结论。

### 6. 主要结论与发现
*   **成功率大幅提升**：在稀疏场景下，CTS-PLL 达到了 **100% 的成功率**，远超基于 CBS 的方法。在稠密场景下，相比 CTS-PIBT，成功率提升了 **28.95% 至 89.47%**。
*   **解质量优化**：通过 LNS 模块，CTS-PLL 在相同时间内生成的解（Flowtime）比 CTS-PIBT 优化了约 **1.25% 至 17.49%**。
*   **鲁棒性验证**：物理机器人实验证明，该算法能有效识别并处理现实中因优先级交替导致的活锁问题。

### 7. 优点
*   **鲁棒性强**：通过 LaCAM 局部重规划解决了配置类算法易陷入死锁的顽疾。
*   **随时性（Anytime）**：能够快速给出首个可行解，并随计算时间增加不断逼近更优解，适应工业实时性需求。
*   **扩展性好**：继承了 PIBT 的轻量级特性，能够处理大规模智能体和任务。

### 8. 不足与局限
*   **局部目标选择的随机性**：在死锁释放阶段，局部目标的生成依赖随机排列，虽然经验上有效，但在极端理论情况下可能仍需多次尝试才能成功。
*   **计算开销**：在极度拥堵的环境下，LNS 的修复过程（优先级规划）可能会因频繁冲突而变慢。
*   **动态环境适应性**：目前主要针对静态地图，未深入探讨任务动态到达或环境障碍物实时变化的情况。

（完）
