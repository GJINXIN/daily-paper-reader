---
title: Emergence of Preferential Attachment and Glass-Ceiling Effects in Autonomous Networks of LLMs
title_zh: 大语言模型（LLM）自主网络中优先连接与玻璃天花板效应的涌现
authors: "Yiming Zhang, Vikram Krishnamurthy"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01148v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 大模型网络中结构性差异的涌现
tldr: 本研究探讨了自主协作的大语言模型（LLM）智能体网络中结构性差异的演化。研究发现，当智能体自主选择合作伙伴时，网络呈现出“优胜劣汰”的偏好附着动态，且存在弱势模型占据核心地位的“玻璃天花板”效应。通过将网络建模为时变有向加权图并引入交叉注意力启发式效用函数，本文证明了智能体中心性的收敛性。实验表明，这种结构性差异受模型规模和提示词设计影响，且偏好附着与模型能力的匹配程度直接决定了集体任务的性能。
selection_source: fresh_fetch
motivation: 旨在探究LLM智能体在自主选择合作伙伴并形成协作网络时，是否会自发产生结构性的不平等及其背后的动力学机制。
method: 采用时变有向加权图建模通信网络，结合收敛映射理论证明稳定性，并利用100个智能体的实验平台进行验证。
result: 证实了网络中存在偏好附着现象和玻璃天花板效应，即较弱的模型有时会不成比例地占据中心位置，导致性能受损。
conclusion: 自主网络形成的中心性差异具有持久性，通过调整偏好附着机制使其与模型能力对齐，可以显著提升多智能体系统的整体协作效能。
---

## 摘要
我们研究了协作型大语言模型（LLM）智能体网络中结构性差异的涌现。当 LLM 智能体自主选择合作伙伴时，由此产生的通信网络表现出优先连接（preferential-attachment）动态：已经处于显著地位的智能体吸引额外连接的可能性越来越大。在某些情况下，较弱的 LLM 智能体（基础模型较小或版本较旧的智能体）相对于较强的 LLM 智能体，可能会不成比例地占据网络中心和具有影响力的位置。我们将此解释为一种依赖于类型的玻璃天花板效应（GCE）。我们将 LLM 智能体网络建模为随时间演化的有向加权图序列，其中向量值的边权重代表累计交换的 token 数、交互轮数和推理工作量。通过对平均场动态（mean-field dynamics）使用收缩映射论证，我们证明了每种智能体类型的重要性（中心性）会收敛至唯一的稳定均衡。为了将模型建立在 LLM 决策机制的基础上，我们引入了一种受交叉注意力（cross-attention）启发的合作伙伴选择效用函数。该效用函数规定了局部连接动态，并结合平均场模型，对极限网络结构及其依赖于类型的中心性差距进行了预测性表征。为了验证该理论，我们开发了一个包含 100 个 LLM 智能体的实验测试平台。我们的实验表明，自主网络形成会产生持久的中心性差异，其大小和方向取决于模型家族、模型大小、系统提示词设计和任务上下文。实验进一步表明，优先连接的效果取决于其与模型能力的匹配程度：当较强的智能体成为中心时，强化优先连接会提高集体性能；而当网络动态反而有利于较弱的智能体时，削弱优先连接则会提高性能。

## Abstract
We investigate the emergence of structural disparities in networks of collaborating large language model (LLM) agents. When LLM agents autonomously choose collaborators, the resulting communication network exhibits preferential-attachment dynamics: agents that are already prominent become increasingly likely to attract additional connections. In some cases, weaker LLM agents (agents with smaller base model or older version) can disproportionately occupy central and influential network positions relative to stronger LLM agents. We interpret this as a type-dependent glass-ceiling effect (GCE). We model the network of LLM agents as a time-evolving sequence of directed weighted graphs, where the vector-valued edge weights represent cumulative tokens exchanged, number of interaction rounds, and reasoning effort. Using a contraction mapping argument on the mean-field dynamics, we prove that the importance (centrality) of each agent type converges to a unique stable equilibrium. To ground the model in LLM decision mechanisms, we introduce a cross-attention-inspired utility for collaborator selection. This utility specifies the local connection dynamics and, together with the mean-field model, yields a predictive characterization of the limiting network structure and its type-dependent centrality gaps. To validate the theory, we develop an experimental testbed with 100 LLM agents. Our experiments show that autonomous network formation can generate persistent centrality disparities, with their magnitude and direction depending on model family, model size, system-prompt design, and task context. They further show that the effect of preferential attachment depends on its alignment with model capability: reinforcing it improves collective performance when stronger agents become central, whereas weakening it improves performance when network dynamics instead favor weaker agents.

---

## 论文详细总结（自动生成）

这篇论文对大语言模型（LLM）自主形成的协作网络进行了深入的社会学和动力学分析。以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当多个 LLM 智能体在没有预设通信拓扑的情况下自主选择合作伙伴时，是否会涌现出类似于人类社会的结构性不平等（如“贫者越贫，富者越富”的**优先连接**现象）？
*   **研究动机**：随着多智能体系统（MAS）的普及，研究者发现 LLM 不仅是工具，还表现出复杂的社交行为。论文旨在探讨这种自主形成的社交结构是否会导致**玻璃天花板效应（GCE）**，即能力较强的模型因网络结构原因被边缘化，而能力较弱的模型反而占据核心地位，以及这种结构对集体决策质量的影响。

### 2. 方法论
*   **核心思想**：将 LLM 网络建模为一个随时间演化的有向加权图，利用**平均场动力学（Mean-field Dynamics）**来描述不同类型智能体中心性的演化过程。
*   **关键技术细节**：
    *   **向量值边权重**：不同于传统的 0/1 连接，论文使用多维向量表示通信强度，包括 Token 交换量、交互轮数和推理努力程度。
    *   **交叉注意力启发式效用（Cross-attention Utility）**：模拟 LLM 的决策机制。目标智能体发出“查询（Query）”表示信息需求，源智能体提供“键（Key）”表示相关性，两者的匹配度决定了连接权重。
    *   **平均场 ODE 模型**：通过随机逼近理论，将复杂的局部交互抽象为常微分方程（ODE）。利用收缩映射原理证明了网络中心性会收敛到一个唯一的稳定均衡点。
    *   **GCE 量化**：定义了“平均玻璃天花板效应”（长期影响比率差异）和“尾部玻璃天花板效应”（高影响力位置的排他性）。

### 3. 实验设计
*   **实验场景**：
    1.  **协作问答（Collaborative QA）**：智能体持有碎片化信息，需通过咨询他人合成最终答案。
    2.  **多智能体辩论（Multi-agent Debate）**：智能体相互挑战和修正论点。
*   **数据集**：使用合成的 QA 数据集，包含 5 个互补的上下文片段，确保单一智能体无法独立回答。
*   **对比设置**：
    *   **能力对齐（Meritocracy）**：对比同系列不同规模模型（如 GPT-4.1 vs GPT-4.1-mini）或闭源 vs 开源模型。
    *   **能力错位（GCE）**：通过系统提示词（System Prompt）分配角色（如“解释者” vs “验证者”），观察即使基础模型相同或弱模型担任特定角色时，是否会产生结构性优势。
*   **干预实验**：引入偏好附着偏置系数 $\beta$，测试强化或削弱这种结构性差异对系统准确率的影响。

### 4. 资源与算力
*   **模型使用**：实验涵盖了 Gemini (2.5/3.5 Flash), GPT (4.1/mini), Grok, LLaMA (70B/8B), Qwen, Mistral 等主流模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于实验涉及 100 个智能体的频繁 API 调用和交互，其主要成本在于推理 API 的消耗而非本地训练算力。

### 5. 实验数量与充分性
*   **实验规模**：构建了一个包含 **100 个 LLM 智能体**的测试平台。
*   **重复性**：每个案例均进行了 **50 次独立运行**以获取平均轨迹和误差条。
*   **充分性**：实验覆盖了 6 个模型家族、多种角色组合以及两种不同的下游任务。通过 Ljung-Box 检验等统计手段验证了 ODE 模型的预测准确性。整体实验设计客观、公平，充分考虑了统计显著性。

### 6. 主要结论与发现
*   **优先连接的普遍性**：LLM 智能体在自主协作中自发形成“中心-外围”结构，少数智能体吸引了大部分通信流量。
*   **玻璃天花板效应的涌现**：提示词定义的角色（如“解释者”）往往比模型本身的实际能力更能决定其在网络中的地位。在某些配置下，较弱的模型会占据中心位置，压制强模型。
*   **信息放大器效应**：中心节点是信息的放大器。如果强模型处于中心，真理传播更快；如果弱模型（易幻觉）处于中心，幻觉会迅速污染整个网络。
*   **性能优化**：在能力对齐的情况下，强化优先连接能提升性能；但在 GCE 存在的情况下，削弱这种偏好、打破“玻璃天花板”反而能显著提高集体准确率。

### 7. 优点
*   **理论与实证结合**：不仅有严谨的数学证明（收敛性、稳定性），还有大规模的实验验证。
*   **新颖的视角**：首次将社会学中的“玻璃天花板”概念引入 LLM 多智能体网络分析，揭示了提示词工程对网络结构的潜在负面影响。
*   **动态建模**：使用向量值权重和连续时间 ODE，比传统的静态图分析更贴近真实的 LLM 交互过程。

### 8. 不足与局限
*   **类型限制**：目前的理论框架主要集中在两种类型的智能体（R 和 B），对于更复杂的多类型（Multi-type）社会的扩展讨论较少。
*   **数据集局限**：主要依赖合成数据集，在更复杂、开放领域的真实世界任务中的表现尚待验证。
*   **成本风险**：自主网络形成过程涉及大量交互，在大规模部署时可能面临极高的 API 成本和延迟问题。

（完）
