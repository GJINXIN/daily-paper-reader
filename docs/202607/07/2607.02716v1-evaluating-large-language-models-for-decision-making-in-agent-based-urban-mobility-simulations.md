---
title: Evaluating Large Language Models for Decision-Making in Agent-Based Urban Mobility Simulations
title_zh: 评估大语言模型在基于智能体的城市交通模拟决策中的应用
authors: "Bruno Cascaes Alves, Míriam Blank Born, Ulisses Gilioli Francescatto Júnior, Felipe Moura Goulart, Letícia Brandão Caldas, Marilton Sanchotene de Aguiar"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02716v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 大模型用于基于智能体的城市模拟决策
tldr: 针对城市交通模拟中规则化决策缺乏灵活性的问题，本研究提出一种将大语言模型（LLM）集成到多智能体系统（MAS）中的混合架构。通过将 GAMA 平台与 LLM 模块连接，使智能体具备基于上下文的路径重规划决策能力，并引入持久化记忆以增强行为一致性。实验表明，LLM 显著提升了智能体在道路阻塞等动态场景下的适应性，为复杂空间决策建模提供了新思路。
selection_source: fresh_fetch
motivation: 传统的基于规则的城市交通模拟方法依赖固定启发式算法，难以模拟智能体在动态环境中的自适应决策过程。
method: 开发了一种连接 GAMA 平台与 LLM 的混合架构，利用 LLM 作为决策层指导路径重规划，并引入持久化记忆以维持行为一致性。
result: 在道路阻塞场景下，LLM 驱动的智能体展现出比规则化方法更高的上下文感知能力和决策灵活性，且记忆机制对性能有显著影响。
conclusion: LLM 能够作为有效的认知层集成到城市交通模拟中，显著丰富多智能体系统在复杂空间环境下的行为表现。
---

## 摘要
城市交通建模在表征动态环境下的决策方面面临挑战。尽管多智能体系统（MAS）被广泛应用，但基于规则的方法依赖于固定的启发式规则，限制了自适应行为。本研究探讨了将大语言模型（LLMs）作为决策组件集成到多智能体模拟中的方法。我们提出了一种混合架构，通过 API 将 GAMA 平台与外部基于 LLM 的模块连接起来，使智能体能够确定是否需要进行路径重规划。LLM 并非取代路径规划算法，而是作为一个决策层来引导重规划行为。该方法结合了持久化记忆，允许过去的交互影响未来的决策，并促进行为的一致性。我们在多种道路封锁场景和人口规模下，对比了基于规则的方法与 LLM 辅助的方法。结果表明，启用 LLM 的智能体表现出更强的自适应能力和上下文感知能力，特别是在路径灵活性较高的场景中。记忆会影响性能和行为一致性，其效果随配置的不同而变化。总体而言，LLM 作为补充性的认知层，丰富了城市交通模拟中的行为表征，并具有在空间多智能体系统中建模复杂决策的潜力。

## Abstract
Urban mobility modeling faces challenges in representing decision-making in dynamic environments. Although Multi-Agent Systems are widely used, rule-based approaches rely on fixed heuristics that limit adaptive behavior. This work investigates the integration of Large Language Models (LLMs) as decision-making components in multi-agent simulations. We propose a hybrid architecture that connects the GAMA platform to an external LLM-based module through an API, enabling agents to determine whether route replanning is necessary. Rather than replacing routing algorithms, the LLM serves as a decision layer that guides replanning behavior. The approach incorporates persistent memory, allowing past interactions to influence future decisions and promote behavioral consistency. We compare rule-based and LLM-assisted approaches across multiple road-blockage scenarios and population scales. Results indicate that LLM-enabled agents exhibit greater adaptability and contextual awareness, particularly in scenarios with higher route flexibility. Memory influences performance and behavioral consistency, with effects varying across configurations. Overall, LLMs serve as complementary cognitive layers that enrich behavioral representations in urban mobility simulations and hold potential for modeling complex decision-making in spatial multi-agent systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluating Large Language Models for Decision-Making in Agent-Based Urban Mobility Simulations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的城市交通多智能体系统（MAS）模拟通常依赖于预定义的规则和固定启发式算法。这种方法在面对复杂的动态环境（如突发路障、天气变化）时，难以表现出人类驾驶员那种基于上下文的自适应决策能力。
*   **核心问题**：探讨大语言模型（LLM）是否可以作为智能体的“认知层”，在空间约束的交通模拟中提升决策的灵活性和真实性，特别是针对“是否需要重新规划路径”这一关键决策点。

### 2. 方法论：核心思想与技术细节
论文提出了一种**混合架构**，将空间模拟平台与认知推理框架解耦：
*   **核心思想**：LLM 不直接计算最短路径（这由传统算法完成），而是作为一个**决策过滤器**。当智能体遇到交通事件时，LLM 根据智能体属性（职业、信任度、疲劳度）和环境上下文（天气、拥堵、距离）决定是“维持原路”还是“触发重规划”。
*   **关键技术组件**：
    *   **GAMA Platform**：负责底层的空间模拟、地理信息系统（GIS）数据处理和智能体物理移动。
    *   **Integration Layer (FastAPI)**：作为中介，处理 GAMA 与外部 LLM 模块之间的异步通信，并管理关系数据库以记录决策日志。
    *   **Agentic Framework (Agno)**：实现认知逻辑。利用 LLM 进行推理，并引入**持久化语义记忆（Semantic Memory）**，使智能体能参考过去的经验来优化当前决策。
*   **决策流程**：事件触发 -> 提取上下文 -> 构造 Prompt -> LLM 推理 -> 返回结构化 JSON 决策 -> GAMA 执行动作。

### 3. 实验设计
*   **实验场景**：
    1.  **局部阻塞（Localized Blockage）**：点状路障，有较多替代路径。
    2.  **扩展阻塞（Extended Blockage）**：关键干道大规模封锁，替代路径极少。
*   **Benchmark 与对比方法**：
    *   **Baseline**：基于固定阈值启发式规则的模型。
    *   **LLM 辅助模型**：分别测试了 **Gemini 1.5 Flash-Lite** 和 **GPT-4o Mini**。
    *   **消融变量**：针对 LLM 模型，对比了“开启记忆”与“关闭记忆”两种配置。
*   **评估指标**：重规划率、到达率（Arrival Rate）、拥堵时间比（Stuck Time Ratio）、模拟总时长、决策错误率。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**本地 GPU 的具体型号或训练时长。
*   **资源使用**：由于采用了 Agentic 架构，主要的计算开销集中在通过 API 调用云端 LLM（Gemini 和 GPT-4o Mini）。论文记录了模拟执行时间，显示 LLM 接入后模拟速度显著变慢（从约 20-60 秒增加到 100-240 秒）。

### 5. 实验数量与充分性
*   **实验规模**：针对每种配置（模型 x 场景 x 人口规模 x 记忆开关）运行了 **10 个独立随机种子**，并报告了平均值和标准差。
*   **充分性评价**：实验设计较为客观，涵盖了不同的人口规模（500 和 1000 智能体）和不同难度的交通场景。通过对比不同厂商的 LLM，验证了结论的普适性。然而，受限于 API 成本和响应速度，实验的人口规模（最高 1000）相对于真实城市模拟仍偏小。

### 6. 主要结论与发现
*   **自适应性提升**：在局部阻塞场景下，LLM 驱动的智能体能更早避开拥堵，显著提升了到达率（部分配置下从 70% 提升至 90%）。
*   **记忆的作用**：语义记忆充当了“行为稳定器”，减少了过度反应的决策，使智能体行为在时间维度上更具一致性。
*   **结构性限制**：在扩展阻塞（极端封锁）场景下，尽管 LLM 优化了局部避堵，但受限于道路网本身的物理结构限制，整体到达率的提升有限。
*   **异质性表现**：LLM 能够根据智能体画像（如学生、工人、退休人员）产生差异化的决策反馈，增加了模拟的社会学真实性。

### 7. 优点（亮点）
*   **架构解耦**：将计算密集型的空间模拟与逻辑密集型的 LLM 推理分离，保证了空间一致性，同时引入了高级认知能力。
*   **引入记忆机制**：通过 Agno 框架实现的语义记忆，解决了 LLM 智能体在长时间模拟中容易出现的“决策漂移”或前后矛盾的问题。
*   **实用性导向**：LLM 仅负责决策而非路径计算，这种“混合动力”模式在现有的技术条件下具有较高的工程可行性。

### 8. 不足与局限
*   **计算开销巨大**：引入 LLM 导致模拟时间增加了 3 到 5 倍，这限制了其在大规模城市（如数万名智能体）实时模拟中的应用。
*   **API 依赖与成本**：依赖外部 API 可能带来延迟和数据隐私风险，且大规模实验的 Token 成本较高。
*   **环境约束的压制**：实验表明，当城市基础设施面临极端压力时，个体智能体的“聪明决策”往往会被物理环境的“结构性僵局”抵消，LLM 的优势在此时不明显。

（完）
