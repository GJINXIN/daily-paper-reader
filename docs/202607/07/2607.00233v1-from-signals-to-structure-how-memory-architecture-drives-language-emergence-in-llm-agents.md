---
title: "From Signals to Structure: How Memory Architecture Drives Language Emergence in LLM Agents"
title_zh: 从信号到结构：记忆架构如何驱动 LLM 智能体中的语言涌现
authors: "Yashar Talebirad, Eden Redman, Ali Parsaee, Osmar R. Zaiane"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00233v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: LLM智能体中的语言涌现
tldr: 本研究探讨了大语言模型（LLM）智能体在路易斯信号博弈中如何从零开始构建共享语言。通过对比五种不同的记忆架构在不同信道容量下的表现，研究发现记忆架构对语言涌现的影响超过了信道容量。特别是拥有持久私有笔记本的智能体能有效利用冗余容量，避免了无状态智能体在词汇量过大时出现的协调崩溃，实现了最稳定的协作，证明了外部化记忆在语言规范形成中的关键作用。
selection_source: fresh_fetch
motivation: 探究LLM智能体的记忆架构如何影响其在交互过程中从零开始建立共享语言规范的能力。
method: 在路易斯信号博弈中对比了五种记忆架构在不同信道容量下对智能体协调效率的影响。
result: 持久性私有笔记本架构能有效防止高容量下的协调崩溃，而无状态智能体在词汇量过大时表现会显著下降。
conclusion: 记忆架构比信道容量更能决定语言的涌现，且冗余的信道容量比理论上的信息瓶颈更有利于建立稳定的语言规范。
---

## 摘要
两个智能体如何从零开始发明一种共享语言？在刘易斯信号博弈（Lewis signaling game）中，发送者和接收者必须仅依靠其交互历史来协调出一套编码。我们研究了 LLM 智能体在不同信道配置下的五种记忆架构，发现记忆架构比信道容量更为重要。拥有持久化私有笔记本（private notebook）的智能体能从过剩的信道容量中获益，并避免了在无状态智能体中观察到的高容量崩溃现象，实现了最可靠的协调（在容量为 25 时达到 $0.867 \pm 0.023$）。无状态智能体在适中容量时达到峰值，随后随着词汇量增长超出滚动上下文窗口所能追踪的范围而性能下降。笔记本将学到的惯例外部化，使智能体无需在每一轮中重新推导编码。一个受信息瓶颈（information bottleneck）启发的论点预测，最优容量应等于对象的数量。然而，实验证明该瓶颈（容量为 8）是一个脆弱点，而过剩的容量通常表现更好。我们表明，仅凭信道容量无法预测协调效果；记忆架构决定了智能体是否能将交互历史转化为稳定的惯例，而理解信号如何演变为语言需要同时考虑这两个维度。

## Abstract
How do two agents invent a shared language from scratch? In a Lewis signaling game, a sender and receiver must coordinate on a code using only their interaction history. We study five memory architectures across varying channel configurations with LLM agents and find that memory architecture matters more than channel capacity. Agents with a persistent private notebook benefit from surplus channel capacity and avoid the high-capacity collapse seen in stateless agents, achieving the most reliable coordination ($0.867 \pm 0.023$ at capacity = 25). Stateless agents peak at moderate capacity and then degrade as the vocabulary grows beyond what a rolling context window can track The notebook externalizes learned conventions, freeing agents from having to re-derive codes each round. An information bottleneck-inspired argument predicts an optimal capacity equal to the number of objects. Instead, the bottleneck (capacity = 8) proves to be a fragility point, and surplus capacity is generally better. We show that channel capacity alone cannot predict coordination; memory architecture determines whether agents turn interaction history into stable conventions, and both dimensions are needed to understand how signals become language.

---

## 论文详细总结（自动生成）

这篇论文探讨了大型语言模型（LLM）智能体在交互过程中如何通过不同的记忆机制从零开始构建共享语言。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的涌现通信（Emergent Communication）研究多基于梯度下降训练的神经智能体，而 LLM 智能体通过上下文学习（In-context Learning）进行自适应。研究者希望探索：在没有预设含义的情况下，LLM 智能体如何通过交互历史发明代码？
*   **核心问题**：在刘易斯信号博弈（Lewis signaling game）中，是**信道容量**（信号空间的大小）还是**记忆架构**（智能体如何存储和处理历史信息）决定了语言的涌现和协调效率？
*   **背景**：信息瓶颈（Information Bottleneck）理论通常预测当信道容量等于对象数量时效率最高，但这一结论在具备推理能力的 LLM 智能体身上是否成立尚不明确。

### 2. 方法论
*   **核心思想**：对比五种不同的记忆架构，观察它们在不同信道容量（$|V|^L$，即词汇量 $V$ 的消息长度 $L$ 次方）下的表现。
*   **关键技术细节（五种记忆架构）**：
    1.  **仅记忆（Memory only）**：无状态架构，仅提供最近 20 轮交互的滚动窗口。
    2.  **环境看板（Env board）**：共享的公共表格，记录成功的映射（作为基准）。
    3.  **草稿纸（Scratchpad）**：持久化的私有自由文本笔记本，每轮可重写，用于记录学到的规范。
    4.  **代码本（Codebook）**：固定容量的槽位列表（10个槽位），通过结构化操作（添加、编辑、无）进行更新。
    5.  **元代码本（Codebook meta）**：在代码本基础上增加了一个持久化的元注释字符串。
*   **算法流程**：发送者观察目标对象并发送符号信号；接收者根据信号从候选者中选择目标；双方获得反馈并更新各自的私有记忆。

### 3. 实验设计
*   **场景设置**：8 个对象（由颜色、形状、大小三种二元特征组成）。每轮从 8 个对象中选出 4 个作为候选，其中 1 个为目标。
*   **模型与参数**：使用 `gpt-5.4-mini` 作为基础模型，温度设为 1.0。
*   **实验分支**：
    *   **研究 1**：固定容量（27），对比五种记忆架构。
    *   **研究 2**：容量扫描（从 4 到 125），对比“草稿纸”与“仅记忆”模式。
    *   **研究 3**：窗口大小扫描（5, 10, 20, 40 轮），区分“巩固能力”与“历史长度”的影响。
*   **衡量指标**：准确率（Accuracy）、拓扑相似性（TopSim，衡量组合性）、互信息（MI）、碰撞率（Collision rate）。

### 4. 资源与算力
*   **模型**：实验完全基于 `gpt-5.4-mini` 的 API 调用。
*   **算力说明**：文中未明确提及具体的 GPU 型号、数量或总训练时长。由于使用的是闭源模型 API，主要的资源消耗体现在 API Token 的使用上。

### 5. 实验数量与充分性
*   **实验规模**：每组实验运行 200 轮。
*   **重复性**：关键实验（如容量为 8 的瓶颈测试）使用了 8 个随机种子以捕捉分布特征；其他主要实验使用了 3 个随机种子。
*   **充分性**：实验设计涵盖了从极度受限到极度冗余的信道容量，并进行了消融实验（如窗口大小测试），逻辑严密。但受限于 API 成本，部分容量配置仅使用了单种子运行，作者在文中也承认了这一点。

### 6. 主要结论与发现
*   **记忆架构胜过信道容量**：记忆架构决定了容量-性能曲线的形状，而不仅仅是平移曲线。
*   **持久化记忆的优势**：拥有“草稿纸”（笔记本）的智能体能有效利用冗余容量，避免了无状态智能体在高容量下的“协调崩溃”。
*   **信息瓶颈是脆弱点**：当容量恰好等于对象数（Cap=8）时，表现出极大的不稳定性（双峰分布），并非理论预测的最优解。
*   **冗余是有益的**：过剩的信道容量有助于智能体修复早期的错误映射，从而建立更稳定的规范。
*   **巩固 vs 历史**：高容量下的失败是由于缺乏“巩固”机制（将历史转化为稳定规范），而非单纯的上下文窗口不够长。

### 7. 优点
*   **视角新颖**：首次系统性地研究了记忆架构对 LLM 涌现通信的影响，而非仅仅关注模型规模或信道参数。
*   **理论挑战**：挑战了传统信息瓶颈理论在 LLM 智能体场景下的适用性，提出了“脆弱点”而非“最优解”的观点。
*   **细致的指标分析**：通过 TopSim 和碰撞率深入剖析了语言的结构化程度，而非仅看任务成功率。

### 8. 不足与局限
*   **模型单一性**：所有实验均基于 `gpt-5.4-mini`，结果可能受到该特定模型训练偏置的影响。
*   **对象空间较小**：仅使用了 8 个对象的简单空间，在更复杂、更具层次性的语义空间中，结论是否成立尚待验证。
*   **规范漂移（Convention Drift）**：研究发现即使有笔记本，智能体在后期仍可能无故修改已建立的规范，导致性能下降，目前尚无完美的解决机制。
*   **统计显著性**：部分实验分支的样本量（种子数）较少，结果更多体现为趋势而非绝对的统计定论。

（完）
