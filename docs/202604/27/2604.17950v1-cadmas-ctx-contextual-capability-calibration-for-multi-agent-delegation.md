---
title: "CADMAS-CTX: Contextual Capability Calibration for Multi-Agent Delegation"
title_zh: CADMAS-CTX：多智能体委派的上下文能力校准
authors: Chuhan Qiao
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17950v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 多智能体委派的上下文能力校准
tldr: 本研究针对多智能体协作中的任务委派问题，提出CADMAS-CTX框架。传统方法假设智能体能力固定，而本项目认为能力受任务上下文（如代码长度、任务深度）影响。该框架通过Beta后验分布建立分层上下文能力画像，并结合风险感知评分进行委派决策。理论证明了上下文感知路由的优越性，实验显示在GAIA和SWE-bench上显著优于静态基准和AutoGen，提升了多智能体团队协作的效率与鲁棒性。
selection_source: fresh_fetch
motivation: 传统的静态技能画像忽略了任务上下文对智能体能力的动态影响，容易导致系统性的委派错误。
method: 提出基于Beta后验的分层上下文能力画像，并利用结合不确定性惩罚的风险感知评分进行委派决策。
result: "在GAIA基准上准确率提升至0.442，在SWE-bench Lite上解决率从22.3%显著提升至31.4%。"
conclusion: 上下文校准和风险感知委派机制能显著优化多智能体团队协作，优于传统的全局技能分配方法。
---

## 摘要
我们在一个更强且更现实的假设下重新审视多智能体委派：智能体的能力并非固定在技能层面，而是取决于任务上下文。例如，编程智能体可能擅长简短的独立编辑，但在长程调试中表现不佳；规划器可能在浅层任务上表现良好，但在链式依赖任务中性能下降。因此，静态的技能级能力画像是对异构情况的平均化，可能导致系统性的错误委派。我们提出了 CADMAS-CTX，一个用于上下文能力校准的框架。对于每个智能体、技能和粗粒度上下文桶，CADMAS-CTX 维护一个 Beta 后验分布，以捕获该任务空间部分的稳定经验。委派决策由一个风险感知评分做出，该评分结合了后验均值和不确定性惩罚，使得智能体仅在同行表现更好且该评估有充分证据支持时才进行委派。本文有三项贡献：首先，分层上下文能力画像用上下文条件后验取代了静态的技能级置信度；其次，基于上下文多臂老虎机理论，我们正式证明了在足够的上下文异构性下，上下文感知路由比静态路由具有更低的累积遗憾，从而形式化了偏差-方差权衡；第三，我们在 GAIA 和 SWE-bench 基准测试上验证了我们的方法。在配备 GPT-4o 智能体的 GAIA 上，CADMAS-CTX 达到了 0.442 的准确率，优于静态基线（0.381）和 AutoGen（0.354），且具有不重叠的 95% 置信区间。在 SWE-bench Lite 上，它将解决率从 22.3% 提高到 31.4%。消融实验表明，不确定性惩罚提高了对上下文标记噪声的鲁棒性。我们的结果表明，与静态全局技能分配相比，上下文校准和风险感知委派显著提升了多智能体协作的效果。

## Abstract
We revisit multi-agent delegation under a stronger and more realistic assumption: an agent's capability is not fixed at the skill level, but depends on task context. A coding agent may excel at short standalone edits yet fail on long-horizon debugging; a planner may perform well on shallow tasks yet degrade on chained dependencies. Static skill-level capability profiles therefore average over heterogeneous situations and can induce systematic misdelegation. We propose CADMAS-CTX, a framework for contextual capability calibration. For each agent, skill, and coarse context bucket, CADMAS-CTX maintains a Beta posterior that captures stable experience in that part of the task space. Delegation is then made by a risk-aware score that combines the posterior mean with an uncertainty penalty, so that agents delegate only when a peer appears better and that assessment is sufficiently well supported by evidence. This paper makes three contributions. First, a hierarchical contextual capability profile replaces static skill-level confidence with context-conditioned posteriors. Second, based on contextual bandit theory, we formally prove context-aware routing achieves lower cumulative regret than static routing under sufficient context heterogeneity, formalizing the bias-variance tradeoff. Third, we empirically validate our method on GAIA and SWE-bench benchmarks. On GAIA with GPT-4o agents, CADMAS-CTX achieves 0.442 accuracy, outperforming static baseline 0.381 and AutoGen 0.354 with non-overlapping 95% confidence intervals. On SWE-bench Lite, it improves resolve rate from 22.3% to 31.4%. Ablations show the uncertainty penalty improves robustness against context tagging noise. Our results demonstrate contextual calibration and risk-aware delegation significantly improve multi-agent teamwork compared with static global skill assignments.

---

## 论文详细总结（自动生成）

以下是对论文《CADMAS-CTX: Contextual Capability Calibration for Multi-Agent Delegation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注多智能体系统（MAS）中的**任务委派（Delegation）**问题。
*   **研究动机**：现有的多智能体框架（如 AutoGen, MetaGPT）通常假设智能体的能力是静态的或仅基于技能标签（如“代码专家”）。然而，LLM 智能体的表现高度依赖于**任务上下文（Context）**。例如，一个编程智能体可能擅长短代码编辑，但在长程调试中失败。
*   **核心问题**：如果将不同上下文下的表现混为一谈，系统会得到一个平均化的错误能力评估，从而导致“系统性误委派”。论文旨在解决“在什么条件下信任谁”的问题，而非仅仅是“谁擅长什么技能”。

### 2. 方法论：核心思想与关键技术
论文提出了 **CADMAS-CTX** 框架，其核心思想是将任务空间划分为粗粒度的上下文桶，并为每个智能体维护动态更新的能力画像。
*   **分层上下文能力画像**：为每个“智能体-技能-上下文桶”组合维护一个 **Beta 后验分布**。上下文桶（Bucket）由难度（易/中/难）、依赖关系（独立/链式）和工具使用（是/否）等维度构成。
*   **风险感知评分函数**：委派决策基于置信下界（LCB）逻辑。评分公式为：`Score = 𝜇 (后验均值) - 𝛾 * √𝑢 (不确定性惩罚)`。其中 $\gamma$ 是控制探索与利用平衡的超参数。
*   **去中心化入口选择与委派**：
    1.  **任务入口**：通过上述评分函数在候选池中选出初始协调者。
    2.  **任务分解与路由**：协调者将任务分解为子任务，并根据评分决定是“自行执行”还是“委派给同行”。
    3.  **经验驱动校准**：任务完成后，根据成功/失败结果通过 Beta-Binomial 逻辑更新对应的后验分布。
*   **冷启动转移**：引入经验贝叶斯收缩机制，允许在数据稀疏时从相邻上下文桶“借用”伪计数，缓解冷启动问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **受控仿真**：验证在上下文翻转（Context Flip）和稀疏数据下的理论表现。
    *   **GAIA (General AI Assistants)**：评估在多步骤工具使用任务中的表现。
    *   **SWE-bench Lite**：评估在长程软件工程任务（解决 GitHub 真实问题）中的表现。
*   **对比方法（Baselines）**：
    *   **Monolithic**：单智能体执行。
    *   **Static**：基于技能的静态校准（忽略上下文）。
    *   **AutoGen**：默认的 GroupChat 编排模式。
    *   **Learned Router**：基于逻辑回归的离线学习路由。
    *   **Thompson Sampling**：基于概率采样的委派策略。
*   **底层模型**：使用了 GPT-4o 和 Claude 3.5 Sonnet 作为智能体背后的基础模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的本地 GPU 型号或训练时长。
*   **资源消耗**：由于实验主要基于闭源模型（GPT-4o, Claude 3.5），其资源消耗主要体现在 API 调用成本上。论文在 GAIA 实验中对比了 API 调用次数，显示 CADMAS-CTX 的调用频率（5.1次/任务）远低于 AutoGen（18.4次/任务）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验运行了 200 个任务，重复 30 个随机种子。
    *   GAIA 验证集（165个任务）重复 5 轮实验。
    *   SWE-bench Lite（300个任务）重复 5 轮实验。
*   **充分性评价**：实验设计较为充分且客观。作者不仅提供了端到端的性能指标，还通过**消融实验**分析了标记噪声（Tagging Noise）、桶粒度（Granularity）以及不确定性惩罚项的影响。此外，论文使用了 95% 置信区间（CI）和自助法（Bootstrap）进行统计显著性检验，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能提升**：在 GAIA 上，CADMAS-CTX 的准确率（0.442）显著高于静态基线（0.381）和 AutoGen（0.354）。在 SWE-bench 上，解决率从 22.3% 提升至 31.4%。
*   **理论验证**：证明了在上下文异构性存在时，静态路由会产生线性遗憾（Linear Regret），而上下文感知路由可实现次线性遗憾。
*   **鲁棒性**：不确定性惩罚项能有效防止在证据不足时过度委派，即使在上下文标记存在 30% 噪声的情况下，系统依然能保持稳健表现。
*   **去中心化优势**：在能力发生动态漂移（如 API 降级）时，去中心化的评分机制比中心化协调器响应更快。

### 7. 优点
*   **理论与实践结合**：将经典的上下文多臂老虎机（Contextual Bandits）理论成功应用于现代 LLM 多智能体编排。
*   **风险感知**：引入不确定性惩罚，解决了 LLM 智能体普遍存在的“过度自信”和“幻觉自评”问题。
*   **高效性**：相比于 AutoGen 等多轮对话框架，通过单次委派逻辑显著降低了 API 成本。

### 8. 不足与局限
*   **桶定义的启发式**：上下文桶的划分（如难度、依赖性）目前依赖于预定义的启发式规则或简单的分类器，可能无法捕捉更复杂的隐式上下文。
*   **边界效应**：离散的桶划分可能在上下文边界处出现性能波动。
*   **技能覆盖**：虽然在代码和通用任务上做了验证，但在更多样化的技能领域（如多模态、创意写作）的泛化性尚待进一步测试。
*   **依赖反馈**：系统高度依赖于任务结果的准确判定（Judge），如果判定环节出错，校准过程会受到干扰。

（完）
