---
title: "Rethinking Failure Attribution in Multi-Agent Systems: A Multi-Perspective Benchmark and Evaluation"
title_zh: 重新审视多智能体系统中的故障归因：多视角基准测试与评估
authors: "Yeonjun In, Mehrab Tanjim, Jayakumar Subramanian, Sungchul Kim, Uttaran Bhattacharya, Wonjoong Kim, Sangwu Park, Somdeb Sarkhel, Chanyoung Park"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25001v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 多智能体系统的故障归因基准
tldr: 针对多智能体系统（MAS）故障归因中存在的单一根因假设与实际复杂性不符的问题，本文提出了“多视角故障归因”范式。通过构建首个多视角基准测试集 MP-Bench 及配套评估协议，研究发现以往认为大模型（LLM）难以胜任故障归因的结论主要源于基准设计的局限性。该研究为实现更真实、可靠的 MAS 调试提供了新工具和视角。
selection_source: fresh_fetch
motivation: 现有的 MAS 故障归因基准通常假设单一确定根因，忽略了复杂交互中存在多个合理解释的实际情况。
method: 提出了多视角故障归因范式，并开发了 MP-Bench 基准测试集及一套专门针对多视角场景的评估协议。
result: 实验表明，在多视角评估下，大模型在故障归因任务上的表现优于此前研究的预期，证明了基准设计的重要性。
conclusion: 多视角基准和评估协议对于真实、可靠的多智能体系统故障诊断与调试至关重要。
---

## 摘要
故障归因对于诊断和改进多智能体系统（MAS）至关重要，然而现有的基准测试和方法在很大程度上假设每个故障都存在单一且确定的根本原因。在实践中，由于智能体之间复杂的依赖关系和模糊的执行轨迹，MAS 故障往往存在多种合理的归因。我们从多视角的立场重新审视了 MAS 故障归因，并提出了多视角故障归因，这是一种明确考虑归因歧义性的实用范式。为了支持这一设定，我们推出了 MP-Bench，这是首个专为 MAS 多视角故障归因设计的基准测试，并配备了针对该范式量身定制的新评估协议。通过广泛的实验，我们发现先前认为大语言模型（LLM）难以进行故障归因的结论，在很大程度上是由现有基准测试设计的局限性所导致的。我们的结果强调了多视角基准测试和评估协议对于实现现实且可靠的 MAS 调试的必要性。

## Abstract
Failure attribution is essential for diagnosing and improving multi-agent systems (MAS), yet existing benchmarks and methods largely assume a single deterministic root cause for each failure. In practice, MAS failures often admit multiple plausible attributions due to complex inter-agent dependencies and ambiguous execution trajectories. We revisit MAS failure attribution from a multi-perspective standpoint and propose multi-perspective failure attribution, a practical paradigm that explicitly accounts for attribution ambiguity. To support this setting, we introduce MP-Bench, the first benchmark designed for multi-perspective failure attribution in MAS, along with a new evaluation protocol tailored to this paradigm. Through extensive experiments, we find that prior conclusions suggesting LLMs struggle with failure attribution are largely driven by limitations in existing benchmark designs. Our results highlight the necessity of multi-perspective benchmarks and evaluation protocols for realistic and reliable MAS debugging.

---

## 论文详细总结（自动生成）

### 论文总结：重新审视多智能体系统中的故障归因

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）驱动的多智能体系统（MAS）广泛应用，如何诊断系统执行失败的原因（故障归因）变得至关重要。
*   **研究动机**：现有的故障归因基准（如 *Who&When*）通常假设每个失败案例都有一个**唯一且确定**的根因步骤。然而，在实际的 MAS 中，由于智能体间复杂的依赖关系和模糊的执行轨迹，不同的分析者基于对“正确策略”的不同理解，往往会给出多个合理但不同的归因结果。
*   **核心问题**：单一视角的确定性评估忽略了归因的歧义性，导致现有研究误认为 LLM 在故障归因任务上表现极差（接近随机）。本文旨在通过引入“多视角”范式，重新评估 LLM 的诊断能力。

#### 2. 方法论：核心思想与关键技术
论文提出了**多视角故障归因（Multi-Perspective Failure Attribution）**范式，并构建了配套的基准测试和评估协议：
*   **核心思想**：将故障归因从“寻找唯一标签”转变为“识别一组合理的候选步骤及其推理理由”。
*   **MP-Bench 基准构建**：
    *   **专家标注**：聘请 3 名资深 AI 专家，对 289 个 MAS 执行日志进行独立标注。每个步骤包含：是否故障、故障原因、理想动作。
    *   **标注整合**：计算每个步骤的“共识率”（Consensus Rate），并据此对故障步骤进行排名。100% 共识代表“视角无关”的核心故障，低共识代表“视角相关”的歧义故障。
*   **评估协议**：
    *   **排名评估**：通过对 LLM 进行 $N$ 次随机采样（Temperature > 0），汇总其预测结果并生成排名，使用 **nDCG@K** 指标衡量其与专家共识排名的一致性。
    *   **推理评估**：采用 **LLM-as-a-Judge**（如 GPT-5.1）对模型生成的故障原因和理想动作进行评分（1-10分），评估其逻辑合理性与上下文一致性。

#### 3. 实验设计
*   **数据集与场景**：
    *   数据源自 **GAIA** 和 **AssistantBench** 任务。
    *   包含两种 MAS 配置：手工设计的 **MAgenticOne** 和自动生成的 **CaptainAgent**。
    *   共 289 个执行日志，总计包含数千个交互步骤。
*   **对比方法（模型）**：
    *   闭源模型：GPT-4.1, GPT-5.1, o3-mini, Claude-Sonnet-4.5。
    *   开源模型：GPT-oss-120B, Qwen3-8B。
    *   基准线：随机归因（Random Baseline）。

#### 4. 资源与算力
*   **算力设备**：开源模型 **Qwen3-8B** 在本地 **NVIDIA RTX A6000 GPU** 上运行。
*   **API 调用**：其他大型模型通过官方 API（OpenAI, Anthropic, Together.ai）访问。
*   **标注成本**：总计投入约 **346 个专家小时**（每位专家约 115 小时），确保了极高的标注质量。
*   **训练时长**：本文主要关注推理与评估，未涉及模型微调或大规模训练，因此未提供训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：对 289 个案例进行了多轮采样（$N=3$ 到 $N=10$）。
*   **灵敏度分析**：针对采样温度（$\tau$ 从 0 到 1）和采样次数（$N$）进行了消融实验。
*   **多模型协作**：测试了不同模型组合（Multi-LLM）对归因准确性的提升。
*   **评估鲁棒性**：使用了三种不同的模型（GPT-5.1, GPT-4.1, Claude-Sonnet-4.5）分别作为整合器和裁判，验证了评估结果的排名一致性（Kendall’s $\tau \approx 0.9$）。
*   **充分性评价**：实验设计严谨，通过多维度验证（统计显著性测试、跨模型对比）证明了结论的客观性。

#### 6. 主要结论与发现
*   **LLM 能力被低估**：在多视角评估协议下，LLM 的表现远超随机水平，且推理理由与专家高度一致。此前“LLM 不擅长故障归因”的观点主要是由于旧基准的确定性假设过于严苛。
*   **随机性是特性而非缺陷**：通过提高采样温度和次数，LLM 能够自然地捕捉到不同的故障视角。
*   **多模型协作更优**：结合来自不同家族的模型（如 GPT + Claude）能显著提升归因性能，因为不同模型具有互补的推理模式。
*   **实践建议**：在实际 MAS 调试中，应避免依赖单次确定性输出，而应采用多模型、多次采样并结合共识排名的方法。

#### 7. 优点
*   **范式创新**：首次挑战了 MAS 故障归因中的“单一根因”假设，提出了更符合工业界调试现状的多视角范式。
*   **高质量基准**：相比于其他依赖自动生成标签的基准（如 Aegis），MP-Bench 的专家标注提供了极高的可信度和丰富的推理文本。
*   **实用性强**：提出的共识排名机制直接为开发者提供了调试优先级建议。

#### 8. 不足与局限
*   **领域覆盖有限**：目前主要集中在通用助手类任务（GAIA, AssistantBench），尚未覆盖软件工程、科学发现等更专业的垂直领域。
*   **规模限制**：由于采用纯人工专家标注，数据规模（289例）相对较小，可能限制了对极罕见故障模式的覆盖。
*   **框架多样性**：虽然涵盖了手工和自动 MAS，但涉及的具体框架数量仍有增加空间。

（完）
