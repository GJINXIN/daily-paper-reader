---
title: "RASR: Retrieval-Augmented Semantic Reasoning for Fake News Video Detection"
title_zh: RASR：用于虚假新闻视频检测的检索增强语义推理
authors: "Hui Li, Peien Ding, Jun Li, Guoqi Ma, Zhanyu Liu, Ge Xu, Junfeng Yao, Jinsong Su"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06687v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 利用检索增强推理进行多模态虚假新闻视频检测
tldr: 本研究针对多模态虚假新闻视频检测中缺乏跨实例关联和领域专家知识的问题，提出了RASR框架。该框架通过语义解析与检索模块获取历史关联证据，利用领域引导的多模态推理模块生成深度分析报告，并结合多视图特征解耦与融合模块实现鲁棒的真实性判定。在FakeSV和FakeTT数据集上的实验证明，RASR在检测精度和跨领域泛化能力上均优于现有主流方法。
selection_source: fresh_fetch
motivation: 现有方法难以利用历史关联证据且缺乏领域专家知识指导，导致跨实例关联性不足及跨领域泛化受限。
method: 提出RASR框架，包含跨实例语义检索、领域引导的多模态推理以及多视图特征自适应融合三大核心模块。
result: "在FakeSV和FakeTT数据集上，RASR显著超越了现有基准模型，检测准确率最高提升了0.93%。"
conclusion: 通过结合检索增强的语义推理与领域先验知识，RASR有效提升了虚假新闻视频检测的准确性与鲁棒性。
---

## 摘要
多模态虚假新闻视频检测是维护网络信息可信度的关键研究方向。现有研究主要通过构建多模态特征融合表示或利用预训练语言模型分析视频-文本一致性来验证内容真实性。然而，这些方法仍面临以下局限性：(1) 缺乏跨实例的全局语义关联，导致难以有效利用历史关联证据来验证当前视频；(2) 跨领域的语义差异阻碍了通用知识的迁移，且缺乏领域特定专家知识的指导。为此，我们提出了一种新颖的检索增强语义推理（RASR）框架。首先，跨实例语义解析器与检索器（CSPR）将视频解构为高层语义基元，并从动态记忆库中检索相关的关联证据。随后，领域引导的多模态推理（DGMP）模块结合领域先验，驱动专家级多模态大语言模型生成具有领域感知能力的深度分析报告。最后，多视图特征解耦与融合（MVDFF）模块通过自适应门控机制整合多维特征，以实现稳健的真实性判定。在 FakeSV 和 FakeTT 数据集上的广泛实验表明，RASR 显著优于最先进的基准模型，实现了卓越的跨领域泛化能力，并将整体检测准确率提升了高达 0.93%。

## Abstract
Multimodal fake news video detection is a crucial research direction for maintaining the credibility of online information. Existing studies primarily verify content authenticity by constructing multimodal feature fusion representations or utilizing pre-trained language models to analyze video-text consistency. However, these methods still face the following limitations: (1) lacking cross-instance global semantic correlations, making it difficult to effectively utilize historical associative evidence to verify the current video; (2) semantic discrepancies across domains hinder the transfer of general knowledge, lacking the guidance of domain-specific expert knowledge. To this end, we propose a novel Retrieval-Augmented Semantic Reasoning (RASR) framework. First, a Cross-instance Semantic Parser and Retriever (CSPR) deconstructs the video into high-level semantic primitives and retrieves relevant associative evidence from a dynamic memory bank. Subsequently, a Domain-Guided Multimodal Reasoning (DGMP) module incorporates domain priors to drive an expert multimodal large language model in generating domain-aware, in-depth analysis reports. Finally, a Multi-View Feature Decoupling and Fusion (MVDFF) module integrates multi-dimensional features through an adaptive gating mechanism to achieve robust authenticity determination. Extensive experiments on the FakeSV and FakeTT datasets demonstrate that RASR significantly outperforms state-of-the-art baselines, achieves superior cross-domain generalization, and improves the overall detection accuracy by up to 0.93%.

---

## 论文详细总结（自动生成）

这篇论文提出了 **RASR**（Retrieval-Augmented Semantic Reasoning），一个旨在提升多模态虚假新闻视频检测准确性和泛化能力的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着社交媒体的普及，虚假新闻视频（结合了视觉、文本和音频）因其极高的欺骗性而成为严重的社会威胁。现有检测方法存在三大瓶颈：
*   **缺乏跨实例关联：** 现有模型通常孤立地处理单个视频，无法利用历史相似案例（如协同造谣活动）作为证据。
*   **领域知识鸿沟：** 不同领域（如政治、医疗、金融）的造谣模式差异巨大，通用模型难以捕捉特定领域的专家级语义特征。
*   **推理噪声耦合：** 直接引入大语言模型（LLM）的推理结果可能带来“幻觉”噪声，干扰最终的真实性判定。

### 2. 方法论
RASR 框架由三个核心模块组成，旨在通过检索增强和领域引导来解决上述问题：
*   **跨实例语义解析器与检索器 (CSPR)：**
    *   将视频的多模态特征转化为高层“语义基元”（Semantic Primitives），捕捉叙事核心而非表面细节。
    *   利用该基元从动态记忆库中检索出语义最相关的历史样本（包括正例和负例），为当前视频提供上下文参考。
*   **领域引导的多模态推理 (DGMP)：**
    *   构建包含“领域标签”和“检索证据”的结构化提示词（Prompt）。
    *   驱动专门的多模态大模型（视觉：MiniCPM-V；文本：Llama-3.1；音频：Qwen2-Audio）生成领域感知的深度分析报告。
    *   引入置信度校验机制，通过计算报告与证据的语义相似度来减轻 LLM 的幻觉问题。
*   **多视图特征解耦与融合 (MV-DFF)：**
    *   **特征对齐：** 使用 InfoNCE 损失函数和难负样本挖掘（Hard-negative mining），将 LLM 的符号化推理与原始多模态特征对齐。
    *   **双视图处理：** 分为“模态增强视图”（受推理报告引导）和“原始一致性视图”（基于原始特征的跨模态一致性）。
    *   **自适应门控：** 通过学习到的门控机制动态平衡两个视图的权重，过滤推理噪声，输出最终的二分类结果（真/假）。

### 3. 实验设计
*   **数据集：** 
    *   **FakeSV：** 最大的中文短视频虚假信息数据集（来自抖音、快手）。
    *   **FakeTT：** 英文/多语言数据集（基于 Snopes 事实核查网站）。
*   **基准模型 (Benchmarks)：** 对比了 12 种方法，包括专门的短视频检测模型（如 DOCTOR, FakingRecipe）、通用多模态泛化模型（CMRF*）、文本-图像泛化模型（MDFEND*）以及直接使用 GPT-4/GPT-4V 的推理结果。
*   **实验场景：** 涵盖了“通用领域检测”和“跨领域泛化”（采用留一领域法，即在源领域训练，在未见的靶领域测试）。

### 4. 资源与算力
*   **硬件：** 使用了 **4 张 NVIDIA A100 GPU**。
*   **软件/参数：** 基于 PyTorch 2.1.0；AdamW 优化器；学习率 $2 \times 10^{-5}$；训练 50 个 epoch。
*   **模型配置：** 视觉编码器为 TimeSformer，文本为 XLM-RoBERTa，音频为 VGGish。

### 5. 实验数量与充分性
论文进行了非常详尽的实验验证：
*   **对比实验：** 在两个数据集的 9 个领域上进行了全面对比。
*   **消融实验：** 验证了检索、领域引导、LLM 推理、解耦融合和特征对齐五个组件的必要性。
*   **参数敏感性：** 分析了检索数量 $K$、语义维度、对齐损失权重等参数的影响。
*   **鲁棒性测试：** 
    *   更换了不同的 MLLM 后端（如 LLaVA, Gemma, Whisper）以验证架构的通用性。
    *   模拟了不同比例（10%-50%）的检索噪声，验证了模型在错误证据下的稳定性。
*   **评价：** 实验设计客观、公平，覆盖了从性能到鲁棒性的各个维度，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先：** RASR 在所有基准测试中均达到 SOTA，在 FakeSV 上准确率达到 85.64%，比最强基准 DOCTOR 提升了 0.8%-0.93%。
*   **泛化能力强：** 在跨领域测试中，RASR 表现出显著的稳定性，证明领域引导能有效弥补语义偏移。
*   **检索与推理是核心：** 消融实验显示，移除检索模块或 LLM 推理模块会导致性能下降最严重。
*   **抗噪性：** 即使检索到的证据中有 30% 是无关噪声，模型依然能保持极高的检测精度。

### 7. 优点
*   **创新性：** 首次将检索增强生成（RAG）的思想深度集成到多模态视频虚假新闻检测中。
*   **多模态深度融合：** 不仅关注视觉和文本，还引入了专门的音频大模型分析，信息维度更完整。
*   **噪声控制：** 通过双视图解耦和特征对齐，巧妙地解决了 LLM 幻觉可能引入的负面影响。

### 8. 不足与局限
*   **计算开销：** 框架依赖多个大型多模态模型（MiniCPM, Llama, Qwen），推理延迟和算力成本较高，可能难以在实时监测场景中大规模部署。
*   **记忆库依赖：** 检索效果高度依赖于动态记忆库的质量和覆盖范围，对于全新的、从未出现过的造谣模式，检索增益可能会减弱。
*   **语言限制：** 虽然在 FakeTT 上做了测试，但核心实验仍集中在中文和英文，对于其他小语种的适用性有待进一步验证。

（完）
