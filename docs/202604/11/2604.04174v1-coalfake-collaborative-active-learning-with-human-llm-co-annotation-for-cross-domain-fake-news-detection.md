---
title: "CoALFake: Collaborative Active Learning with Human-LLM Co-Annotation for Cross-Domain Fake News Detection"
title_zh: CoALFake：结合人机协同标注的协作式主动学习，用于跨领域虚假新闻检测
authors: "Esma Aïmeur, Gilles Brassard, Dorsaf Sallami"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04174v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 人机协作标注用于跨领域虚假新闻检测
tldr: 针对跨领域虚假新闻检测中标注数据匮乏和领域泛化性差的问题，本文提出CoALFake框架。该框架创新性地结合了人类与大语言模型（LLM）的协同标注，利用LLM实现低成本扩展并由人类确保标签可靠性。通过领域感知的主动学习和嵌入技术，该方法能动态捕捉领域特征并优化样本采样。实验证明，CoALFake在多个数据集上均优于现有基准，实现了高效且低成本的跨领域检测。
selection_source: fresh_fetch
motivation: 现有的跨领域虚假新闻检测面临标注数据获取成本高以及难以平衡领域特定特征与通用模式的挑战。
method: 提出一种结合人类-LLM协同标注与领域感知主动学习的框架，利用领域嵌入和多样化采样策略提升模型泛化能力。
result: 实验结果显示，CoALFake在多个数据集上一致优于现有基准模型，且在极少人工干预下仍能保持高性能。
conclusion: 人类与LLM的协同标注是一种极具成本效益的方法，能有效解决跨领域虚假新闻检测中的数据稀缺和领域偏移问题。
---

## 摘要
虚假新闻在不同领域的泛滥凸显了当前检测系统的关键局限性，这些系统通常表现出狭窄的领域特异性和较差的泛化能力。现有的跨领域方法面临两个关键挑战：(1) 依赖标注数据，而标注数据往往难以获取且获取成本高昂；(2) 僵化的领域分类或对领域特定特征的忽视导致的信息损失。为了解决这些问题，我们提出了 CoALFake，这是一种用于跨领域虚假新闻检测的新颖方法，它将人类与大语言模型（LLM）协同标注与领域感知的主动学习（AL）相结合。我们的方法利用 LLM 进行可扩展、低成本的标注，同时保持人工监督以确保标签的可靠性。通过集成领域嵌入技术，CoALFake 能够动态捕捉领域特定的细微差别和跨领域模式，从而训练出领域无关的模型。此外，一种领域感知采样策略通过优先考虑多样化的领域覆盖来优化样本获取。在多个数据集上的实验结果表明，所提出的方法始终优于各种基准模型。我们的结果强调，人机协同标注是一种极具成本效益的方法，且能提供卓越的性能。在多个数据集上的评估显示，即使在极少的人工监督下，CoALFake 也始终优于一系列现有基准。

## Abstract
The proliferation of fake news across diverse domains highlights critical limitations in current detection systems, which often exhibit narrow domain specificity and poor generalization. Existing cross-domain approaches face two key challenges: (1) reliance on labelled data, which is frequently unavailable and resource intensive to acquire and (2) information loss caused by rigid domain categorization or neglect of domain-specific features. To address these issues, we propose CoALFake, a novel approach for cross-domain fake news detection that integrates Human-Large Language Model (LLM) co-annotation with domain-aware Active Learning (AL). Our method employs LLMs for scalable, low-cost annotation while maintaining human oversight to ensure label reliability. By integrating domain embedding techniques, the CoALFake dynamically captures both domain specific nuances and cross-domain patterns, enabling the training of a domain agnostic model. Furthermore, a domain-aware sampling strategy optimizes sample acquisition by prioritizing diverse domain coverage. Experimental results across multiple datasets demonstrate that the proposed approach consistently outperforms various baselines. Our results emphasize that human-LLM co-annotation is a highly cost-effective approach that delivers excellent performance. Evaluations across several datasets show that CoALFake consistently outperforms a range of existing baselines, even with minimal human oversight.

---

## 论文详细总结（自动生成）

以下是对论文《CoALFake: Collaborative Active Learning with Human-LLM Co-Annotation for Cross-Domain Fake News Detection》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：虚假新闻检测在跨领域场景下表现不佳，主要面临两大挑战：
    1.  **标注成本高昂**：新兴领域的标注数据稀缺，完全依赖人工标注耗时且昂贵。
    2.  **领域泛化性差与信息损失**：现有方法通常采用硬性的领域分类（如“政治”或“娱乐”），忽略了新闻可能跨多个领域的语义复杂性，导致领域迁移时性能大幅下降。
*   **研究背景**：随着社交媒体的发展，虚假新闻跨领域传播极快，亟需一种既能降低标注成本，又能捕捉领域间细微差别并具备强泛化能力的检测框架。

### 2. 方法论
CoALFake 框架由四个核心模块组成：
*   **领域感知采样（Domain-Aware Sampling）**：
    *   利用 K-means 聚类识别数据中的潜在领域，并通过轮廓系数（Silhouette Score）动态调整聚类数。
    *   采用逆比例权重策略优先采样代表性不足的领域，并结合最大熵（Entropy）选择模型最不确定的样本，解决主动学习的“冷启动”和领域不平衡问题。
*   **人机协同标注（Human-LLM Co-Annotation）**：
    *   **LLM 标注**：利用 GPT-3.5 通过 k-NN 检索相似示例进行少样本（Few-shot）提示词标注。
    *   **标签验证**：引入**置信学习（Confident Learning, CL）**估计噪声分布，识别出 LLM 可能标注错误的样本。
    *   **人工重标**：仅将识别出的高风险噪声样本（约 20%）交由人类专家审核，平衡成本与准确性。
*   **领域嵌入（Domain Embedding）**：
    *   不使用硬标签，而是计算样本与各聚类中心的余弦相似度，通过 Softmax 转化为概率向量，作为低维领域嵌入，保留跨领域的语义连续性。
*   **领域无关模型（Domain-Agnostic Model）**：
    *   采用多任务学习架构，将特征映射为“领域特定（Specific）”和“领域共享（Shared）”两个子空间。
    *   使用**交叉注意力机制（Cross-Attention）**促进空间信息交换，并引入正交损失（Orthogonality Loss）和对比损失（Contrastive Loss）确保两个子空间的职能分离。

### 3. 实验设计
*   **数据集**：整合了三个主流数据集：PolitiFact（政治）、GossipCop（娱乐）和 CoAID（医疗保健）。
*   **基准方法（Benchmarks）**：对比了 8 种代表性模型，包括：
    *   传统特征/传播模型：HPNF, AE, SAFE。
    *   跨领域/多领域模型：EDDFN, MDFEND, FuDFEND, DITFEND, SLFEND。
*   **采样策略对比**：对比了随机采样、最大熵、最小置信度和 K-Means 多样性采样。
*   **评估指标**：准确率（Acc）、精确率（Prec）、召回率（Rec）和 F1 分数。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU 数量及型号）或总训练时长。
*   **软件环境**：使用了 OpenAI 的 GPT-3.5-Turbo API（通过 Azure 服务）、Sentence-BERT 提取文本特征、fastText 库进行概率预测。
*   **超参数**：设置嵌入维度 $d=512$，Epochs=300，Batch Size=128，优化器为 Adam。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同领域的数据集上进行了全面的性能对比。
    *   **消融实验**：分别验证了交叉注意力、领域特定特征和领域共享特征的必要性。
    *   **参数敏感性分析**：对 5 个损失函数权重（$\lambda_1$ 至 $\lambda_5$）以及嵌入维度、Batch Size 等进行了网格搜索实验。
    *   **成本效益分析**：测试了从 0% 到 100% 不同比例的人工干预对性能和费用的影响。
*   **充分性评价**：实验设计较为充分且客观。通过统一所有基准模型的训练数据（混合领域训练）确保了对比的公平性，且采样策略的对比验证了其在主动学习场景下的优越性。

### 6. 主要结论
*   **性能领先**：CoALFake 在所有数据集上的 F1 分数均显著优于现有最强基准（如在 Politifact 上 F1 达到 0.93，而基准最高为 0.85）。
*   **协同标注的高效性**：仅需 20% 的人工重标，模型性能即可接近全人工标注的水平，而成本仅为全人工标注的约 21%。
*   **LLM 的角色**：实验发现 LLM 作为“标注者”为下游模型提供训练数据，比直接将 LLM 作为“检测器”进行零样本/少样本推理的效果更好。

### 7. 优点与亮点
*   **软性领域表示**：通过领域嵌入而非硬标签处理跨领域问题，有效减少了语义信息的丢失。
*   **智能纠错机制**：引入置信学习（CL）来筛选 LLM 的潜在错误，使人工审核更具针对性，极大提升了标注效率。
*   **子空间解耦**：通过正交和对比损失设计，成功分离了领域特有知识和通用虚假新闻模式，增强了泛化能力。

### 8. 不足与局限
*   **LLM 依赖性**：实验仅基于 GPT-3.5-Turbo，未测试其他开源模型（如 Llama 3）或更新版本的性能差异。
*   **未知领域验证不足**：虽然进行了跨领域测试，但未采用“留一领域验证（Leave-one-domain-out）”模式来严格测试模型对完全未见过领域的零样本迁移能力。
*   **人工标注假设**：实验中假设原始数据集标签即为“人类重标结果”，但在实际应用中，人类专家的主观偏差和标注一致性可能影响最终效果。

（完）
