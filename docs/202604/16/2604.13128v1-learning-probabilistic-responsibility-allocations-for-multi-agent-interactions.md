---
title: Learning Probabilistic Responsibility Allocations for Multi-Agent Interactions
title_zh: 学习多智能体交互中的概率责任分配
authors: "Isaac Remy, Caleb Chang, Karen Leung"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.13128v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 学习多智能体交互中的责任分配以实现社会合规
tldr: 本研究探讨了多智能体交互中的责任分配问题，即个体为配合他人而偏离自身目标的程度。通过结合条件变分自编码器（CVAE）和多智能体轨迹预测技术，提出了一种学习概率责任分配模型的方法。该方法利用可微分优化层解决缺乏真实标签的问题，将责任分配映射为可观测的控制行为。在INTERACTION驾驶数据集上的实验证明，该模型不仅预测性能优异，还能为理解复杂的交互模式提供具有解释性的视角。
selection_source: fresh_fetch
motivation: 旨在通过理解人类在交互中如何分配责任（即为他人做出让步的程度），来设计更符合社会规范且可靠的自动驾驶系统。
method: 结合CVAE的潜空间与多智能体轨迹预测，并引入可微分优化层，在无真实责任标签的情况下学习受场景和上下文约束的概率责任分配分布。
result: 在INTERACTION真实驾驶数据集上实现了强大的预测性能，并能通过责任分配这一维度提供对多智能体交互模式的深入解释。
conclusion: 该研究证明了概率责任分配模型在捕捉交互不确定性和提升自动驾驶系统社会合规性方面的有效性与解释价值。
---

## 摘要
在交互场景中，人类行为不仅受个人目标的影响，还受到与他人共享的约束（如安全性）的塑造。理解人们如何分配责任（即一个人为了迁就他人而在多大程度上偏离其期望策略），可以为设计符合社会规范且值得信赖的自主系统提供参考。在这项工作中，我们提出了一种学习概率责任分配模型的方法，该模型能够捕捉多智能体交互中固有的多模态不确定性。具体而言，我们的方法利用条件变分自编码器的隐空间，并结合多智能体轨迹预测技术，学习以场景和智能体上下文为条件的责任分配分布。尽管缺乏责任标签的真值，但通过引入一个可微优化层，将责任分配映射到可获得的诱导控制量，使得模型依然是可处理的。我们在 INTERACTION 驾驶数据集上评估了我们的方法，并证明该方法不仅实现了强大的预测性能，还通过责任的视角为多智能体交互模式提供了具有可解释性的见解。

## Abstract
Human behavior in interactive settings is shaped not only by individual objectives but also by shared constraints with others, such as safety. Understanding how people allocate responsibility, i.e., how much one deviates from their desired policy to accommodate others, can inform the design of socially compliant and trustworthy autonomous systems. In this work, we introduce a method for learning a probabilistic responsibility allocation model that captures the multimodal uncertainty inherent in multi-agent interactions. Specifically, our approach leverages the latent space of a conditional variational autoencoder, combined with techniques from multi-agent trajectory forecasting, to learn a distribution over responsibility allocations conditioned on scene and agent context. Although ground-truth responsibility labels are unavailable, the model remains tractable by incorporating a differentiable optimization layer that maps responsibility allocations to induced controls, which are available. We evaluate our method on the INTERACTION driving dataset and demonstrate that it not only achieves strong predictive performance but also provides interpretable insights, through the lens of responsibility, into patterns of multi-agent interaction.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Probabilistic Responsibility Allocations for Multi-Agent Interactions》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**在多智能体交互（如驾驶场景）中，人类如何分配“责任”以确保安全。**
这里的“责任”被定义为：个体为了配合他人、遵守共享约束（如避免碰撞）而偏离其原始期望目标的程度。
*   **研究动机**：现有的自动驾驶系统往往只关注轨迹预测，而忽略了行为背后的社会规范。
*   **背景**：先前的研究多采用确定性模型，但人类交互具有高度的不确定性和多模态性（例如：在路口谁先走可能存在多种合理的责任分配）。因此，本文旨在建立一个概率模型，捕捉这种不确定性，使自主系统更具社会合规性和可解释性。

### 2. 方法论
核心思想是将**条件变分自编码器（CVAE）**与**控制屏障函数（CBF）**相结合，通过可微分优化层学习隐藏的责任分配。

*   **概率责任模型**：利用 CVAE 学习场景上下文（s）到责任分配（$\gamma$）的分布 $p(\gamma|s)$。由于现实中没有“责任”的真值标签，模型通过观察到的智能体控制量（u）进行间接学习。
*   **关键技术细节**：
    *   **Transformer 架构**：借鉴 AgentFormer，使用 Transformer 处理变长序列和多智能体间的复杂时空关系。
    *   **可微分安全滤波器**：将责任分配 $\gamma$ 作为权重引入一个二次规划（QP）问题中。该 QP 最小化智能体控制量与期望控制量的偏差，同时满足 CBF 安全约束。
    *   **端到端训练**：利用可微分优化求解器（如 `qpax`），使梯度能从最终的控制误差反向传播到 CVAE 的编码器和解码器，从而在无标签情况下学习 $\gamma$。
*   **算法流程**：输入历史轨迹 $\to$ 编码器提取特征 $\to$ 采样隐变量 $z$ $\to$ 解码器生成责任分配 $\gamma$ $\to$ 安全滤波器输出预测控制量 $u$ $\to$ 计算预测 $u$ 与真实 $u$ 的误差进行优化。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成数据集**：模拟两个智能体在走廊相遇的“对峙”场景，验证模型捕捉双峰分布（即 A 让 B 或 B 让 A）的能力。
    2.  **INTERACTION 真实驾驶数据集**：使用无人机拍摄的真实路口交互数据，包含复杂的车辆避让行为。
*   **Benchmark 与对比方法**：
    *   **直接控制预测**：不经过责任层，直接用 CVAE 预测控制量（性能上限）。
    *   **期望控制启发式**：假设智能体仅执行其原始目标（性能下限）。
    *   **消融实验**：对比了 5 种不同的激活函数（如 Softmax, Tanh, Clip 等）对责任分配 $\gamma$ 的约束效果。
*   **评估指标**：平均位移误差（ADE）和错过率（Miss Rate）。

### 4. 资源与算力
*   **框架与库**：使用 JAX 框架，结合 Equinox 神经网络库和 `qpax` 可微分 QP 求解器。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **训练/推理时长**：提到在 INTERACTION 数据集上训练了 100 个 epoch（batch size 32）。推理时间约为 **0.463 ± 0.008 秒**（样本量为 32），这表明该方法在实时性方面仍有优化空间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   合成实验验证了概率分布的重建能力。
    *   真实数据实验使用了 5 分钟的轨迹数据，产生约 3 万个数据点。
    *   进行了 5 组消融实验来测试不同数学约束对责任建模的影响。
*   **充分性评价**：实验设计较为**客观且具有针对性**。通过合成数据验证了模型处理多模态的能力，通过真实数据验证了扩展性。但实验仅限于一种特定的路口场景，对于更复杂的交通环境（如包含行人、自行车或不同天气条件）的覆盖尚显不足。

### 6. 主要结论与发现
*   **概率建模的必要性**：概率模型能有效捕捉交互中的多模态行为（如在路口红绿灯外的博弈），这是确定性模型无法做到的。
*   **可解释性**：通过学习到的 $\gamma$ 值，可以直观地看出在特定时刻哪个智能体承担了更多的避让责任（$\gamma$ 越小，责任越大）。
*   **预测性能**：引入责任机制的模型在轨迹预测精度上显著优于简单的启发式模型，且接近无约束的深度学习模型，同时保证了物理上的安全约束。

### 7. 优点
*   **理论与实践结合**：将严谨的控制理论（CBF）与强大的生成模型（Transformer-CVAE）结合，既保证了安全性又具备学习能力。
*   **无需人工标注**：通过可微分优化层解决了责任分配难以直接观测和标注的难题。
*   **强解释性**：提供了一个量化指标（责任权重）来解释智能体为何做出特定的避让动作。

### 8. 不足与局限
*   **数据偏差风险**：INTERACTION 数据集中大部分车辆处于匀速行驶，导致模型在处理静止或剧烈交互时的多样性受限。
*   **计算开销**：由于解码过程中包含一个 QP 优化层，推理速度（约 0.46s）对于高频实时控制系统（通常要求 >10Hz）来说可能偏慢。
*   **智能体筛选**：目前采用基于距离的启发式方法筛选相关智能体，可能忽略了远距离但具有高动态威胁的对象。
*   **泛化性**：实验场景单一，尚未在更多样化的驾驶环境或异构智能体（如人机共驾）中进行广泛验证。

（完）
