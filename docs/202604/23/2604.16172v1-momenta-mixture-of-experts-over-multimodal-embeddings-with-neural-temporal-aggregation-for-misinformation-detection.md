---
title: "MOMENTA: Mixture-of-Experts Over Multimodal Embeddings with Neural Temporal Aggregation for Misinformation Detection"
title_zh: MOMENTA：基于多模态嵌入混合专家模型与神经时间聚合的虚假信息检测
authors: "Yeganeh Abdollahinejad, Ahmad Mousavi, Naeemul Hassan, Kai Shu, Nathalie Japkowicz, Shahriar Khosravi, Amir Karami"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16172v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 社交媒体上的多模态虚假信息检测
tldr: 针对社交媒体多模态虚假信息检测中存在的模态异构性、语义不一致及随时间演化的挑战，本文提出MOMENTA框架。该框架结合专家混合模型（MoE）处理异构模式，利用双向协同注意力和差异感知分支捕捉模态间冲突，并通过神经时间聚合机制建模叙事演化。此外，引入领域对抗学习增强泛化性。实验证明其在多个主流数据集上均取得领先性能。
selection_source: fresh_fetch
motivation: 现有的多模态虚假信息检测方法在处理模态间语义冲突、信息随时间的动态演化以及跨领域泛化方面存在局限性。
method: 提出一种集成MoE、双向协同注意力、差异感知分支及带漂移和动量编码的时间聚合机制的统一框架，并结合领域对抗学习进行多目标优化。
result: 在Fakeddit、MMCoVaR、Weibo和XFacta等多个异构数据集上，该模型在准确率、F1值、AUC和MCC等指标上均表现优异且稳定。
conclusion: MOMENTA通过整合多模态交互、时间动态和领域不变性学习，为复杂社交媒体环境下的虚假信息检测提供了一个稳健且高效的解决方案。
---

## 摘要
社交媒体上多模态内容的广泛传播使得虚假信息检测日益具有挑战性，因为误导性叙事不仅源于单一的文本或视觉内容，还源于模态间的语义不一致及其随时间的演变。现有的多模态虚假信息检测方法通常以静态方式建模跨模态交互，且在异构数据集、领域和叙事设置中往往表现出有限的鲁棒性。为了应对这些挑战，我们提出了 MOMENTA，这是一个统一的多模态虚假信息检测框架，在单一架构中捕捉模态异构性、跨模态不一致性、时间动态和跨领域泛化能力。MOMENTA 采用特定模态的混合专家（MoE）模块来建模多样的虚假信息模式，利用双向协同注意力在共享语义空间中对齐文本和视觉表示，并使用差异感知分支显式捕捉模态间的语义冲突。为了建模叙事演变，我们引入了一种基于注意力的时间聚合机制，在重叠的时间窗口上进行漂移和动量编码，使框架能够捕捉虚假信息传播中的短期波动和长期趋势。此外，领域对抗学习和原型记忆库提高了领域不变性，并稳定了跨数据集的表示学习。该模型采用多目标优化策略进行训练，共同强化分类性能、跨模态对齐、对比学习、时间一致性和领域鲁棒性。在 Fakeddit、MMCoVaR、Weibo 和 XFacta 上的实验表明，MOMENTA 在准确率、F1 分数、AUC 和 MCC 方面均取得了强劲且一致的结果，突显了其在多模态虚假信息检测中的有效性。

## Abstract
The widespread dissemination of multimodal content on social media has made misinformation detection increasingly challenging, as misleading narratives often arise not only from textual or visual content alone, but also from semantic inconsistencies between modalities and their evolution over time. Existing multimodal misinformation detection methods typically model cross-modal interactions statically and often show limited robustness across heterogeneous datasets, domains, and narrative settings. To address these challenges, we propose MOMENTA, a unified framework for multimodal misinformation detection that captures modality heterogeneity, cross-modal inconsistency, temporal dynamics, and cross-domain generalization within a single architecture. MOMENTA employs modality-specific mixture-of-experts modules to model diverse misinformation patterns, bidirectional co-attention to align textual and visual representations in a shared semantic space, and a discrepancy-aware branch to explicitly capture semantic disagreement between modalities. To model narrative evolution, we introduce an attention-based temporal aggregation mechanism with drift and momentum encoding over overlapping time windows, enabling the framework to capture both short-term fluctuations and longer-term trends in misinformation propagation. In addition, domain-adversarial learning and a prototype memory bank improve domain invariance and stabilize representation learning across datasets. The model is trained using a multi-objective optimization strategy that jointly enforces classification performance, cross-modal alignment, contrastive learning, temporal consistency, and domain robustness. Experiments on Fakeddit, MMCoVaR, Weibo, and XFacta show that MOMENTA achieves strong, consistent results across accuracy, F1-score, AUC, and MCC, highlighting its effectiveness for multimodal misinformation detection.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **MOMENTA** 的统一框架，旨在解决社交媒体中多模态虚假信息检测的复杂挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：社交媒体上的虚假信息往往通过文本和视觉（图像）的组合传播。误导性叙事不仅源于单一模态，更常源于模态间的**语义不一致**（如“张冠李戴”的图片）以及叙事随时间的**动态演化**。
*   **核心问题**：现有的检测方法大多是静态的，难以捕捉虚假信息随时间变化的特征；同时，模型在面对不同平台、语言或主题（跨领域）时，鲁棒性和泛化能力较差。
*   **研究意义**：通过整合模态异构性建模、跨模态冲突检测、时间演化分析和领域不变性学习，提升在真实复杂环境下的虚假信息识别精度。

### 2. 核心方法论
MOMENTA 框架由以下几个关键技术组件构成：
*   **多模态编码与专家混合（MoE）**：
    *   使用 **XLM-RoBERTa** 编码文本，**CLIP (ViT-L/14)** 编码图像。
    *   为每个模态设计了**特定模态的 MoE 层**，通过输入依赖的门控机制，让不同的“专家”网络处理不同类型的虚假信息（如讽刺、完全伪造、脱离语境等）。
*   **双向协同注意力（Co-Attention）**：
    *   通过共享查询（Query）、键（Key）和值（Value）投影，实现文本与图像在共享语义空间中的深度对齐。
*   **差异感知分支（Discrepancy Branch）**：
    *   显式计算文本和图像嵌入之间的绝对差值和元素积，捕捉模态间的语义冲突信号。
*   **神经时间聚合（Temporal Aggregation）**：
    *   采用**重叠时间窗口**对帖子进行分组。
    *   引入**漂移（Drift）**（连续窗口间的表示变化）和**动量（Momentum）**（变化幅度的平滑值）编码，捕捉叙事演化的方向和强度。
*   **领域泛化与多目标优化**：
    *   **领域对抗学习**：通过梯度反转层（GRL）训练领域分类器，迫使编码器学习领域无关的特征。
    *   **原型记忆库（Prototype Memory Bank）**：使用 EMA（指数移动平均）维护全局类原型，稳定跨数据集的表示。
    *   **多目标损失函数**：结合了 Focal Loss（处理类别不平衡）、对齐损失、对比学习损失、时间一致性损失和 R-Drop 正则化。

### 3. 实验设计
*   **数据集**：在四个主流基准数据集上进行了评估：
    1.  **Fakeddit**：大规模多粒度虚假新闻数据集。
    2.  **MMCoVaR**：专注于新冠疫苗相关的多模态数据集。
    3.  **Weibo**：中文社交媒体虚假信息数据集。
    4.  **XFacta**：当代真实世界的多模态虚假信息数据集。
*   **对比方法（Benchmarks）**：对比了包括 E-CaTCH、DCCF、KEN、MIMoE-FND、DGExplain 和基于 GPT-4o 的检测方法等多种 SOTA（州际领先）模型。
*   **评估指标**：准确率（Acc）、精确率（Prec）、召回率（Rec）、F1 分数、Macro-F1、AUC 和 MCC（马修斯相关系数）。

### 4. 资源与算力
*   **论文说明**：文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：提到了使用了预训练的 XLM-RoBERTa 和 CLIP 模型，并提供了 GitHub 代码库地址，暗示了实验的可重复性。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个异构数据集上进行了完整的性能对比，涵盖了不同语言（中/英）和不同主题。
*   **分析深度**：除了主实验外，还进行了诊断性分析，包括：
    *   指标矩阵分析（雷达图）。
    *   决策阈值校准分析。
    *   残差误差分布分析。
    *   各数据集对性能提升的贡献度分析。
*   **评价**：实验设计较为充分且客观，通过跨数据集的对比验证了模型的泛化能力，并使用了 MCC 等对不平衡数据更公平的指标。

### 6. 主要结论与发现
*   **性能领先**：MOMENTA 在所有四个数据集上均刷新了 SOTA 记录，尤其在挑战性较大的跨领域设置（如 MMCoVaR 和 XFacta）中提升更明显（准确率提升 1.1% 至 1.7%）。
*   **稳定性**：模型在不同指标（AUC、F1、MCC）下表现均衡，说明其不仅分类准，且排序质量和类别平衡性也很好。
*   **时间特征的重要性**：引入漂移和动量编码能有效捕捉虚假信息的传播动态，有助于识别演化中的误导性叙事。

### 7. 优点与亮点
*   **综合性强**：在一个框架内同时解决了异构性、不一致性、时间性和领域泛化四个核心问题。
*   **创新的时间建模**：不同于简单的序列建模，提出的漂移和动量编码为理解虚假信息的“演化轨迹”提供了新视角。
*   **鲁棒性设计**：通过领域对抗学习和原型记忆库，增强了模型在真实世界（多源数据）中的实用性。

### 8. 不足与局限
*   **架构复杂度**：集成了 MoE、时间聚合、多个辅助头和复杂的损失函数，导致模型参数量大、计算开销较高，可能限制其在超大规模实时系统中的部署。
*   **超参数敏感性**：时间窗口大小、衰减率、动量系数等超参数较多，文中未做详细的敏感性分析。
*   **模态局限**：目前仅关注文本和图像，尚未整合视频、音频或社交网络拓扑（如转发关系图）等其他重要信号。
*   **黑盒性质**：虽然有差异分支提供了一定解释性，但 MoE 内部的专家决策过程仍缺乏直观的可解释性。

（完）
