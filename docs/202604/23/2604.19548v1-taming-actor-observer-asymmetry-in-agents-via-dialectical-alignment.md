---
title: Taming Actor-Observer Asymmetry in Agents via Dialectical Alignment
title_zh: 通过辩证对齐缓解智能体中的行动者-观察者不对称性
authors: "Bobo Li, Rui Wu, Zibo Ji, Meishan Zhang, Hao Fei, Min Zhang, Mong-Li Lee, Wynne Hsu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19548v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 多智能体框架中的认知偏差
tldr: 本文研究了LLM智能体在多角色协作中存在的“行动者-观察者不对称（AOA）”认知偏差，即智能体作为行动者时倾向于外归因，而作为观察者时倾向于内归因。为解决这一问题，研究者构建了量化偏差的基准测试，并提出了ReTAS框架，通过辩证对齐技术（正题-反题-合题）结合GRPO算法，引导模型合成冲突观点以达成客观共识，显著提升了系统在模糊故障场景下的可靠性。
selection_source: fresh_fetch
motivation: 发现LLM智能体在自我反思与相互审计中存在类似人类的归因偏差，导致对失败原因的判断随视角变化而产生矛盾。
method: 提出ReTAS框架，利用辩证思维链结合群体相对策略优化（GRPO），训练模型通过合成多视角观点来实现视角不变的推理。
result: "实验表明ReTAS有效缓解了超过20%的归因不一致现象，并显著提高了模糊场景下的故障解决成功率。"
conclusion: 通过辩证对齐可以有效抑制智能体的认知偏差，增强多智能体协作系统的推理客观性与鲁棒性。
---

## 摘要
大语言模型智能体已迅速从静态文本生成器演变为能够执行复杂自主工作流的动态系统。为了增强可靠性，越来越多地采用分配专门角色的多智能体框架，以实现自我反思和相互审计。虽然这种角色扮演有效地利用了领域专家知识，但我们发现它同时诱发了一种类似于人类的认知偏差，即行动者-观察者不对称性（AOA）。具体而言，作为行动者的智能体（在自我反思期间）倾向于将失败归因于外部因素，而观察者（在相互审计期间）则将同样的错误归因于内部缺陷。我们使用新开发的模糊失败基准测试（Ambiguous Failure Benchmark）对此进行了量化，结果显示，对于大多数模型，仅通过切换视角就会在超过 20% 的案例中触发 AOA 效应。为了抑制这种偏差，我们引入了 ReTAS（通过正题-反题-合题进行推理），这是一个通过辩证对齐训练的模型，旨在强制执行视角不变的推理。通过将辩证思维链与群体相对策略优化（Group Relative Policy Optimization）相结合，ReTAS 引导智能体将冲突的观点综合成客观的共识。实验表明，ReTAS 有效地缓解了归因不一致性，并显著提高了模糊场景下的故障解决率。

## Abstract
Large Language Model agents have rapidly evolved from static text generators into dynamic systems capable of executing complex autonomous workflows. To enhance reliability, multi-agent frameworks assigning specialized roles are increasingly adopted to enable self-reflection and mutual auditing. While such role-playing effectively leverages domain expert knowledge, we find it simultaneously induces a human-like cognitive bias known as Actor-Observer Asymmetry (AOA). Specifically, an agent acting as an actor (during self-reflection) tends to attribute failures to external factors, whereas an observer (during mutual auditing) attributes the same errors to internal faults. We quantify this using our new Ambiguous Failure Benchmark, which reveals that simply swapping perspectives triggers the AOA effect in over 20% of cases for most models. To tame this bias, we introduce ReTAS (Reasoning via Thesis-Antithesis-Synthesis), a model trained through dialectical alignment to enforce perspective-invariant reasoning. By integrating dialectical chain-of-thought with Group Relative Policy Optimization, ReTAS guides agents to synthesize conflicting viewpoints into an objective consensus. Experiments demonstrate that ReTAS effectively mitigates attribution inconsistency and significantly improves fault resolution rates in ambiguous scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《Taming Actor-Observer Asymmetry in Agents via Dialectical Alignment》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：论文揭示了多智能体系统（MAS）在角色扮演（Role-playing）过程中存在一种类似人类的认知偏差——**行动者-观察者不对称性（Actor-Observer Asymmetry, AOA）**。
*   **研究动机**：在多智能体协作中，智能体常被分配为“执行者（Actor）”或“审计者（Observer）”。研究发现，当任务失败时，执行者倾向于将错误归因于外部环境（如指令模糊、服务器问题），而审计者则倾向于归因为执行者的内部缺陷（如逻辑错误、能力不足）。这种归因不一致会导致智能体间失去共识，降低协作的可靠性。

### 2. 方法论：ReTAS 框架
论文提出了 **ReTAS (Reasoning via Thesis-Antithesis-Synthesis)**，旨在通过辩证对齐实现视角不变的推理。
*   **核心思想**：借鉴费希特（Fichte）的辩证法，将反思过程分解为三个阶段：
    1.  **正题 (Thesis)**：生成符合当前角色设定的初始解释（带有角色偏见）。
    2.  **反题 (Antithesis)**：模拟对立视角，寻找证据中的盲点，挑战初始解释。
    3.  **合题 (Synthesis)**：整合冲突观点，基于客观证据得出视角不变的最终结论。
*   **关键技术细节**：
    *   **数据生成**：构建了包含外部故障（检索缺失）和内部故障（推理错误）的结构化数据集。
    *   **辩证对齐训练**：
        *   **阶段一：监督微调 (SFT)**：让模型学习 TAS 的推理格式。
        *   **阶段二：强化学习对齐 (GRPO)**：使用群体相对策略优化算法，通过复合奖励函数（格式奖励、归因准确奖励、答案正确奖励）引导模型内化辩证思维，而非仅仅模仿模板。

### 3. 实验设计
*   **基准测试 (Benchmark)**：
    *   **AFB (Ambiguous Failure Benchmark)**：涵盖 10 个领域（编程、RAG、安全等），包含 200 条模糊失败轨迹，用于量化 AOA 偏差。
    *   **FinQA-TAS & Spider-TAS**：基于金融推理和文本转 SQL 任务构建的故障归因数据集，具有可验证的故障根因。
*   **对比方法**：
    *   **标准提示词**：GPT-5.1, DeepSeek-V3.2, QwQ-32B, Qwen3-30B, GLM-4.6 等。
    *   **单视角反思 (Single View)**：仅进行自我诊断。
    *   **双视角反思 (Dual View)**：简单的角色对立辩论，不含辩证合成。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H200 GPU**，运行环境为 Ubuntu。
*   **训练时长**：
    *   SFT 阶段：每个 Epoch 约 15 分钟。
    *   GRPO 阶段：约 **9 小时**（最大序列长度 2048，优化步数 750 步）。
*   **模型规模**：以 Qwen3-4B 为基础模型进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 FinQA-TAS（约 8000 样本）和 Spider-TAS（约 8000 样本）上进行了完整训练与测试。
    *   进行了**消融实验**，验证了不同奖励组件（R1, R2, R3）和 GRPO 算法的必要性。
    *   进行了**跨领域泛化实验**，将训练好的模型直接应用于 AFB 基准测试。
    *   进行了**动态谈判实验 (Sales Arena)**，模拟多轮博弈场景。
*   **评价**：实验设计较为充分且客观。通过对比不同参数规模的模型（4B vs 32B/671B），证明了该方法在小参数模型上也能实现超越大模型的归因一致性，具有很强的说服力。

### 6. 主要结论与发现
*   **AOA 偏差普遍存在**：即使是顶级模型（如 GPT-5.1），在切换视角时也会出现显著的归因不一致（Flip 现象）。
*   **规模并非万能**：单纯增加模型参数不能消除认知偏差，甚至某些大模型在双视角下表现更差。
*   **ReTAS 的优越性**：仅 4B 规模的 ReTAS 模型在归因准确率和视角一致性上显著优于 32B 的 QwQ 模型，并有效提升了下游任务的 F1 分数。
*   **辩证思维的价值**：通过“正-反-合”的结构化推理，智能体能从防御性辩解转向客观证据分析。

### 7. 优点与亮点
*   **跨学科结合**：成功将社会心理学概念（AOA）与哲学方法（辩证法）引入 LLM 智能体对齐领域。
*   **参数高效**：证明了通过精细的辩证对齐训练，小模型可以在特定认知任务上超越超大规模模型。
*   **结构化推理**：TAS 框架为解决智能体幻觉和防御性响应提供了一种可解释、可操作的路径。

### 8. 不足与局限
*   **任务范围限制**：实验主要集中在具有明确证据链的推理任务（如 RAG、SQL），对于高度主观或开放式（如创意写作）的故障归因，其效果尚待验证。
*   **合成数据依赖**：AFB 基准测试部分依赖合成数据，可能与真实世界中极其复杂的长程规划故障存在差异。
*   **计算开销**：虽然推理时高效，但 GRPO 强化学习阶段仍需要一定的算力支持和精细的奖励函数调优。

（完）
