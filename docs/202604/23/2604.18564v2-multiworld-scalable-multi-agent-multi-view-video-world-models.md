---
title: "MultiWorld: Scalable Multi-Agent Multi-View Video World Models"
title_zh: MultiWorld：可扩展的多智能体多视角视频世界模型
authors: "Haoyu Wu, Jiwen Yu, Yingtian Zou, Xihui Liu"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18564v2"
tags: ["query:mas-ss"]
score: 8.0
evidence: 用于模拟复杂交互的多智能体视频世界模型
tldr: 针对现有视频世界模型在多智能体交互和多视角一致性方面的局限，本文提出 MultiWorld 统一框架。该框架通过多智能体条件模块实现精确控制，并利用全局状态编码器确保多视角观测的一致性。MultiWorld 支持智能体和视角数量的灵活扩展，并能并行合成视频，在多玩家游戏和多机器人任务中表现出优异的视频保真度、动作遵循能力和视角一致性。
selection_source: fresh_fetch
motivation: 现有的视频世界模型大多局限于单智能体场景，难以捕捉现实世界中复杂的多智能体交互及多视角一致性。
method: 提出 MultiWorld 框架，引入多智能体条件模块实现精确控制，并利用全局状态编码器确保跨视角的一致性与高效并行合成。
result: 在多玩家游戏和多机器人操作任务中的实验表明，该模型在视频质量、动作遵循度和多视角一致性上均优于现有基准。
conclusion: MultiWorld 为可扩展的多智能体多视角世界建模提供了有效方案，显著提升了复杂环境下的模拟能力。
---

## 摘要
视频世界模型在模拟响应用户或智能体动作的环境动力学方面取得了显著成功。它们被建模为以动作为条件的视频生成模型，将历史帧和当前动作作为输入来预测未来帧。然而，大多数现有方法局限于单智能体场景，无法捕捉现实世界多智能体系统中固有的复杂交互。我们提出了 MultiWorld，这是一个用于多智能体多视角世界建模的统一框架，能够在保持多视角一致性的同时，实现对多个智能体的精确控制。我们引入了多智能体条件模块（Multi-Agent Condition Module）以实现精确的多智能体可控性，并引入了全局状态编码器（Global State Encoder）以确保不同视角之间观测的一致性。MultiWorld 支持智能体数量和视角数量的灵活扩展，并并行合成不同视角以实现高效率。在多玩家游戏环境和多机器人操纵任务上的实验表明，MultiWorld 在视频保真度、动作遵循能力和多视角一致性方面优于基准模型。项目主页：https://multi-world.github.io/

## Abstract
Video world models have achieved remarkable success in simulating environmental dynamics in response to actions by users or agents. They are modeled as action-conditioned video generation models that take historical frames and current actions as input to predict future frames. Yet, most existing approaches are limited to single-agent scenarios and fail to capture the complex interactions inherent in real-world multi-agent systems. We present \textbf{MultiWorld}, a unified framework for multi-agent multi-view world modeling that enables accurate control of multiple agents while maintaining multi-view consistency. We introduce the Multi-Agent Condition Module to achieve precise multi-agent controllability, and the Global State Encoder to ensure coherent observations across different views. MultiWorld supports flexible scaling of agent and view counts, and synthesizes different views in parallel for high efficiency. Experiments on multi-player game environments and multi-robot manipulation tasks demonstrate that MultiWorld outperforms baselines in video fidelity, action-following ability, and multi-view consistency. Project page: https://multi-world.github.io/

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MultiWorld**，一个旨在解决多智能体、多视角场景下视频世界建模挑战的统一框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：现有的视频世界模型（如 Sora、Genie 等）大多假设环境中仅存在单个智能体，忽略了现实世界中常见的多智能体协作（如协作机器人、多人游戏）及其复杂的交互。
*   **核心挑战**：
    1.  **多智能体可控性**：如何将特定动作准确关联到对应智能体，并同步其执行。
    2.  **多视角一致性**：不同智能体从不同视角观察同一环境，如何确保生成的视频在几何和视觉上保持一致。
    3.  **框架可扩展性**：现实环境中的智能体和相机数量是多变的，模型需支持灵活扩展而非固定配置。

### 2. 方法论
MultiWorld 基于 **Flow Matching (FM)** 和 **Transformer (DiT)** 架构，提出了两个核心模块：
*   **多智能体条件模块 (MACM)**：
    *   **智能体身份嵌入 (AIE)**：采用旋转位置嵌入（RoPE）为每个智能体的动作令牌注入独特的身份信息，解决“镜像动作”导致的身份歧义。
    *   **自适应动作加权 (AAW)**：利用 MLP 动态预测各智能体动作的权重，使模型优先关注正在产生显著环境变化的活跃智能体。
*   **全局状态编码器 (GSE)**：
    *   **核心思想**：利用预训练的 **VGGT**（视觉几何基础模型）提取 3D 感知的全局环境状态。
    *   **实现细节**：将多个视角的初始观测压缩为统一的隐空间表示，作为生成各视角的共同条件，从而在不显式构建 3D 点云的情况下确保跨视角的一致性。
*   **可扩展与并行推理**：
    *   支持任意数量的智能体和视角。
    *   **并行生成**：不同视角的视频合成在共享全局状态下并行进行，推理延迟不随视角增加而显著增长（双视角下比串行快 1.5 倍）。
    *   **自回归生成**：支持长时程模拟，通过迭代更新全局环境状态，生成长度可达训练窗口的 4 倍。

### 3. 实验设计
*   **数据集/场景**：
    1.  **多人游戏数据集**：基于《双人成行》(It Takes Two) 录制的 100 小时真实玩家数据（2100 万帧），包含复杂的协作交互。
    2.  **多机器人操纵数据集**：基于 RoboFactory 模拟器，涵盖 2 到 4 个机器人的抓取、堆叠等任务，包含成功和失败的轨迹。
*   **对比基准 (Baselines)**：
    *   **Standard**：独立处理每个视角的标准图像-动作-视频模型。
    *   **Concat-View**：将多视角拼接为单个视频进行训练。
    *   **COMBO**：一种组合式多智能体模型，通过组合单智能体模型来实现多智能体控制。
*   **评估指标**：FVD（视频质量）、PSNR/SSIM/LPIPS（图像保真度）、RPE（重投影误差，衡量视角一致性）、IDM（逆动力学模型，衡量动作遵循能力）。

### 4. 资源与算力
*   **模型规模**：基于 Wan2.2-5B 视频生成模型。
*   **硬件资源**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练细节**：学习率为 5e-5，全局 Batch Size 为 64，训练迭代次数为 40,000 次。
*   **训练时长**：整个训练过程大约耗时 **4 天**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个截然不同的领域（游戏与机器人）进行了验证，涵盖了从 2 到 4 个智能体的不同配置。
*   **消融实验**：非常详尽，分别验证了 MACM、GSE、AIE 的频率选择、AAW 的必要性以及不同 GSE 后端（如 DINOv2, VAE）的效果。
*   **客观性**：通过引入 RPE 指标量化了几何一致性，并使用预训练的 IDM 模型客观评估了动作遵循准确率，实验设计较为公平、全面。

### 6. 主要结论与发现
*   **性能领先**：MultiWorld 在所有指标上均优于基准模型，尤其在 FVD 和 RPE（视角一致性）上提升显著。
*   **动作遵循**：引入 AIE 和 AAW 后，模型能更精准地响应多个智能体的并发指令，减少了智能体“消失”或动作错误的情况。
*   **长时程能力**：通过自回归策略，模型能够稳定生成超出训练长度的视频，并保持物理逻辑的一致性（如阴影、足迹的持久性）。

### 7. 优点与亮点
*   **架构优雅**：通过解耦视角生成与全局状态编码，实现了极佳的可扩展性和推理效率。
*   **3D 感知**：巧妙利用 VGGT 提取 3D 特征，解决了视频生成中长期存在的跨视角几何冲突问题。
*   **实用性强**：能够模拟机器人协作中的失败轨迹，这对于具身智能的强化学习训练具有重要价值。

### 8. 不足与局限
*   **分辨率限制**：在处理远距离或微小物体时，由于空间分辨率限制，智能体的形状可能出现模糊或歧义。
*   **算力开销**：尽管支持并行，但长时程、多视角的模拟对显存和计算资源的需求依然巨大，限制了其在超长交互场景下的应用。
*   **数据依赖**：模型性能高度依赖于高质量的多视角同步数据，这类数据在现实世界中获取成本较高。

（完）
