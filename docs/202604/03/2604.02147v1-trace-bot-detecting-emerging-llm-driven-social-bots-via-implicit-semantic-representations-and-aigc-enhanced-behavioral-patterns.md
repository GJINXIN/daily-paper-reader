---
title: "TRACE-Bot: Detecting Emerging LLM-Driven Social Bots via Implicit Semantic Representations and AIGC-Enhanced Behavioral Patterns"
title_zh: TRACE-Bot：通过隐式语义表示和 AIGC 增强的行为模式检测新兴的大语言模型驱动社交机器人
authors: "Zhongbo Wang, Zhiyu Lin, Zhu Wang, Haizhou Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02147v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 检测在线话语中的大模型驱动社交机器人及行为模式
tldr: 针对大语言模型（LLM）驱动的社交机器人生成内容拟人化、难以识别的问题，本文提出 TRACE-Bot 框架。该框架采用双通道架构，结合预训练语言模型提取的隐式语义特征与融合 AIGC 检测信号的多维行为模式，从个人信息、互动行为和推文等多源异构数据中捕捉机器人特征。实验表明，TRACE-Bot 在两个公开数据集上均达到 SOTA 性能，能有效应对复杂的机器人策略。
selection_source: fresh_fetch
motivation: 现有的社交机器人检测方法难以识别 LLM 生成的高拟人化内容，且缺乏对 AIGC 生成模式与行为动态之间关联的有效建模。
method: 提出 TRACE-Bot 双通道框架，通过预训练语言模型提取语义表示，并结合 AIGC 检测器增强的行为特征进行多模态融合分类。
result: "在两个 LLM 驱动的社交机器人数据集上分别实现了 98.46% 和 97.50% 的准确率，显著优于现有基准方法。"
conclusion: 结合隐式语义表示与 AIGC 增强的行为模式是检测新兴 LLM 驱动社交机器人的有效途径，具有极强的鲁棒性。
---

## 摘要
大语言模型（LLM）驱动的社交机器人通过生成能够规避传统检测的类人内容，对在线舆论构成了日益严重的威胁。现有方法由于过度依赖单模态信号、对人工智能生成内容（AIGC）特定生成模式的敏感性不足，以及未能充分建模语言模式与行为动态之间的相互作用，导致检测准确率受限。为解决这些局限性，我们提出了 TRACE-Bot，这是一个统一的双通道框架，共同建模隐式语义表示和 AIGC 增强的行为模式。TRACE-Bot 从异构数据源（包括个人信息数据、交互行为数据和推文数据）构建细粒度表示。其双通道架构通过预训练语言模型捕捉语言表示，并通过由最先进（SOTA）AIGC 检测器信号增强的多维活动特征来捕捉行为异常。融合后的表示随后通过轻量级预测头进行分类。在两个公开的 LLM 驱动社交机器人数据集上的实验证明了其 SOTA 性能，准确率分别达到 98.46% 和 97.50%。结果进一步表明，该方法对先进的机器人策略具有很强的鲁棒性，突显了联合利用隐式语义表示和 AIGC 增强的行为模式在检测新兴 LLM 驱动社交机器人方面的有效性。

## Abstract
Large Language Model-driven (LLM-driven) social bots pose a growing threat to online discourse by generating human-like content that evades conventional detection. Existing methods suffer from limited detection accuracy due to overreliance on single-modality signals, insufficient sensitivity to the specific generative patterns of Artificial Intelligence-Generated Content (AIGC), and a failure to adequately model the interplay between linguistic patterns and behavioral dynamics. To address these limitations, we propose TRACE-Bot, a unified dual-channel framework that jointly models implicit semantic representations and AIGC-enhanced behavioral patterns. TRACE-Bot constructs fine-grained representations from heterogeneous sources, including personal information data, interaction behavior data and tweet data. A dual-channel architecture captures linguistic representations via a pretrained language model and behavioral irregularities via multidimensional activity features augmented with signals from state-of-the-art (SOTA) AIGC detectors. The fused representations are then classified through a lightweight prediction head. Experiments on two public LLM-driven social bot datasets demonstrate SOTA performance, achieving accuracies of 98.46% and 97.50%, respectively. The results further indicate strong robustness against advanced bot strategies, highlighting the effectiveness of jointly leveraging implicit semantic representations and AIGC-enhanced behavioral patterns for emerging LLM-driven social bot detection.

---

## 论文详细总结（自动生成）

### TRACE-Bot：检测新兴 LLM 驱动社交机器人的深度总结

#### 1. 核心问题与研究动机
随着大语言模型（LLM）的普及，社交机器人已进化为“LLM 驱动”形态。这类机器人能够生成高度拟人化的内容，并动态调整交互策略，导致传统的基于静态特征、行为统计或简单语言多样性假设的检测方法失效。
*   **现有挑战**：
    *   **多模态融合不足**：现有方法多依赖单一模态（仅文本或仅行为），难以捕捉非人语义与生成式行为模式之间的复杂关联。
    *   **对 AIGC 特征不敏感**：缺乏对 LLM 生成内容的内在规律（如分布偏差、潜在语义痕迹）的建模。
    *   **建模解耦**：文本语义与行为动态往往被独立处理，忽略了两者之间的内在相关性。

#### 2. 方法论：TRACE-Bot 框架
论文提出了 **TRACE-Bot**，一个统一的双通道架构，旨在联合建模隐式语义表示和 AIGC 增强的行为模式。
*   **核心思想**：通过两个并行通道分别处理文本语义和行为特征，并引入专业的 AIGC 检测器信号作为辅助特征。
*   **关键技术细节**：
    *   **数据准备**：整合个人信息（元数据）、互动行为（转评赞序列）和推文数据。
    *   **文本通道（Textual Channel）**：使用 **GPT-2** 作为编码器，提取用户个人资料中的隐式语义表示。通过均值池化（Mean Pooling）处理隐藏层状态，捕捉语言风格和连贯性。
    *   **行为通道（Behavioral Channel）**：
        *   **行为序列构建**：将用户行为编码为符号序列（Original, Retweet, Reply），利用 **zlib 压缩算法** 计算压缩率，以此量化行为的规律性（机器人通常更具规律性，压缩率更高）。
        *   **AIGC 增强**：集成 SOTA AIGC 检测器（**Fast DetectGPT** 和 **GLTR**）对推文进行评分，将评分的统计值（均值、标准差等）作为特征输入。
    *   **特征融合与检测**：将双通道特征拼接，通过一个轻量级的多层感知机（MLP）预测头进行二分类。

#### 3. 实验设计
*   **数据集**：使用了两个专门针对 LLM 驱动机器人的公开数据集：**Fox8-23**（包含 9000+ 账号）和 **BotSim-24**（大规模基准测试，包含多轮交互记录）。
*   **基准方法（Benchmarks）**：对比了 11 种代表性模型，涵盖：
    *   **传统机器学习**：Digital DNA, BotRuler。
    *   **深度学习**：基于 LSTM 的模型、混合框架。
    *   **图神经网络（GNN）**：BotRGCN, BotMoE, CACL, BotDGT 等。
    *   **大模型相关方法**：LMBot。
*   **评价指标**：准确率 (ACC)、精确率 (PRE)、召回率 (REC) 和 F1 分数。

#### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**：Intel(R) Xeon(R) Platinum 8255C。
    *   **GPU**：**NVIDIA T4 (32 GB VRAM)**。
    *   **内存**：32 GB。
*   **软件环境**：Python 3.8.10, PyTorch 2.4.1, CUDA 12.2.0。
*   **训练细节**：最大 10 个 epoch，batch size 为 256，使用早停机制（Early Stopping）。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证，体现了高度的客观性与公平性：
*   **对比实验**：在两个数据集上全面超越 11 种基准模型。
*   **消融实验**：分别验证了文本通道、行为通道以及不同数据模态（个人信息、行为、推文）的贡献。
*   **表示学习分析**：通过 **t-SNE 可视化**、ARI 和 SIL 指标证明了特征空间的聚类效果。
*   **标签效率实验**：测试了在仅有 10%-100% 标注数据情况下的性能，证明了模型在小样本下的鲁棒性。
*   **鲁棒性测试**：改变训练集比例（50%-80%）以及类别不平衡比例（1:3 到 3:1）。
*   **超参数与编码研究**：对比了不同预训练模型（BERT, RoBERTa, DeBERTa 等）和 Prompt 策略的影响。

#### 6. 主要结论与发现
*   **性能领先**：TRACE-Bot 在 Fox8-23 和 BotSim-24 上分别达到 **98.46%** 和 **97.50%** 的准确率，刷新了 SOTA。
*   **双通道的必要性**：文本通道捕捉语言痕迹，行为通道抑制误报，两者结合效果远超单通道。
*   **AIGC 检测器的价值**：首次证明了将 AIGC 检测评分作为特征能显著提升社交机器人检测的精度。
*   **数据效率**：模型在仅使用 30%-40% 标注数据时即可接近峰值性能。

#### 7. 优点与亮点
*   **创新视角**：这是首个探索“文本-行为双通道融合”且“引入 AIGC 检测信号”来识别 LLM 机器人的工作。
*   **特征工程巧妙**：利用 zlib 压缩率来量化行为规律性，既简单又有效。
*   **鲁棒性强**：实验证明模型在面对类别不平衡和数据稀缺时依然保持高召回率和高精确率。
*   **可复现性高**：使用了开源的 GPT-2 和标准 AIGC 检测器，不依赖闭源大模型。

#### 8. 不足与局限
*   **语言与平台限制**：目前仅在 Twitter（现 X）的**英文**内容上进行了验证，未测试中文或其他小语种环境。
*   **机器人类型覆盖**：主要针对 LLM 驱动的机器人，对于传统的基于简单脚本或规则的机器人，其泛化能力有待进一步验证。
*   **模态局限**：目前仅处理文本和行为数据，尚未涉及图像、视频或音频等多模态 AIGC 内容的检测。

（完）
