---
title: Semantic Modeling for World-Centered Architectures
title_zh: 以世界为中心架构的语义建模
authors: "Andrei Mantsivoda, Darya Gavrilina"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01359v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 面向机构系统和协作的世界中心多智能体系统
tldr: 针对传统以智能体为中心架构在复杂领域中存在的语义不一致和难以解释等问题，本文提出了以世界为中心的多智能体系统（WMAS）。该架构通过共享的显式世界模型替代孤立的局部表示，确保了全局一致性与可验证行为。研究引入了语义模型作为数学形式化手段，并开发了 Ontobox 平台作为 WMAS 的具体实现，为构建稳定、可解释的复杂系统提供了新方案。
selection_source: fresh_fetch
motivation: 传统以智能体为中心的架构难以满足企业等结构化领域对语义一致性、可解释性和长期稳定性的严苛要求。
method: 提出 WMAS 架构，通过共享的显式语义模型对世界进行数学形式化建模，并依据本体显式性和规范性等维度对世界进行分类。
result: 实现了智能体在统一世界模型上的学习与协作，确保了系统行为的全局一致性及可验证性。
conclusion: WMAS 及其 Ontobox 平台为构建高度结构化且需严格监管的多智能体系统提供了一种更具优势的替代架构。
---

## 摘要
我们引入了以世界为中心的多智能体系统（WMAS），作为传统以智能体为中心架构的替代方案，并认为诸如企业和制度系统等结构化领域需要共享且显式的世界表示，以确保语义一致性、可解释性和长期稳定性。我们根据本体显式性、规范性等维度对世界进行了分类。在 WMAS 中，学习与协作是在共享的世界模型上运行，而非孤立的智能体局部表示，从而实现了全局一致性和可验证的系统行为。我们提出将语义模型作为表示此类世界的数学形式化方法。最后，我们展示了作为 WMAS 实现的 Ontobox 平台。

## Abstract
We introduce world-centered multi-agent systems (WMAS) as an alternative to traditional agent-centered architectures, arguing that structured domains such as enterprises and institutional systems require a shared, explicit world representation to ensure semantic consistency, explainability, and long-term stability. We classify worlds along dimensions including ontological explicitness, normativity, etc. In WMAS, learning and coordination operate over a shared world model rather than isolated agent-local representations, enabling global consistency and verifiable system behavior. We propose semantic models as a mathematical formalism for representing such worlds. Finally, we present the Ontobox platform as a realization of WMAS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Modeling for World-Centered Architectures》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的多智能体系统（MAS）通常是“以智能体为中心”的，即每个智能体维护私有的环境表示。在企业、金融、医疗等高度结构化的领域，这种模式会导致智能体间的语义不一致、决策逻辑不可追溯以及系统行为难以验证。
*   **研究动机**：作者认为在制度化（Institutional）和规范化（Normative）的领域，需要一种“以世界为中心”的架构（WMAS）。在这种架构中，世界本身（而非智能体）是主要的表示、学习和协调基质，从而确保全局语义一致性和长期稳定性。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将“世界”定义为一个显式的数学元组 $W = (E, R, S, A, T, C)$（包含实体、关系、状态空间、动作、转换函数和约束），并提出**语义模型（Semantic Models）**作为其形式化实现。
*   **关键技术细节**：
    *   **双层架构**：
        1.  **基础语义层（Ground Semantic Layer）**：基于“对象本体（Object Ontologies）”，存储事实、实体状态和事务性操作。它是系统的“单一真理来源”。
        2.  **因果知识层（Causal Knowledge Layer）**：基于逻辑-概率推理，存储从基础层学习到的概率因果关系，用于预测和决策支持。
    *   **语义机器学习（SML）**：一种增量式学习机制。当基础层的状态发生变化时，SML 会局部更新因果知识层，无需全局重训，保证了知识与现实的同步。
    *   **智能体集成**：智能体（包括人类、符号智能体或大语言模型 LLM）通过受控接口与共享世界交互。LLM 被视为“受限推理器”，其输入和动作被限制在本体定义的范围内，以减少幻觉。

### 3. 实验设计与基准对比
*   **实验场景**：论文并未采用传统的 AI 算法基准测试（如 ImageNet 或 GLUE），而是通过 **Ontobox 平台**（前身为 bSystem）在多个实际工业/机构场景中进行了验证。
*   **应用案例**：
    *   私人诊所管理系统。
    *   汽车贷款管理系统。
    *   水培农场自动控制系统。
*   **对比对象**：主要是定性地对比了“以智能体为中心”的传统架构，强调在状态控制、语义一致性和可解释性方面的优势。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **原因分析**：由于该研究侧重于系统架构和符号化/概率逻辑推理，而非大规模深度学习模型的训练，其算力需求通常集中在本体查询优化和增量式概率推理上，而非高能耗的张量计算。

### 5. 实验数量与充分性
*   **实验规模**：论文提到了在上述三个不同领域的实际部署应用。
*   **充分性评价**：
    *   从**工程可行性**角度看，实验是充分的，证明了 WMAS 可以在复杂业务逻辑中运行。
    *   从**统计学/定量对比**角度看，缺乏与传统 MAS 框架在处理效率、冲突率等指标上的量化对比数据。实验更倾向于“存在性证明（Existence Proof）”。

### 6. 主要结论与发现
*   **适用性边界**：以世界为中心的架构最适合“本体显式、结构稳定、规范驱动、状态可观测、语义增长受限”的领域（如企业管理）。
*   **架构优势**：通过共享语义模型，可以有效缓解智能体信念冲突、决策逻辑不可追踪等问题。
*   **LLM 增强**：将 LLM 锚定在显式本体中，是解决 LLM 在专业领域应用时产生幻觉和缺乏逻辑约束的有效途径。

### 7. 优点与亮点
*   **理论严谨性**：对“世界”的维度（本体显式性、规范性、感知-审议比等）进行了清晰的分类学定义。
*   **可解释性**：因果知识层采用逻辑-概率关系，可以直接转化为自然语言，为人类用户提供透明的决策依据。
*   **工程实用性**：提出的 Ontobox 平台将本体作为“可执行的技术规范”，提高了复杂管理系统的开发效率。

### 8. 不足与局限
*   **应用限制**：该架构不适用于感知主导、环境高度不可预测或语义边界开放的领域（如开放道路自动驾驶或未知环境探索）。
*   **初始成本**：构建高质量的对象本体需要深厚的领域专家知识，前期知识工程投入较大。
*   **反应速度**：SML 的增量学习虽然保证了适应性，但对于需要瞬时反应的高频控制任务可能存在延迟。
*   **量化数据缺失**：论文缺乏关于系统扩展性、推理延迟等性能指标的详细实验数据。

（完）
