---
title: "DVAR: Adversarial Multi-Agent Debate for Video Authenticity Detection"
title_zh: DVAR：用于视频真实性检测的对抗性多智能体辩论
authors: "Hongyuan Qi, Feifei Shao, Ming Li, Hehe Fan, Jun Xiao"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16987v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 用于视频真实性检测的多智能体法证推理
tldr: 针对视频生成技术快速演进导致的检测泛化性难题，本文提出DVAR框架。该框架将视频真实性检测转化为多智能体辩论过程，通过“生成假设”与“自然机制”智能体的对抗性推理及奥卡姆剃刀原则进行裁决。DVAR无需训练，结合动态知识库GenVideoKB，在保持高性能的同时显著提升了对未知生成架构的泛化能力和推理透明度。
selection_source: fresh_fetch
motivation: 传统视频检测方法难以泛化到训练分布之外的新型生成技术，且缺乏透明的推理逻辑。
method: 构建多智能体辩论框架，通过生成与自然机制智能体的对抗质询，并利用最小描述长度原则量化解释成本以进行裁决。
result: DVAR在多个数据集上达到了与有监督最先进方法相当的性能，并在未知生成架构上展现出卓越的泛化能力。
conclusion: 该研究证明了将视频检测转化为结构化辩论推理的有效性，为取证分析提供了一种鲁棒且可解释的新方案。
---

## 摘要
视频生成技术的飞速发展给媒体取证带来了严峻挑战，因为传统的检测方法往往难以泛化到其训练分布之外。针对这一问题，我们提出了 DVAR（基于辩论的视频真实性推理），这是一个无需训练的框架，它将视频检测重新定义为一个结构化的多智能体取证推理过程。DVAR 超越了传统的模式匹配范式，组织了“生成假设智能体”与“自然机制智能体”之间的对抗竞争。通过多轮迭代的交叉质询，这些智能体针对异常证据为其各自的解释进行辩护，推动逻辑收敛，使真相在严格的压力测试中浮出水面。为了裁定这些相互冲突的主张，我们通过最小描述长度（MDL）框架应用奥卡姆剃刀原则，定义了“解释成本”来量化每条推理路径的“逻辑负担”。此外，我们还集成了 GenVideoKB，这是一个动态知识库，旨在提供关于生成边界和失效模式的高层推理启发式知识。广泛的实验表明，DVAR 在与有监督的最先进方法竞争中取得了具有竞争力的性能，同时对未见的生成架构表现出卓越的泛化能力。通过将检测转化为透明的辩论，DVAR 为稳健的视频真实性评估提供了显式且可解释的推理轨迹。

## Abstract
The rapid evolution of video generation technologies poses a significant challenge to media forensics, as conventional detection methods often fail to generalize beyond their training distributions. To address this, we propose DVAR (Debate-based Video Authenticity Reasoning), a training-free framework that reformulates video detection as a structured multi-agent forensic reasoning process. Moving beyond the paradigm of pattern matching, DVAR orchestrates a competition between a Generative Hypothesis Agent and a Natural Mechanism Agent. Through iterative rounds of cross-examination, these agents defend their respective explanations against abnormal evidence, driving a logical convergence where the truth emerges from rigorous stress-testing. To adjudicate these conflicting claims, we apply Occam's Razor through the Minimum Description Length (MDL) framework, defining an Explanatory Cost to quantify the "logical burden" of each reasoning path. Furthermore, we integrate GenVideoKB, a dynamic knowledge repository that provides high-level reasoning heuristics on generative boundaries and failure modes. Extensive experiments demonstrate that DVAR achieves competitive performance against supervised state-of-the-art methods while exhibiting superior generalization to unseen generative architectures. By transforming detection into a transparent debate, DVAR provides explicit, interpretable reasoning traces for robust video authenticity assessment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DVAR**（Debate-based Video Authenticity Reasoning）的视频真实性检测框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的深度伪造（Deepfake）检测方法主要依赖于在特定数据集上进行有监督的模式匹配。随着视频生成技术（如 Sora 等）的快速演进，这些“黑盒”模型在面对未见过的生成架构时，往往会出现性能崩溃（泛化性差），且缺乏可解释的推理逻辑。
*   **研究背景**：人类专家在检测伪造视频时，通常会寻找违反物理规律的异常，并权衡“自然现象”与“生成伪造”哪种解释更合理。论文旨在利用多模态大模型（LVLM）模拟这种认知过程，将检测任务从“图像分类”转变为“逻辑推理”。

### 2. 方法论：DVAR 框架
DVAR 是一个**无需训练（Training-free）**的框架，包含四个关键阶段：
*   **证据发现（Evidence Discovery）**：从视频中采样关键帧，识别出“伪造痕迹”（Forgery Traces），即那些违反物理常识或时间一致性的异常现象。
*   **多智能体多轮辩论（Multi-agent Debate）**：
    *   **生成假设智能体（GHA）**：攻击方，将异常归因于生成模型的缺陷（如扩散模型的不稳定性）。
    *   **自然机制智能体（NMA）**：辩护方，尝试用自然物理过程（如风吹、滚动快门、景深模糊）来解释异常。
    *   通过多轮对抗质询，迫使双方逻辑收敛，减少大模型的幻觉。
*   **解释成本计算（Explanatory Cost Calculation）**：引入**最小描述长度（MDL）**原则（奥卡姆剃刀）。如果一个假设需要过多的辅助假设或复杂的描述才能解释通，则其“逻辑负担”更重。公式化为计算压缩后解释的 Token 长度：$\Delta \ell_j = \ell(t_j | nat) - \ell(t_j | gen)$。
*   **最终决策裁决（Arbiter）**：由裁决智能体汇总所有痕迹的辩论记录和成本差异，给出最终标签（真/假）、置信度及推理依据。
*   **外部知识库（GenVideoKB）**：一个动态更新的知识库，存储生成模型的边界、失效模式及历史失败案例，为辩论提供启发式引导。

### 3. 实验设计
*   **数据集**：
    *   **Buster**：专注于分布外（OOD）泛化性评估的长视频数据集。
    *   **GenVidBench**：包含数百万视频的大规模基准测试（实验中使用了 4,126 个视频的代表性子集）。
*   **对比方法**：
    *   **有监督模型**：3D ResNet, I3D, ViViT, DeMamba, BusterX++ 等。
    *   **提示词基准**：直接使用 GPT-5.4 进行判断（Direct Prompting）以及增加思维链（CoT）。
*   **评估指标**：准确率（ACC）和 F1 分数。

### 4. 资源与算力
*   **算力需求**：论文明确指出 DVAR 是**无需训练**的，因此不需要昂贵的 GPU 集群进行模型训练。
*   **推理实现**：实验主要通过 API 调用完成（使用了 GPT-5.4, GPT-4o, Qwen-3.5-Plus 等模型）。推理过程在标准工作站上即可运行。
*   **开销**：虽然节省了训练算力，但多轮辩论增加了推理时的 Token 消耗（约为直接提示的 3 倍），不过可以通过 Token 缓存技术缓解。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上进行了测试，涵盖了多种生成器。
*   **消融实验**：非常充分。分别验证了证据发现、对抗辩论、成本计算和知识库（GenVideoKB）每个组件的贡献。
*   **敏感性分析**：测试了不同采样帧率（FPS）和辩论轮数对结果的影响，确定了 5 FPS 和 2 轮辩论为最优平衡点。
*   **客观性**：通过使用不同的底层大模型（GPT, Qwen）验证了框架的鲁棒性，证明性能提升源于框架设计而非单一模型的强大。

### 6. 主要结论与发现
*   **性能卓越**：DVAR 在无需任何训练的情况下，在 Buster OOD 测试中达到了 92.2% 的准确率，与最先进的有监督模型（BusterX++）持平。
*   **泛化能力强**：在面对训练集之外的新型生成器时，DVAR 的表现远超传统有监督方法。
*   **推理必要性**：直接使用大模型（Direct Prompting）准确率仅为 52.3%，证明了结构化辩论和解释成本机制是实现高精度检测的关键。

### 7. 优点与亮点
*   **透明度与可解释性**：不仅给出真假判断，还提供了详细的辩论轨迹，展示了为何判定为伪造。
*   **无需训练**：极大地降低了部署门槛，能够快速适应不断涌现的新型视频生成技术。
*   **逻辑严密**：引入 MDL 原则量化“解释成本”，为大模型的定性推理提供了定量裁决标准。

### 8. 不足与局限
*   **推理延迟与成本**：多智能体辩论涉及多次 API 调用，Token 消耗较大，实时性不如轻量级 CNN 模型。
*   **低运动场景失效**：在动作简单、重复或持续时间极短的视频中，由于缺乏足够的物理交互和时间动态，证据发现模块可能难以提取有效痕迹。
*   **压缩伪影干扰**：高度压缩的真实视频产生的伪影可能被误认为是生成伪造，导致误报。

（完）
