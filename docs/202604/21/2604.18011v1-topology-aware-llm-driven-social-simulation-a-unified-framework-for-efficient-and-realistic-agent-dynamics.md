---
title: "Topology-Aware LLM-Driven Social Simulation: A Unified Framework for Efficient and Realistic Agent Dynamics"
title_zh: 拓扑感知的 LLM 驱动社会模拟：一个高效且真实的智能体动态统一框架
authors: "Yuwei Xu, Shulun Zhang, Yingli Zhou, Shipei Zeng, Laks V. S. Lakshmanan, Chenhao Ma"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18011v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 拓扑感知的 LLM 驱动社会模拟框架
tldr: "针对现有大语言模型驱动的社会模拟忽略网络拓扑结构导致的效率低下和真实感不足问题，本文提出TopoSim框架。该框架通过将具有相似结构角色的智能体对齐到共享骨干单元以减少冗余计算，并引入基于拓扑的异构社会影响建模。实验表明，TopoSim在保持或提升模拟保真度的同时，显著降低了50%-90%的Token消耗，并能更准确地复现现实社会的结构化现象。"
selection_source: fresh_fetch
motivation: 现有的LLM社会模拟将社交网络视为固定支架，忽略了影响行为收敛和异构影响的结构信号，导致模拟效率低且不真实。
method: 提出TopoSim框架，通过共享骨干单元协调相似角色的智能体更新，并基于网络拓扑引入异构的社会影响交互模式。
result: "在多个数据集上实现了50%-90%的Token成本削减，同时在模拟保真度和现实结构现象的复现上表现优异。"
conclusion: TopoSim为高效且真实的LLM驱动社会模拟提供了统一的拓扑感知方案，具有极强的扩展性和泛化能力。
---

## 摘要
社会模拟通过建模个体交互如何产生大规模社会动态，对于理解人类集体行为至关重要。大语言模型（LLMs）的最新进展使得多智能体框架具备了类人的推理和沟通能力。然而，现有的基于 LLM 的模拟将社交网络视为固定的通信支架，未能利用塑造现实系统中行为收敛和异质影响的结构信号，这往往导致模拟动态效率低下且不真实。为应对这一挑战，我们提出了 TopoSim，这是一个统一的拓扑感知社会模拟框架，它从两个互补的维度将结构推理显式地集成到智能体交互中。首先，TopoSim 将具有相似结构角色和交互上下文的智能体对齐到共享的主干单元中，从而实现协调更新，在保留涌现社会动态的同时减少冗余计算。其次，TopoSim 将社会影响建模为结构诱导信号，引入了基于网络拓扑而非统一影响假设的异质交互模式。在三个社会模拟框架和多种数据集上的广泛实验表明，TopoSim 在减少 50% - 90% Token 消耗的同时，实现了相当或更高的模拟保真度。此外，我们的方法能更准确地复现现实社会系统中观察到的关键结构现象，并表现出强大的泛化性和可扩展性。

## Abstract
Social simulation is essential for understanding collective human behavior by modeling how individual interactions give rise to large-scale social dynamics. Recent advances in large language models (LLMs) have enabled multi-agent frameworks with human-like reasoning and communication capabilities. However, existing LLM-based simulations treat social networks as fixed communication scaffolds, failing to leverage the structural signals that shape behavioral convergence and heterogeneous influence in real-world systems, which often leads to inefficient and unrealistic dynamics. To address this challenge, we propose TopoSim, a unified topology-aware social simulation framework that explicitly integrates structural reasoning into agent interactions along two complementary dimensions. First, TopoSim aligns agents with similar structural roles and interaction contexts into shared backbone units, enabling coordinated updates that reduce redundant computation while preserving emergent social dynamics. Second, TopoSim models social influence as a structure-induced signal, introducing heterogeneous interaction patterns grounded in network topology rather than uniform influence assumptions. Extensive experiments across three social simulation frameworks and diverse datasets demonstrate that TopoSim achieves comparable or improved simulation fidelity while reducing token consumption by 50 - 90%. Moreover, our approach more accurately reproduces key structural phenomena observed in real-world social systems and exhibits strong generalization and scalability.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TopoSim** 的统一框架，旨在解决大语言模型（LLM）驱动的社会模拟中存在的效率低下和真实感不足的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：社会模拟通过建模微观个体交互来揭示宏观社会现象。近年来，基于 LLM 的多智能体模拟因其强大的推理和沟通能力而兴起。
*   **核心问题**：
    1.  **计算冗余**：现有模拟将每个智能体视为独立个体。但在现实中，处于相似网络位置（拓扑角色）的个体往往面临相似的上下文，导致 LLM 进行大量重复且冗余的推理，消耗极高。
    2.  **影响力同质化假设**：现有模型通常假设邻居间的影响力是均等的，忽略了现实中“大 V”与普通用户之间巨大的影响力差异，导致模拟结果不真实。
*   **研究动机**：如何显式利用网络拓扑信号来引导智能体行为，从而实现既高效又真实的社会模拟？

### 2. 方法论：TopoSim 框架
TopoSim 通过两个核心模块将拓扑结构集成到模拟中：

*   **拓扑诱导的更新协调（Update Coordination）**：
    *   **核心思想**：将结构角色相似且当前状态一致的智能体归为“协调单元（Coordination Unit）”。
    *   **技术细节**：首先利用 `struc2vec` 生成结构嵌入，筛选候选对；然后结合邻居意见分布（JS 散度）和内部状态（意见得分、固执度）计算一致性得分。
    *   **流程**：对每个单元仅调用一次 LLM 推理，并将结果共享给单元内所有成员，从而将推理次数从 $|V|$ 降至单元数 $|C|$。
*   **拓扑感知的异构影响力（Role Differentiation）**：
    *   **核心思想**：利用拓扑结构自动推导个体间的不对称影响力。
    *   **技术细节**：采用**个性化 PageRank (PPR)** 算法计算节点间的结构影响力权重。
    *   **流程**：在 Prompt 构建时，根据 PPR 权重将邻居信息划分为不同的重要性层级（如核心信息 vs. 外围信息），引导 LLM 在推理时给予不同权重的关注。
*   **理论分析**：论文通过 Lipschitz 连续性假设证明了协调更新的误差界限，展示了效率与保真度之间的权衡。

### 3. 实验设计
*   **实验框架**：在三个主流框架上进行验证：**EchoChamberSim (ECS)**（回声壁模拟）、**OASIS**（社交媒体模拟）、**FDE-LLM**（基于真实数据的意见预测）。
*   **数据集**：
    *   **合成数据**：ECS-50/500/5000（不同规模的合成网络）。
    *   **半合成数据**：OASIS（包含 196 个真实 X 账号及其连接）。
    *   **真实数据**：Weibo（约 200 名真实用户关于热点新闻的讨论轨迹）。
*   **对比基准（Benchmark）**：主要对比对象是**全量 LLM 模拟（Base）**，即不进行协调和角色区分的原始模拟方式。
*   **评估指标**：极化程度（Pol.）、全局分歧（GD）、归一化聚类指数（NCI）以及与真实轨迹的相似度。

### 4. 资源与算力
*   **硬件环境**：使用了一台 Ubuntu 24.04 服务器，配置为：
    *   **CPU**：2 颗 Intel Xeon Gold 6330。
    *   **内存**：1 TB RAM。
    *   **GPU**：1 块 **NVIDIA RTX A5000**。
*   **模型后端**：默认使用 `gpt-4o-mini`，并在鲁棒性测试中使用了 `Qwen-plus` 和 `DeepSeek-V3`。
*   **训练/运行时间**：文中未明确给出具体的总运行小时数，但强调了 Token 消耗的显著降低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 50 节点到 **5000 节点**（1.5 万条边）的跨度，这在 LLM 智能体模拟领域属于较大规模。
*   **实验维度**：
    *   **效率测试**：统计了不同规模下的 Token 节省情况。
    *   **保真度测试**：对比了宏观指标的演变曲线。
    *   **消融实验**：分别验证了“更新协调”和“角色区分”的效果。
    *   **鲁棒性测试**：更换了不同的 LLM 后端。
*   **评价**：实验设计较为全面，涵盖了合成与真实场景，且通过跨模型验证确保了结论的客观性。

### 6. 主要结论与发现
*   **极高的效率提升**：在保持模拟效果的前提下，TopoSim 减少了 **50% - 90%** 的 Token 消耗。在 OASIS 框架下，Token 消耗从 4.53M 降至 376K。
*   **更强的真实性**：在微博真实数据集上，引入角色区分（RD）后的模拟轨迹与真实轨迹的相似度保持在 90% 以上，显著优于基准模型。
*   **良好的可扩展性**：成功将 LLM 模拟扩展至 5000 个智能体，且宏观动态保持稳定。
*   **分工明确**：消融实验证明，角色区分主要提升真实感，而更新协调主要负责提升效率。

### 7. 优点与亮点
*   **拓扑驱动的创新**：不再将社交网络视为死板的通信管道，而是将其作为主动的推理信号。
*   **大幅降本增效**：通过“协调单元”解决了 LLM 模拟中长期存在的计算昂贵问题，使大规模模拟成为可能。
*   **无需额外标注**：影响力权重完全由拓扑结构（PPR）自动推导，无需人工定义角色或额外监督数据。
*   **理论支撑**：提供了近似误差的数学分析，增强了方法的严谨性。

### 8. 不足与局限
*   **静态网络限制**：目前的研究基于静态社交网络，尚未考虑现实中社交关系随时间动态演变（加好友/取关）的情况。
*   **Prompt 依赖性**：虽然模型具有鲁棒性，但角色区分的效果仍依赖于 Prompt 对重要性层级的描述，不同 LLM 对此类指令的敏感度可能存在差异。
*   **应用场景**：主要集中在意见传播和新闻扩散，对于更复杂的协作任务（如软件开发模拟）是否适用尚待验证。

（完）
