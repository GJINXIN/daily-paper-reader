---
title: Retrieval-Augmented Multimodal Model for Fake News Detection
title_zh: 用于虚假新闻检测的检索增强多模态模型
authors: "Yiheng Li, Weihai Lu, Hanyi Yu, Yue Wang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18112v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 社交媒体上的多模态虚假新闻检测
tldr: 针对多模态虚假新闻检测中孤立评估新闻、缺乏跨实例叙事一致性及领域知识不足的问题，本文提出RAMM模型。该模型以多模态大语言模型为骨干，通过抽象叙事对齐模块捕捉跨领域叙事一致性，并利用语义表示对齐模块实现基于实例的类比推理。实验证明RAMM在多个数据集上表现优异，有效提升了对新兴和跨领域虚假新闻的检测能力。
selection_source: fresh_fetch
motivation: 现有模型因孤立评估新闻而难以捕捉跨实例叙事一致性，且在处理新兴或数据稀缺领域时缺乏必要的领域特定知识。
method: 提出RAMM模型，采用多模态大模型为骨干，结合抽象叙事对齐模块聚合知识，并通过语义表示对齐模块将推理过程转变为基于实例的类比推理。
result: 在三个公开数据集上的实验结果表明，RAMM在多模态多领域虚假新闻检测任务中表现出色，验证了其各模块的有效性。
conclusion: RAMM通过引入检索增强和类比推理机制，有效解决了虚假新闻检测中的叙事建模与领域泛化挑战。
---

## 摘要
近年来，多模态多领域虚假新闻检测受到了越来越多的关注。然而，该研究方向面临两个重大挑战：（1）无法捕捉跨实例的叙事一致性：现有模型通常孤立地评估每条新闻，未能捕捉到跨实例的叙事一致性，因此难以应对社交媒体驱动的集群式虚假新闻传播；（2）缺乏用于推理的领域特定知识：传统模型仅依赖于训练期间编码在参数中的知识，难以泛化到新领域或数据稀缺领域（如突发事件或小众话题）。为了应对这些挑战，我们提出了用于虚假新闻检测的检索增强多模态模型（RAMM）。首先，RAMM 采用多模态大语言模型（MLLM）作为骨干网络，以捕捉新闻样本中的跨模态语义信息。其次，RAMM 包含一个抽象叙事对齐模块，该模块能够自适应地从不同领域的各种实例中提取抽象叙事一致性，聚合相关知识，从而实现对高层叙事信息的建模。最后，RAMM 引入了语义表示对齐模块，将模型的决策范式与人类对齐——具体而言，它将模型的推理过程从对多模态特征的直接推断转变为基于实例的类比推理过程。在三个公开数据集上的广泛实验结果验证了所提方法的有效性。我们的代码可在以下链接获取：https://github.com/li-yiheng/RAMM

## Abstract
In recent years, multimodal multidomain fake news detection has garnered increasing attention. Nevertheless, this direction presents two significant challenges: (1) Failure to Capture Cross-Instance Narrative Consistency: existing models usually evaluate each news in isolation, fail to capture cross-instance narrative consistency, and thus struggle to address the spread of cluster based fake news driven by social media; (2) Lack of Domain Specific Knowledge for Reasoning: conventional models, which rely solely on knowledge encoded in their parameters during training, struggle to generalize to new or data-scarce domains (e.g., emerging events or niche topics). To tackle these challenges, we introduce Retrieval-Augmented Multimodal Model for Fake News Detection (RAMM). First, RAMM employs a Multimodal Large Language Model (MLLM) as its backbone to capture cross-modal semantic information from news samples. Second, RAMM incorporates an Abstract Narrative Alignment Module. This component adaptively extracts abstract narrative consistency from diverse instances across distinct domains, aggregates relevant knowledge, and thereby enables the modeling of high-level narrative information. Finally, RAMM introduces a Semantic Representation Alignment Module, which aligns the model's decision-making paradigm with that of humans - specifically, it shifts the model's reasoning process from direct inference on multimodal features to an instance-based analogical reasoning process. Extensive experimental results on three public datasets validate the efficacy of our proposed approach. Our code is available at the following link: https://github.com/li-yiheng/RAMM

---

## 论文详细总结（自动生成）

这是一份关于论文《Retrieval-Augmented Multimodal Model for Fake News Detection》（用于虚假新闻检测的检索增强多模态模型）的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：社交媒体上的虚假新闻常以“集群”形式传播，即多条内容不同（图片、文字有差异）的新闻实际上在推销同一个虚假叙事（如“某地选举舞弊”）。
*   **核心问题**：
    1.  **缺乏跨实例叙事一致性**：现有模型通常孤立地评估单条新闻，无法识别出属于同一虚假叙事集群的关联性。
    2.  **领域知识匮乏**：传统模型依赖训练参数中的静态知识，在面对新兴事件或数据稀缺的特定领域（如医疗、科学）时，推理能力和泛化性较差。
*   **整体含义**：本文旨在通过“检索增强”和“类比推理”机制，让模型像人类事实核查员一样，通过关联历史信息和跨领域知识来识别虚假新闻。

### 2. 方法论
论文提出了 **RAMM** 框架，主要包含三个核心组件：
*   **MLLM 骨干网络**：采用类似 BLIP-2 的架构，使用 Vision Transformer (ViT) 提取图像特征，通过 Q-Former 进行模态对齐，并以大语言模型（如 Qwen2.5 或 Llama3）作为核心推理引擎。
*   **抽象叙事对齐模块 (ANA)**：
    *   **叙事提取**：利用 LLM 总结新闻的底层抽象叙事（而非表面文字）。
    *   **同质检索**：基于叙事嵌入（SimCSE/BGE）从库中检索领域内和跨领域的相似实例。
    *   **公共信息瓶颈损失 (CIBL)**：这是本文的关键技术，通过变分编码器将当前新闻与检索到的相似新闻进行融合，提取“公共叙事特征”并剔除噪声。该损失函数包含对齐（Alignment）、重构（Reconstruction）和压缩（Compression）三部分，确保特征既包含核心叙事又保留实例独特性。
*   **语义表示对齐模块 (SRA)**：
    *   **类比推理**：利用 CLIP 计算相似度，从训练集中检索最相关的真实/虚假案例作为“示例（Demonstration）”。
    *   **提示词工程**：将示例与目标新闻一同输入 MLLM，引导模型进行基于实例的类比推理，而非直接进行黑盒分类。

### 3. 实验设计
*   **数据集**：
    *   **Weibo & Weibo-21**：中文多领域数据集，包含科学、军事、教育、社会等 9 个领域。
    *   **FineFake**：英文多领域数据集，包含政治、娱乐、商业等 6 个领域。
*   **Benchmark 与对比方法**：
    *   **单模态多领域方法**：MDFEND, M3DFEND, PLDFEND。
    *   **多模态多领域方法**：KATMF, MMDFND, DAMMFND, IMOL, MMHT。
    *   **MLLM 方法**：GSFND, SNIFFER, NRFE, BLIP2。
*   **评价指标**：F1-score、Accuracy、AUC。

### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA A100 GPU** 进行实验。
*   **软件框架**：PyTorch 2.0。
*   **模型配置**：中文使用 Qwen2.5-7B，英文使用 Llama3-8B；叙事生成使用 Qwen3-MAX。
*   **训练细节**：训练 3 个 epoch，AdamW 优化器，学习率 1e-4，Batch Size 为 4。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了非常详尽的实验，包括：
    *   **总体性能对比**：在三个数据集上全面超越 SOTA。
    *   **消融实验**：分别验证了 SRA、ANA 模块以及 CIBL 损失函数中各部分的贡献。
    *   **参数敏感性分析**：测试了检索数量（k值）和损失权重对结果的影响。
    *   **领域泛化实验**：通过“留一领域（Leave-domain-out）”测试，验证了模型在完全陌生领域（零样本）下的鲁棒性。
    *   **噪声鲁棒性实验**：模拟检索到错误/噪声信息的情况，验证模型的抗干扰能力。
    *   **可视化分析**：使用 t-SNE 展示了特征空间的聚类效果。
*   **评价**：实验设计科学、客观，对比基准涵盖了从传统深度学习到最新大模型的各类主流方法，充分证明了 RAMM 的优越性。

### 6. 主要结论与发现
*   **叙事一致性至关重要**：显式建模新闻背后的抽象叙事，能显著提升对集群式虚假新闻的识别率。
*   **类比推理优于直接推理**：通过 SRA 模块引入参考示例，能有效对齐人类的决策逻辑，提升 MLLM 的判断准确度。
*   **跨领域检索增强泛化**：引入跨领域的相似叙事信息，能帮助模型在数据稀缺的领域表现更好。
*   **CIBL 损失的有效性**：通过信息瓶颈理论处理检索信息，能有效防止模型因引入检索噪声而导致的性能下降。

### 7. 优点
*   **创新性强**：首次将检索增强机制引入 MLLM 虚假新闻检测，并专门针对“叙事集群”问题建模。
*   **理论支撑扎实**：提出的 CIBL 损失函数有明确的信息论推导，解决了特征对齐中的表示坍缩问题。
*   **泛化能力突出**：在跨领域和零样本场景下表现优异，具有很强的实际应用潜力。

### 8. 不足与局限
*   **计算开销**：由于使用了 MLLM 和多次检索（叙事提取、SRA 检索），推理延迟可能较高，在大规模实时检测场景下存在挑战。
*   **依赖检索库质量**：模型的表现高度依赖于检索库的覆盖范围和质量，如果库中缺乏相关叙事，增强效果会打折扣。
*   **LLM 偏见风险**：使用 LLM 提取叙事摘要可能引入 LLM 本身的幻觉或预训练偏见。

（完）
