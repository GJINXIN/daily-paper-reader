---
title: "Proactive Agent Research Environment: Simulating Active Users to Evaluate Proactive Assistants"
title_zh: 主动式智能体研究环境：通过模拟活跃用户评估主动式助手
authors: "Deepak Nathani, Cheng Zhang, Chang Huan, Jiaming Shan, Yinfei Yang, Alkesh Patel, Zhe Gan, William Yang Wang, Michael Saxon, Xin Eric Wang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00842v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 模拟活跃用户以评估主动式助手
tldr: 本研究针对主动型智能体缺乏真实用户模拟框架的问题，提出了Pare框架。Pare将应用程序建模为具有状态导航和依赖状态动作空间的有限状态机，从而实现了更真实的动态用户模拟。基于此，研究者推出了包含143个任务的Pare-Bench基准测试，涵盖通信、办公和日程管理等领域，旨在全面评估智能体在背景观察、目标推理、干预时机及多应用协作方面的能力。
selection_source: fresh_fetch
motivation: 现有的评估方法将应用简化为扁平的API调用，无法捕捉数字环境中用户交互的有状态和顺序特性，导致难以模拟真实用户。
method: 提出了Pare框架，通过将应用建模为有限状态机，支持具有状态感知能力的导航和动作空间，以实现主动用户模拟。
result: 构建了包含143个多样化任务的Pare-Bench基准，用于测试智能体在目标推理、干预时机选择和多应用编排等方面的表现。
conclusion: Pare为主动型数字助理的开发和评估提供了一个更具现实意义的仿真环境，填补了用户模拟领域的空白。
---

## 摘要
能够预测用户需求并自主执行任务的主动式智能体作为数字助手具有广阔的前景，然而，缺乏真实的用户模拟框架阻碍了其发展。现有方法将应用程序建模为扁平的工具调用 API，未能捕捉数字环境中用户交互的有状态和顺序特性，使得真实的用户模拟难以实现。我们推出了主动式智能体研究环境（Pare），这是一个用于在数字环境中构建和评估主动式智能体的框架。Pare 将应用程序建模为具有有状态导航和状态依赖动作空间的有限状态机，供用户模拟器使用，从而实现了活跃用户模拟。在此基础上，我们提出了 Pare-Bench，这是一个包含 143 个多样化任务的基准测试，涵盖了通信、生产力、日程安排和生活方式类应用，旨在测试上下文观察、目标推理、干预时机以及多应用编排能力。

## Abstract
Proactive agents that anticipate user needs and autonomously execute tasks hold great promise as digital assistants, yet the lack of realistic user simulation frameworks hinders their development. Existing approaches model apps as flat tool-calling APIs, failing to capture the stateful and sequential nature of user interaction in digital environments and making realistic user simulation infeasible. We introduce Proactive Agent Research Environment (Pare), a framework for building and evaluating proactive agents in digital environments. Pare models applications as finite state machines with stateful navigation and state-dependent action space for the user simulator, enabling active user simulation. Building on this foundation, we present Pare-Bench, a benchmark of 143 diverse tasks spanning communication, productivity, scheduling, and lifestyle apps, designed to test context observation, goal inference, intervention timing, and multi-app orchestration.

---

## 论文详细总结（自动生成）

### 论文总结：Proactive Agent Research Environment (Pare)

#### 1. 论文的核心问题与整体含义
*   **研究背景**：当前的语言模型（LM）智能体大多处于“被动响应”模式，即仅在接收到明确指令后才行动。而理想的数字助手应该是“主动式”的，能够通过观察环境和用户行为，预判需求并自主执行任务。
*   **核心问题**：开发主动式智能体的最大障碍是缺乏真实的评估框架。现有的模拟环境将应用程序简化为扁平的 API 调用，忽略了真实用户在操作 App 时必须经历的界面导航、状态转换和顺序交互。这种简化导致无法真实模拟用户与助手的动态互动。
*   **整体含义**：本文提出了 **Pare** 框架，通过将 App 建模为有限状态机（FSM），构建了一个非对称的模拟环境，旨在为主动式智能体的目标推理、干预时机和多应用协作能力提供客观、动态的评估。

#### 2. 论文提出的方法论
*   **核心思想**：引入**非对称模拟设计**。用户模拟器必须像真人一样在 App 的不同状态（屏幕）间导航，而主动式助手则拥有直接访问后端 API 的权限。这种设计模拟了现实中助手拥有系统级权限而用户受限于 UI 操作的真实场景。
*   **关键技术细节**：
    *   **FSM 建模**：每个 App 被定义为一个有限状态机，状态代表屏幕，转换代表用户动作（如填写表单、点击跳转）。
    *   **Stackelberg POMDP 建模**：将用户与助手的交互形式化为 Stackelberg 部分可观测马尔可夫决策过程。用户是“领导者”，助手是“跟随者”，助手通过观察用户的动作序列 $A_U$ 和环境事件 $E$ 来推断隐藏的目标 $G_U$。
    *   **Observe-Execute 架构**：助手分为两个阶段：
        1.  **观察模式（Observe）**：持续监测，通过只读工具收集信息，决定是否提出建议（Proposal）。
        2.  **执行模式（Execute）**：一旦用户接受建议，助手切换到全权限模式完成任务。
*   **公式定义**：评估核心在于最大化建议接受率 $R_{Accept}$（反映干预时机的准确性）和任务成功率 $R_{Succeed}$（反映执行能力）。

#### 3. 实验设计
*   **Benchmark (Pare-Bench)**：包含 **143 个多样化任务**，涵盖通信（邮件、短信）、生产力（笔记、文件）、日程安排（日历、提醒）和生活方式（打车、购物、租房）等 10 多个 App。
*   **对比方法**：评估了 7 种主流 LLM 作为助手的表现：
    *   **闭源模型**：Claude 4.5 Sonnet, GPT-5, Gemini 3 Pro, Gemini 3 Flash。
    *   **开源模型**：Qwen 3 4B, Llama 3.2 3B, Gemma 3 4B。
*   **实验场景**：
    *   **标准测试**：评估基础成功率和接受率。
    *   **鲁棒性测试**：引入工具调用失败（概率 0.1-0.4）和环境噪声（每分钟 2-6 个干扰事件）。
    *   **消融实验**：更换不同的 LLM 作为用户模拟器，观察对评估结果的影响。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：该研究侧重于评估框架的构建和现有预训练模型的推理表现，而非从头训练新模型，因此算力消耗主要集中在 LLM API 的调用和本地小模型的推理上。

#### 5. 实验数量与充分性
*   **实验规模**：每个场景在 4 轮独立运行中进行测试，以减少随机性影响。
*   **充分性**：
    *   **多维度评估**：不仅关注成功率，还分析了建议决策模式（接受、拒绝、需进一步收集上下文）。
    *   **鲁棒性验证**：通过模拟工具故障和信息噪音，验证了模型在复杂现实环境下的稳定性。
    *   **公平性**：通过“用户模型消融实验”证明，尽管不同用户模型会导致绝对分值的波动，但对不同助手的性能排名保持一致，证明了 Benchmark 的客观性。

#### 6. 论文的主要结论与发现
*   **性能天花板**：即使是最强的模型（Gemini 3 Flash 和 Claude 4.5 Sonnet），成功率也仅在 **42%** 左右，说明主动式任务仍极具挑战。
*   **小模型瓶颈**：对于 3B-4B 规模的小模型，主要的瓶颈在于**任务执行**而非目标推理。
*   **观察的重要性**：表现优秀的模型通常会进行更多的“只读”操作来收集环境信息，证明了充分的上下文观察是准确推理的前提。
*   **干预时机**：Claude 4.5 表现出最高的建议接受率（78.2%）和最低的建议频率，说明其在“何时干预”上最为谨慎和准确。

#### 7. 优点
*   **真实性高**：通过 FSM 模拟 UI 导航，解决了以往模拟器过于理想化、脱离实际交互逻辑的问题。
*   **架构合理**：Observe-Execute 架构强制要求用户确认，既体现了助手的主动性，又保障了用户的控制权和隐私。
*   **评估全面**：Pare-Bench 覆盖面广，且包含动态事件流，能够测试智能体处理异步信息的能力。

#### 8. 不足与局限
*   **非视觉交互**：目前仍基于 API 和文本描述，未涉及多模态视觉（如直接处理屏幕截图），无法评估智能体的视觉定位能力。
*   **用户模型偏差**：虽然使用了 LLM 模拟用户，但仍难以完全捕捉真实人类的情感、疲劳、多任务并行中的注意力分散等复杂特质。
*   **时间跨度限制**：实验设定了 10 个回合的上限，可能无法充分评估需要跨越数天或超长路径的复杂主动任务。
*   **隐私边界**：虽然 API 抽象提供了隐私保护，但在现实部署中，持续观察用户行为仍面临巨大的伦理和隐私挑战。

（完）
