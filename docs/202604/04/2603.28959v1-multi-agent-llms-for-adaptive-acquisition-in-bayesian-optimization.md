---
title: Multi-Agent LLMs for Adaptive Acquisition in Bayesian Optimization
title_zh: 用于贝叶斯优化中自适应采集的多智能体大语言模型
authors: "Andrea Carbonati, Mohammadsina Almasi, Hadis Anahideh"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28959v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 用于自适应搜索和决策策略的多智能体大语言模型
tldr: 本研究探讨了大语言模型（LLM）在贝叶斯优化中处理探索与利用权衡的能力。针对单智能体LLM在同时进行策略选择和候选生成时容易出现认知负荷、搜索不稳定及过早收敛的问题，提出了一种多智能体框架。该框架将搜索过程解耦为负责分配搜索权重（如信息量、多样性）的策略智能体和负责生成候选方案的执行智能体。实验证明，这种解耦方法使搜索决策显式可控，显著提升了LLM在连续优化任务中的表现。
selection_source: fresh_fetch
motivation: 单智能体LLM在处理复杂的探索与利用权衡时存在认知负荷，导致搜索动态不稳定且容易陷入局部最优。
method: 提出多智能体框架，通过策略智能体制定可解释的搜索权重，并由生成智能体据此产生候选方案。
result: 在各类连续优化基准测试中，该框架显著提高了LLM介导搜索的效率和鲁棒性。
conclusion: 显式分离战略决策与战术执行是优化LLM在黑盒优化任务中表现的有效途径。
---

## 摘要
探索与利用的权衡是序列决策和黑盒优化的核心，然而大语言模型（LLMs）如何对这种权衡进行推理和管理仍不为人所知。与贝叶斯优化（其中探索和利用通过采集函数显式编码）不同，基于 LLM 的优化依赖于对历史评估的隐式、基于提示的推理，这使得搜索行为难以分析或控制。在这项工作中，我们对 LLM 介导的搜索策略学习进行了指标层面的研究，探讨了 LLM 在探索的多种操作定义（包括信息量、多样性和代表性）下如何构建和调整探索-利用策略。我们表明，在单个提示中同时执行策略选择和候选生成的单智能体 LLM 方法会遭受认知过载，导致不稳定的搜索动态和过早收敛。为了解决这一局限性，我们提出了一个多智能体框架，将探索-利用控制分解为战略策略调解和战术候选生成。策略智能体为多个搜索标准分配可解释的权重，而生成智能体则根据由权重定义的搜索策略生成候选方案。这种分解使得探索-利用决策变得显式、可观察且可调节。在各种连续优化基准上的实证结果表明，将战略控制与候选生成分离显著提高了 LLM 介导搜索的有效性。

## Abstract
The exploration-exploitation trade-off is central to sequential decision-making and black-box optimization, yet how Large Language Models (LLMs) reason about and manage this trade-off remains poorly understood. Unlike Bayesian Optimization, where exploration and exploitation are explicitly encoded through acquisition functions, LLM-based optimization relies on implicit, prompt-based reasoning over historical evaluations, making search behavior difficult to analyze or control. In this work, we present a metric-level study of LLM-mediated search policy learning, studying how LLMs construct and adapt exploration-exploitation strategies under multiple operational definitions of exploration, including informativeness, diversity, and representativeness. We show that single-agent LLM approaches, which jointly perform strategy selection and candidate generation within a single prompt, suffer from cognitive overload, leading to unstable search dynamics and premature convergence. To address this limitation, we propose a multi-agent framework that decomposes exploration-exploitation control into strategic policy mediation and tactical candidate generation. A strategy agent assigns interpretable weights to multiple search criteria, while a generation agent produces candidates conditioned on the resulting search policy defined as weights. This decomposition renders exploration-exploitation decisions explicit, observable, and adjustable. Empirical results across various continuous optimization benchmarks indicate that separating strategic control from candidate generation substantially improves the effectiveness of LLM-mediated search.

---

## 论文详细总结（自动生成）

### 论文总结：用于贝叶斯优化中自适应采集的多智能体大语言模型

#### 1. 核心问题与研究动机
*   **核心问题**：在黑盒优化（Black-Box Optimization, BBO）中，如何让大语言模型（LLM）更有效地平衡“探索”（Exploration）与“利用”（Exploitation）。
*   **研究背景**：传统的贝叶斯优化（BO）通过显式的采集函数（如 EI, UCB）来平衡权衡，而基于 LLM 的优化通常依赖于隐式的提示词推理，导致搜索行为难以分析、控制且不稳定。
*   **动机**：单智能体 LLM 在同时处理“高层战略决策”（决定搜索策略）和“底层战术执行”（生成具体数值坐标）时，会产生**认知过载**，导致搜索动态不稳定、容易陷入局部最优或过早收敛。

#### 2. 方法论
该论文提出了一种**多智能体框架**，将搜索过程解耦为两个专门的角色：
*   **核心思想**：模仿传统 BO 中“采集函数设计”与“候选点优化”的分离，将战略与战术任务分配给不同的 LLM 智能体。
*   **关键技术细节**：
    *   **指标化定义**：定义了四种显式指标：① **利用（Exploitation）**：倾向于改进当前最佳值；② **信息量（Informativeness）**：倾向于不确定性高的区域；③ **多样性（Diversity）**：鼓励全局覆盖；④ **代表性（Representativeness）**：促进反映数据分布结构的采样。
    *   **策略智能体（Strategy Agent）**：负责推理当前的优化上下文（历史数据、进展趋势、停滞情况），并输出一组可解释的权重向量 $w_t$，定义各项指标的优先级。
    *   **生成智能体（Generation Agent）**：接收策略智能体给出的权重，作为约束条件来生成具体的候选点 $x_{t+1}$。
*   **算法流程**：
    1. 构建策略提示词（包含历史数据 $D_{t-1}$）。
    2. 策略智能体推断指标权重 $W_t$。
    3. 构建生成提示词（包含 $D_{t-1}$ 和 $W_t$）。
    4. 生成智能体产生候选点 $x_t$。
    5. 评估目标函数并更新历史记录。

#### 3. 实验设计
*   **实验场景/数据集**：
    *   **Rosenbrock 函数**：经典的平滑解析基准测试，用于评估数值精细优化能力。
    *   **超参数调优（HPT）**：高维机器学习任务，具有离散和非结构化特征。
    *   **机器人推物（Robot Pushing）**：复杂的工程控制问题，具有非凸和不连续的动力学特征。
*   **Benchmark 与对比方法**：
    *   **传统 BO 方法**：期望改进（EI）和置信上限（UCB）。
    *   **对比基准**：单智能体 LLM（Single-Agent LLM），即在单个提示词中完成策略和生成。
*   **模型选择**：所有实验均使用 **Llama-3.3-70b-instruct** 模型。

#### 4. 资源与算力
*   **算力说明**：论文提到实验是在伊利诺伊大学芝加哥分校（UIC）管理的计算基础设施上进行的。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。由于使用的是 70B 规模的模型，推测使用了高性能计算集群（如 A100 或 H100 节点）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个不同领域的基准测试进行了完整运行。
    *   每个实验设定了固定的 30 次函数查询预算（Evaluation Budget）。
    *   进行了**消融实验**（Ablation Studies），通过“配对指标”测试（如仅保留利用+多样性）来分析不同探索准则对性能的影响。
*   **充分性评价**：实验设计较为客观，涵盖了从简单解析函数到复杂工程问题的跨度。通过跟踪权重随时间的变化，提供了直观的策略演化证据。但受限于 LLM 推理成本，总查询次数（30次）相对较少，可能无法完全展现长期收敛特性。

#### 6. 主要结论与发现
*   **解耦的有效性**：将战略控制与候选生成分离，显著提高了搜索的稳定性和有效性，减少了单智能体常见的“随机震荡”现象。
*   **场景适应性**：
    *   在**平滑景观**（如 Rosenbrock）中，LLM 表现不如传统 BO（EI/UCB），因为 LLM 难以进行极高精度的数值微调。
    *   在**复杂/非结构化景观**（如 HPT 和机器人任务）中，多智能体 LLM 表现出极强的竞争力，甚至优于传统方法，因为它能根据搜索状态动态调整策略。
*   **自适应行为**：LLM 能够识别搜索停滞，并主动增加“多样性”或“信息量”的权重以跳出局部最优。

#### 7. 优点
*   **可解释性**：通过显式的权重向量，研究者可以清晰地观察到 LLM 在每一轮迭代中是在“想什么”（战略意图）。
*   **模块化设计**：多智能体框架降低了单个提示词的复杂性，缓解了 LLM 的认知负担。
*   **灵活性**：该框架不依赖于特定的概率代理模型（如高斯过程），因此更容易扩展到具有复杂约束或混合数据类型的优化问题。

#### 8. 不足与局限
*   **数值精度瓶颈**：LLM 本质上是文本处理工具，在需要极高数值精度的连续空间优化中，其表现仍逊于基于数学公式的传统优化器。
*   **计算成本**：相比于计算开销极小的传统采集函数，调用 70B 规模的 LLM 进行每一轮迭代的推理成本非常高。
*   **提示词敏感性**：尽管使用了结构化模板，但 LLM 对提示词微小变动的敏感性（Prompt Sensitivity）依然是一个潜在的偏差风险。
*   **预算限制**：实验仅验证了小规模预算（30次采样）下的表现，在大规模搜索空间下的扩展性有待验证。

（完）
