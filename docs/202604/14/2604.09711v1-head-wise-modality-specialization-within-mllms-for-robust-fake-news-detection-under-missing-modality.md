---
title: Head-wise Modality Specialization within MLLMs for Robust Fake News Detection under Missing Modality
title_zh: 多模态大语言模型中用于模态缺失下鲁棒虚假新闻检测的按头模态专门化
authors: "Kai Qian, Weijie Shi, Jiaqi Wang, Mengze Li, Hao Chen, Yue Cui, Hanghui Guo, Ziyi Liu, Jia Zhu, Jiajie Xu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09711v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 使用多模态大模型进行虚假新闻检测
tldr: 针对多模态虚假新闻检测中常见的模态缺失问题，本文提出了一种基于多模态大语言模型（MLLM）的注意力头模态专门化方法。通过研究发现注意力头对不同模态具有专门化特性，研究者设计了头级专门化机制和单模态知识保留策略，在有限的单模态标注下增强了模型对低贡献模态的验证能力，显著提升了缺失模态场景下的检测鲁棒性。
selection_source: fresh_fetch
motivation: 现实中虚假新闻常面临模态缺失，而现有模型因低贡献模态学习不足及单模态标注稀缺，难以在缺失模态时保持稳健的检测能力。
method: 提出头级模态专门化机制，通过注意力约束将不同注意力头分配给特定模态，并结合单模态知识保留策略防止模型偏离已学习的单模态知识。
result: 实验证明该方法在模态缺失情况下显著提高了检测的鲁棒性，同时在全模态输入下依然保持了优异的性能。
conclusion: 通过显式引导MLLM内部注意力头的模态专门化，可以有效解决多模态虚假新闻检测中的模态缺失挑战。
---

## 摘要
多模态虚假新闻检测（MFND）旨在通过共同利用文本和视觉证据来验证新闻的可信度。然而，现实世界中的新闻传播常因图片删除、截图损坏等问题而面临模态缺失。因此，在这种场景下进行鲁棒检测需要保持对每种模态的强大验证能力，但由于低贡献模态的学习不足以及单模态标注的稀缺，这在 MFND 中极具挑战性。为解决这一问题，我们提出了多模态大语言模型（MLLMs）中的按头模态专门化（Head-wise Modality Specialization），以实现模态缺失下的鲁棒 MFND。具体而言，我们首先系统地研究了 MLLMs 中的注意力头及其与模态缺失下性能的关系，表明模态关键头通过其模态专门化，充当了单模态验证能力的关键载体。基于这一观察，为了更好地保留低贡献模态的验证能力，我们引入了一种按头专门化机制，将这些头显式地分配给不同的模态，并通过注意力下界约束来保持其专门化。此外，为了更好地利用稀缺的单模态标注，我们提出了一种单模态知识保留策略，防止这些头偏离从有限监督中学习到的单模态知识。实验表明，我们的方法在提高模态缺失下鲁棒性的同时，也保持了完整多模态输入下的性能。

## Abstract
Multimodal fake news detection (MFND) aims to verify news credibility by jointly exploiting textual and visual evidence. However, real-world news dissemination frequently suffers from missing modality due to deleted images, corrupted screenshots, and similar issues. Thus, robust detection in this scenario requires preserving strong verification ability for each modality, which is challenging in MFND due to insufficient learning of the low-contribution modality and scarce unimodal annotations. To address this issue, we propose Head-wise Modality Specialization within Multimodal Large Language Models (MLLMs) for robust MFND under missing modality. Specifically, we first systematically study attention heads in MLLMs and their relationship with performance under missing modality, showing that modality-critical heads serve as key carriers of unimodal verification ability through their modality specialization. Based on this observation, to better preserve verification ability for the low-contribution modality, we introduce a head-wise specialization mechanism that explicitly allocates these heads to different modalities and preserves their specialization through lower-bound attention constraints. Furthermore, to better exploit scarce unimodal annotations, we propose a Unimodal Knowledge Retention strategy that prevents these heads from drifting away from the unimodal knowledge learned from limited supervision. Experiments show that our method improves robustness under missing modality while preserving performance with full multimodal input.

---

## 论文详细总结（自动生成）

这篇论文针对多模态虚假新闻检测（MFND）在现实应用中面临的“模态缺失”问题，提出了一种基于多模态大语言模型（MLLM）内部注意力头专门化的改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态虚假新闻检测模型通常假设图文模态完整，但在实际传播中，图片被删或截图损坏导致的“模态缺失”非常普遍。
*   **研究动机**：
    *   **模态贡献不均**：模型往往过度依赖贡献度高的模态（如文本），导致对低贡献模态（如图像）的学习不足，一旦主导模态缺失，性能会剧烈下降。
    *   **单模态标注稀缺**：现有的虚假新闻数据集多为成对标注，缺乏单独针对文本或图像的真实性标签，难以训练出鲁棒的单模态验证能力。
    *   **现有方法局限**：传统的模态恢复或补全方法假设缺失模态可以通过剩余模态推断，但在虚假新闻中，去掉一个模态往往会彻底改变新闻的语义和验证逻辑。

### 2. 方法论
论文提出了**按头模态专门化（Head-wise Modality Specialization, HMS）**框架，核心思想是利用和保护 MLLM 内部已有的模态敏感注意力头。
*   **模态关键头识别**：首先在预训练模型上运行推理，计算每个注意力头对图像和文本 Token 的注意力分配比例，识别出专门处理图像或文本的“关键头”。
*   **按头模态专门化 (HMS)**：引入注意力下界约束（Lower-bound Attention Constraint）。在微调过程中，强制要求这些关键头对对应模态的注意力权重不低于阈值 $\tau$，防止多模态训练稀释了单模态的专门化路径。
*   **单模态知识保留 (UKR)**：提出一种梯度收缩策略。在阶段化训练中，如果当前是图像训练阶段，则收缩文本关键头的梯度更新（乘以因子 $\gamma$），反之亦然。这能防止稀缺的单模态知识被后续的多模态更新所覆盖。
*   **阶段化训练流程**：每个 Epoch 内依次进行图像专用、文本专用和多模态联合训练三个阶段。

### 3. 实验设计
*   **数据集**：
    *   **Weibo**：中等规模，文本贡献度远高于图像，用于测试对弱势模态的保护。
    *   **DGM4**：大规模多模态篡改数据集，具有完整的单模态标签，用于模拟不同比例（0.25%, 1%, 5%）的稀缺标注场景。
*   **Benchmark 与对比方法**：
    *   传统 MFND 方法：MTS, COOLANT。
    *   缺失模态专用方法：MoMKE。
    *   强基线：基于 Qwen3-VL-2B 的朴素全量微调（Qwen + SFT）。
*   **评估指标**：Macro F1（针对全模态、仅文本、仅图像三种推理场景）。

### 4. 资源与算力
*   **模型骨干**：Qwen3-VL-2B-Instruct。
*   **训练细节**：使用 LoRA 进行高效微调，Batch Size 为 24，训练 5 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到使用了 LoRA 这种参数高效微调技术，暗示了对算力需求相对适中。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的数据集上进行了测试，涵盖了从极低（0.25%）到相对较高（5%）的单模态监督预算。
*   **消融实验**：对比了“无关键头（随机选头）”、“无 HMS”、“无 UKR”等变体，验证了各组件的有效性。
*   **敏感性分析**：对注意力阈值 $\tau$ 和梯度收缩因子 $\gamma$ 进行了参数搜索。
*   **评价**：实验设计较为全面，对比了传统模型与最新的大模型基线，客观地展示了在提升鲁棒性的同时如何保持全模态性能。

### 6. 主要结论与发现
*   **专门化头的存在性**：预训练 MLLM 内部确实存在对模态高度敏感的注意力头，且这些头对单模态验证至关重要。
*   **训练的稀释效应**：普通的联合微调会使注意力头的专门化变得“弥散”，削弱单模态路径。
*   **鲁棒性提升**：该方法在 Weibo 数据集的图像缺失场景下，将 Macro F1 从 0.3187 提升至 0.6063，提升极其显著，同时未损害全模态下的表现。

### 7. 优点
*   **可解释性强**：基于对 MLLM 内部机理（注意力头）的深入分析提出方法，而非单纯的黑盒堆叠。
*   **轻量且高效**：通过约束项和梯度控制实现，不需要修改模型架构，也不需要复杂的模态生成/补全模块。
*   **解决实际痛点**：针对虚假新闻检测中“图文贡献极度不均”和“标注缺失”的痛点给出了有效方案。

### 8. 不足与局限
*   **模型依赖性**：实验主要基于 Qwen3-VL 架构，虽然理论通用，但在其他架构（如基于交叉注意力的模型）上的表现尚未验证。
*   **超参数敏感度**：虽然论文称不敏感，但 $\tau$ 和 $\gamma$ 的选择仍需针对不同数据集进行一定程度的调优。
*   **标注依赖**：虽然针对稀缺标注设计，但仍需要极少量的单模态真实性标注（如 Weibo 实验中手动标注了 100 条），在完全零单模态标注的情况下的表现未知。

（完）
