---
title: "ATSS: Detecting AI-Generated Videos via Anomalous Temporal Self-Similarity"
title_zh: ATSS：通过异常时序自相似性检测 AI 生成视频
authors: "Hang Wang, Chao Shen, Lei Zhang, Zhi-Qi Cheng"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04029v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 通过时域模式检测AI生成视频
tldr: 针对AI生成视频（AIGV）日益逼真带来的取证挑战，本文提出了一种基于异常时间自相似性（ATSS）的检测方法。研究发现AIGV受提示词驱动，在视觉和语义域表现出非自然的重复性关联。ATSS通过构建视觉、文本及跨模态相似性矩阵，并利用双向交叉注意力融合机制捕捉全局时间演化逻辑。实验证明该方法在多个大规模基准测试中显著优于现有技术，具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有检测方法多关注局部伪影或短期不一致性，难以捕捉AI生成视频中由确定性轨迹导致的全局时间演化异常。
method: 提出ATSS框架，通过提取帧级描述构建视觉、文本和跨模态相似性矩阵，并利用Transformer编码器和双向交叉注意力机制进行多模态融合检测。
result: 在GenVideo、EvalCrafter等四个大规模数据集上的实验表明，ATSS在AP、AUC和ACC等指标上均显著超越了现有最先进方法。
conclusion: 异常时间自相似性是AIGV的有效识别特征，结合多模态相似性分析能显著提升AI生成视频检测的准确性和泛化性。
---

## 摘要
AI 生成视频 (AIGV) 已达到前所未有的逼真度，对数字取证构成了严峻威胁。现有的 AIGV 检测器主要关注局部伪影或短期时序不一致性，因此往往无法捕捉主导全局时序演化的底层生成逻辑，限制了 AIGV 的检测性能。在本文中，我们识别出 AIGV 中一种独特的指纹，称为异常时序自相似性 (ATSS)。与表现出随机自然动态的真实视频不同，AIGV 遵循确定性的锚点驱动轨迹（例如文本或图像提示词），从而在视觉和语义领域引发了非自然的重复相关性。为了利用这一特性，我们提出了 ATSS 方法，这是一种多模态检测框架，通过三重相似性表示和交叉注意力融合机制来利用这一见解。具体而言，ATSS 通过利用逐帧描述来构建视觉、文本和跨模态相似性矩阵，从而重建语义轨迹，这些矩阵共同量化了内在的时序异常。这些矩阵由专门的 Transformer 编码器进行编码，并通过双向交叉注意力融合模块进行集成，以有效地建模模态内和模态间的动态。在 GenVideo、EvalCrafter、VideoPhy 和 VidProM 四个大规模基准测试上的广泛实验表明，ATSS 在 AP、AUC 和 ACC 指标上显著优于现有最先进的方法，并在多种视频生成模型中表现出卓越的泛化能力。ATSS 的代码和模型将在 https://github.com/hwang-cs-ime/ATSS 发布。

## Abstract
AI-generated videos (AIGVs) have achieved unprecedented photorealism, posing severe threats to digital forensics. Existing AIGV detectors focus mainly on localized artifacts or short-term temporal inconsistencies, thus often fail to capture the underlying generative logic governing global temporal evolution, limiting AIGV detection performance. In this paper, we identify a distinctive fingerprint in AIGVs, termed anomalous temporal self-similarity (ATSS). Unlike real videos that exhibit stochastic natural dynamics, AIGVs follow deterministic anchor-driven trajectories (e.g., text or image prompts), inducing unnaturally repetitive correlations across visual and semantic domains. To exploit this, we propose the ATSS method, a multimodal detection framework that exploits this insight via a triple-similarity representation and a cross-attentive fusion mechanism. Specifically, ATSS reconstructs semantic trajectories by leveraging frame-wise descriptions to construct visual, textual, and cross-modal similarity matrices, which jointly quantify the inherent temporal anomalies. These matrices are encoded by dedicated Transformer encoders and integrated via a bidirectional cross-attentive fusion module to effectively model intra- and inter-modal dynamics. Extensive experiments on four large-scale benchmarks, including GenVideo, EvalCrafter, VideoPhy, and VidProM, demonstrate that ATSS significantly outperforms state-of-the-art methods in terms of AP, AUC, and ACC metrics, exhibiting superior generalization across diverse video generation models. Code and models of ATSS will be released at https://github.com/hwang-cs-ime/ATSS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ATSS** 的新型 AI 生成视频（AIGV）检测框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着视频生成模型（如 Sora、Runway）的飞速发展，生成的视频在视觉上已极其逼真。现有的检测方法大多关注局部伪影（如闪烁）或短期不一致性，难以捕捉主导全局演化的生成逻辑，导致在面对高质量、长程一致的 AIGV 时表现不佳。
*   **研究动机**：作者发现 AIGV 存在一个独特的“指纹”——**异常时序自相似性（ATSS）**。真实视频的动态是随机且异构的，而 AIGV 遵循由提示词（Prompt）驱动的确定性轨迹，这导致视频在视觉和语义维度上表现出非自然的、高度重复的相关性（即“强迫对齐”现象）。

### 2. 提出的方法论
ATSS 是一个多模态检测框架，其核心流程如下：
*   **语义轨迹重建**：利用预训练的图像描述模型（BLIP-2）为视频的每一帧生成文本描述，将视觉演化转化为可量化的语义轨迹。
*   **三重相似性矩阵构建**：
    1.  **视觉自相似性矩阵 ($S^{(v)}$)**：计算帧与帧之间视觉特征的余弦相似度，捕捉像素级的重复性。
    2.  **文本自相似性矩阵 ($S^{(t)}$)**：计算帧描述之间的语义相似度，捕捉高层逻辑的冗余。
    3.  **跨模态相似性矩阵 ($S^{(c)}$)**：计算一帧的视觉特征与另一帧文本描述的相关性，量化生成过程中的语义锚点约束。
*   **时序建模与融合**：
    *   使用三个独立的 **Transformer 编码器** 分别处理这三类矩阵，提取模态内的时序动态。
    *   引入 **双向交叉注意力融合机制（Cross-Attentive Fusion）**：让视觉和文本特征相互增强，并共同精炼跨模态特征。
*   **分类预测**：将融合后的特征输入多层感知机（MLP），输出视频真伪的概率。

### 3. 实验设计
*   **数据集**：在四个大规模基准测试上进行了评估，涵盖了 40 个子集：
    *   **GenVideo**：包含 Pika、Sora、Gen2 等 10 种生成器。
    *   **EvalCrafter**：包含 14 种生成器。
    *   **VideoPhy**：侧重于物理常识评估的 10 种生成器。
    *   **VidProM**：包含 6 种基于真实用户提示词的生成器。
*   **对比方法**：对比了 13 种最先进（SOTA）的方法，包括：
    *   传统 Deepfake 检测器：STIL, FTCN, TALL, MINTIME 等。
    *   专用 AIGV 检测器：AIGVDet, DeMamba, DeCoF, NSG-VD, ReStraV, D3 等。
*   **评价指标**：平均精度（AP）、曲线下面积（AUC）和准确率（ACC）。

### 4. 资源与算力
*   **硬件**：使用了 **单张 NVIDIA L40S GPU**。
*   **训练细节**：总计训练 **200 个 epoch**。
*   **模型配置**：采样帧数 $T=8$；特征提取器（BLIP-2）在训练期间保持冻结，仅训练 Transformer 编码器和 MLP 头部。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集、40 个不同的视频生成模型子集上进行了测试，实验规模巨大。
*   **消融实验**：详细测试了视觉、文本、跨模态分支的各自贡献，以及不同图像描述模型（如 BLIP-base/large）和特征编码器（CLIP/X-CLIP）对结果的影响。
*   **客观性与公平性**：所有基线方法均使用官方代码或严格按论文实现，并在相同的训练/测试划分下进行对比，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：ATSS 在所有四个数据集上均显著优于现有方法。例如在 GenVideo 上，AP 达到 97.56%，比之前的 SOTA 方法 D3 提升了 5.4%。
*   **泛化能力强**：尽管只在 Pika 生成的视频上训练，但 ATSS 能有效识别从未见过的生成模型（如 Sora）产生的视频。
*   **语义的重要性**：消融实验证明，文本和跨模态相似性矩阵对于识别高质量 AIGV 至关重要，单纯依靠视觉特征（像素级）已不足以应对现代生成技术。

### 7. 优点与亮点
*   **视角独特**：从“生成逻辑”而非“局部伪影”入手，提出了“异常时序自相似性”这一极具辨识度的取证指纹。
*   **多模态融合**：巧妙地结合了图像描述技术，将难以捕捉的视觉动态转化为易于量化的文本相关性。
*   **即插即用**：框架对底层的特征提取器具有良好的鲁棒性，可以随基础模型的升级而进一步增强。

### 8. 不足与局限
*   **极端高质量视频的挑战**：在 Sora 和 StreamingT2V (ST2V) 等模型上的表现有所下降。这是因为这些模型通过分层精炼或更复杂的物理模拟，减少了确定性的重复模式，使其动态更接近真实视频的随机性。
*   **计算开销**：由于需要对每一帧生成文本描述，推理过程涉及大型多模态模型（BLIP-2），在实时检测场景下可能存在计算延迟。
*   **采样依赖**：性能可能受采样帧数的影响，对于极短或极长且动态极其复杂的视频，固定帧采样可能丢失关键信息。

（完）
