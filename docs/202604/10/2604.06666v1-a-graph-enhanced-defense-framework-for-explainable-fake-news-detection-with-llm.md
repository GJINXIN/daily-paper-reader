---
title: A Graph-Enhanced Defense Framework for Explainable Fake News Detection with LLM
title_zh: 一种基于大语言模型的可解释虚假新闻检测图增强防御框架
authors: "Bo Wang, Jing Ma, Hongzhan Lin, Zhiwei Yang, Ruichao Yang, Yuan Tian, Yi Chang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06666v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 基于LLM的可解释虚假新闻检测
tldr: 针对可解释虚假新闻检测中外部报告不可靠及突发新闻处理难的问题，本文提出G-Defense框架。该框架通过将新闻声明分解为子声明并构建依赖图，利用检索增强生成（RAG）获取证据并生成竞争性解释。通过基于图的防御式推理模块评估真实性，并利用大语言模型生成直观的解释图。实验证明该方法在检测准确率和解释质量上均达到领先水平。
selection_source: fresh_fetch
motivation: 现有的可解释虚假新闻检测方法效率低且难以应对突发新闻，且大语言模型依赖的外部报告可能存在不准确性。
method: 提出G-Defense框架，通过分解子声明构建依赖图，结合RAG技术获取证据，并利用防御式推理模块和LLM生成解释图。
result: 实验结果显示，G-Defense在真实性检测性能和解释生成质量方面均优于现有主流方法。
conclusion: 该研究证明了结合图结构建模与大语言模型推理能有效提升虚假新闻检测的可靠性与可解释性。
---

## 摘要
可解释虚假新闻检测旨在评估新闻声明的真实性，同时提供易于理解的解释。现有结合调查新闻学的方法通常效率低下，且难以应对突发新闻。大语言模型（LLMs）的最新进展使得利用外部检索报告作为检测和解释生成的证据成为可能，但未经证实的报告可能会引入不准确性。此外，有效的可解释虚假新闻检测应为声明的所有方面提供全面的解释，以协助公众验证其准确性。为了应对这些挑战，我们提出了一种图增强防御框架（G-Defense），该框架仅基于未经证实的报告提供细粒度的解释。具体而言，我们通过将新闻声明分解为若干子声明并建模其依赖关系，构建了一个以声明为中心的图。对于每个子声明，我们利用检索增强生成（RAG）技术检索显著证据并生成竞争性解释。随后，我们引入了一个基于该图的类防御推理模块来评估整体真实性。最后，我们提示大语言模型生成一个直观的解释图。实验结果表明，G-Defense 在真实性检测和解释质量方面均达到了最先进的性能。

## Abstract
Explainable fake news detection aims to assess the veracity of news claims while providing human-friendly explanations. Existing methods incorporating investigative journalism are often inefficient and struggle with breaking news. Recent advances in large language models (LLMs) enable leveraging externally retrieved reports as evidence for detection and explanation generation, but unverified reports may introduce inaccuracies. Moreover, effective explainable fake news detection should provide a comprehensible explanation for all aspects of a claim to assist the public in verifying its accuracy. To address these challenges, we propose a graph-enhanced defense framework (G-Defense) that provides fine-grained explanations based solely on unverified reports. Specifically, we construct a claim-centered graph by decomposing the news claim into several sub-claims and modeling their dependency relationships. For each sub-claim, we use the retrieval-augmented generation (RAG) technique to retrieve salient evidence and generate competing explanations. We then introduce a defense-like inference module based on the graph to assess the overall veracity. Finally, we prompt an LLM to generate an intuitive explanation graph. Experimental results demonstrate that G-Defense achieves state-of-the-art performance in both veracity detection and the quality of its explanations.

---

## 论文详细总结（自动生成）

### 论文总结：基于大语言模型的可解释虚假新闻检测图增强防御框架 (G-Defense)

#### 1. 核心问题与整体含义
*   **研究动机**：虚假新闻检测不仅需要准确的判定，还需要人类可理解的解释。现有方法存在三大痛点：
    1.  **时效性差**：依赖专业事实核查报告，导致在突发新闻传播初期无法及时响应。
    2.  **证据不可靠**：直接利用互联网未经证实的原始报告作为证据，容易引入噪声和偏见。
    3.  **细粒度缺失**：复杂的新闻声明往往包含多个事实维度，现有模型通常将其视为整体，缺乏对内部逻辑结构的深入分析。
*   **核心目标**：在仅有未经证实报告的现实场景下，通过大语言模型（LLM）和图结构建模，实现对复杂声明的细粒度真实性检测与直观解释。

#### 2. 方法论
G-Defense 框架由四个核心模块组成：
*   **图构建模块**：利用 LLM 将复杂声明分解为多个原子级的“子声明”（Sub-claims），并识别子声明之间以及子声明与主声明之间的逻辑依赖关系，构建“以声明为中心”的有向图。
*   **竞争性解释生成 (RAG)**：针对每个子声明，利用检索增强生成技术从原始报告库中检索 Top-k 相关句子。随后，提示 LLM 分别生成“支持”和“反驳”该子声明的两份竞争性解释。其核心假设是：符合事实的一方，其解释在逻辑和证据上会更稳健。
*   **类防御推理模块**：将图结构序列化为文本，连同所有子声明及其竞争解释输入到微调后的 LLM Encoder（如 Flan-T5-large）。模型通过对比两方解释的质量并结合图中的逻辑依赖，预测最终的真实性标签。
*   **解释总结模块**：根据预测结果，过滤掉与事实不符的解释，生成最终的“解释图”和一段精炼的文本摘要，展示完整的推理路径。

#### 3. 实验设计
*   **数据集**：
    *   **RAWFC**：包含 2,012 条声明，分为“真、半真、假”三类。
    *   **LIAR-RAW**：包含 12,590 条声明，分为六个细粒度等级。
    *   *特点*：这两个数据集均使用核查结论发布前的原始报告，模拟真实的早期检测环境。
*   **对比方法 (Benchmarks)**：
    *   **传统模型**：dEFEND, SBERT-FC, CofCED（前 SOTA）等。
    *   **LLM 相关方法**：GPT-3.5 (Zero-shot), FactLLaMA (LoRA 微调), L-Defense (该团队的前作)。
*   **评估指标**：宏平均 F1 值（macF1）、精确率（P）、召回率（R），以及针对解释质量的 LLM 评分和人工评分。

#### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A40 GPU** 上完成。
*   **模型配置**：
    *   生成任务使用 **GPT-3.5-turbo-0125** API。
    *   推理分类器使用 **Flan-T5-large**（约 7.8 亿参数）。
*   **训练细节**：在 RAWFC 上训练 5 个 epoch（学习率 7e-5），在 LIAR-RAW 上训练 5 个 epoch（学习率 5e-5）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主对比实验、消融实验（验证子声明、边、证据、竞争机制的必要性）。
    *   针对声明复杂度（子声明数量）进行了分层性能分析。
    *   开展了 LLM 自动评估（基于 GPT-3.5/4）和 5 名人工标注者的主观评估。
*   **充分性与客观性**：实验覆盖了从 3 分类到 6 分类的不同难度场景，并提供了统计显著性检验（p-test）。通过人工评估验证了 LLM 自动评分的可靠性，实验设计严谨、客观。

#### 6. 主要结论与发现
*   **性能领先**：G-Defense 在两个数据集上均刷新了 SOTA 记录，尤其在 RAWFC 上 macF1 提升了约 3.1%。
*   **分解的价值**：声明分解对复杂声明（子声明数 $\ge 3$）的提升显著，证明了细粒度分析的必要性。
*   **防御机制有效**：竞争性解释能有效暴露证据不足的一方，LLM 在缺乏证据时生成的“谎言解释”质量明显较低，这成为了模型判定的重要信号。
*   **解释直观性**：人工评估显示，G-Defense 生成的解释图能显著降低人类理解成本，并提高人类识别虚假新闻的准确率。

#### 7. 优点
*   **逻辑建模深入**：通过图结构捕捉子声明间的依赖，比单纯的文本拼接更符合人类专家的核查逻辑。
*   **鲁棒性强**：竞争性解释机制利用了 LLM 的推理特性，缓解了原始报告中噪声数据带来的干扰。
*   **可解释性极佳**：输出结果不仅有结论，还有结构化的推理路径，透明度高。

#### 8. 不足与局限
*   **推理延迟**：由于涉及多轮 LLM API 调用（分解、生成、总结），端到端推理时间较长，难以满足极高并发的实时过滤需求。
*   **API 依赖与成本**：框架核心依赖闭源 LLM API，存在一定的经济成本和隐私风险（尽管文中尝试了 LLaMA3.1 等开源替代方案）。
*   **模态单一**：目前仅支持文本分析，尚未扩展到包含图片、视频等多模态信息的虚假新闻检测。
*   **错误传播风险**：若第一步“声明分解”出现严重偏差，可能会误导后续的证据检索和最终判定。

（完）
