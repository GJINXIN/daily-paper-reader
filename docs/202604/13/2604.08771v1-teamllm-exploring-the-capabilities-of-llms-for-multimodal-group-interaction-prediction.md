---
title: "TeamLLM: Exploring the Capabilities of LLMs for Multimodal Group Interaction Prediction"
title_zh: TeamLLM：探索大语言模型在多模态群体交互预测中的能力
authors: "Diana Romero, Xin Gao, Daniel Khalkhali, Salma Elmalaki"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08771v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 协作场景模拟与群体交互
tldr: 本研究探讨了大语言模型（LLMs）在预测多模态群体交互行为中的潜力。通过将混合现实（MR）环境下的个体行为、群体结构和时序上下文编码为自然语言，研究评估了零样本、少样本和微调三种模式。结果显示，LLMs在对话预测等语言相关行为上显著优于传统LSTM模型，但在空间视觉推理方面存在局限。该研究为利用LLM进行团队动力学感知提供了重要基准和设计准则。
selection_source: fresh_fetch
motivation: 旨在探索LLMs是否能有效处理多模态传感器数据，以预测协作环境中的复杂群体协调模式和团队动力学。
method: 将个体行为、群体结构和时序上下文转化为自然语言描述，并对比评估了LLM在零样本、少样本及监督微调下的预测表现。
result: "微调后的LLM在对话预测上达到96%的准确率，比LSTM基准提升3.2倍，且保持了极低的推理延迟。"
conclusion: LLMs擅长处理具有语言逻辑的交互预测，但在缺乏空间推理能力的情况下难以处理共同注意力等视觉任务，且在模拟模式下存在级联错误风险。
---

## 摘要
预测群体行为（即个体在协作任务中如何协调、沟通和互动）对于设计能够通过实时预测和协作场景的逼真模拟来支持团队表现的系统至关重要。大语言模型（LLMs）在处理人类活动识别（HAR）的传感器数据方面已展现出潜力，但其在团队动力学或群体级多模态感知方面的能力仍有待探索。本文研究了 LLMs 是否能够根据协作混合现实（MR）环境中的多模态传感器数据预测群体协调模式。我们将层级上下文——包括个体行为概况、群体结构属性和时间活动上下文——编码为自然语言，并评估了三种 LLM 适配范式（零样本、少样本和有监督微调）与统计基准模型的对比。我们对 16 个小组（64 名参与者，约 25 小时的传感器数据）进行的评估表明，在基于语言的行为预测方面，LLMs 相比 LSTM 基准模型实现了 3.2 倍的提升，其中微调模型在对话预测方面的准确率达到 96%，同时保持了低于 35 毫秒的延迟。除了性能提升外，我们还刻画了基于文本的 LLMs 在多模态感知中的边界：对话预测之所以成功，是因为轮次转换映射到了语言模式，而共享或共同注意力可能需要文本型 LLMs 无法捕捉的空间和视觉推理。我们进一步识别了模拟模式的脆弱性（由于级联上下文错误导致性能下降 83%），以及少样本学习对示例选择策略的敏感性极低。这些发现为 LLMs 何时适用于团队动力学的 CPS/IoT 感知建立了准则，并为未来多模态基础模型的设计提供了参考。

## Abstract
Predicting group behavior, how individuals coordinate, communicate, and interact during collaborative tasks, is essential for designing systems that can support team performance through real-time prediction and realistic simulation of collaborative scenarios. Large Language Models (LLMs) have shown promise for processing sensor data for human-activity recognition (HAR), yet their capabilities for team dynamics or group-level multimodal sensing remain unexplored. This paper investigates whether LLMs can predict group coordination patterns from multimodal sensor data in collaborative Mixed Reality (MR) environments. We encode hierarchical context -- individual behavioral profiles, group structural properties, and temporal activity context -- as natural language and evaluate three LLM adaptation paradigms (zero-shot, few-shot, and supervised fine-tuning) against statistical baselines. Our evaluation on 16 groups (64 participants, $\sim$25 hours of sensor data) reveals that LLMs achieve 3.2$\times$ improvement over LSTM baselines for linguistically-grounded behaviors, with fine-tuning reaching 96\% accuracy for conversation prediction while maintaining sub-35ms latency. Beyond performance gains, we characterize the boundaries of text-based LLMs for multimodal sensing conversation prediction succeeds because turn-taking maps to linguistic patterns, while shared or joint attention may require spatial and visual reasoning that text only LLMs cannot capture. We further identify simulation mode brittleness (83\% degradation from cascading context errors) and minimal few-shot sensitivity to example selection strategy. These findings establish guidelines when LLMs are appropriate for CPS/IoT sensing for team dynamics and inform the design of future multimodal foundation models.

---

## 论文详细总结（自动生成）

### TeamLLM：探索大语言模型在多模态群体交互预测中的能力

#### 1. 核心问题与整体含义
论文探讨了如何利用大语言模型（LLMs）预测协作环境中的**群体行为（Group Behavior）**。研究背景在于，传统的统计模型（如 LSTM）在处理复杂的群体动力学（如成员间的协调、沟通和互动）时，往往难以捕捉深层的语义上下文。论文旨在验证 LLMs 是否能通过理解多模态传感器数据（如视线、音频、位置），预测群体在混合现实（MR）任务中的协调模式，并为未来设计实时团队支持系统提供理论依据。

#### 2. 方法论
核心思想是将多模态传感器数据转化为**层级化的自然语言上下文**，利用 LLM 的语义推理能力进行预测。
*   **层级上下文编码**：
    *   **个体行为概况 ($C_{indiv}$)**：通过聚类分析识别个体的行为特征（如“频繁发言者”、“高视线活跃度”）。
    *   **群体结构属性 ($C_{group}$)**：计算网络指标（如密度、互惠性、中心性），描述群体互动的结构。
    *   **时序活动上下文 ($C_{temp}$)**：识别协作阶段（如“活跃讨论”、“达成共识”）及历史交互趋势。
*   **预测目标**：预测下一时段的**社会图谱（Sociogram）**，包括对话、空间邻近和共同注意力三种交互矩阵。
*   **适配范式**：评估了**零样本（Zero-shot）**、**少样本（Few-shot）**和**监督微调（SFT, 使用 LoRA）**三种模式。

#### 3. 实验设计
*   **数据集与场景**：招募 64 名参与者组成 16 个小组，在 MR 环境中执行图像分类协作任务，累计约 25 小时传感器数据。
*   **基准模型（Benchmark）**：
    *   **Persistence**：简单重复上一时刻状态。
    *   **LSTM**：使用 2048 单元的双向 LSTM，输入相同的多模态特征。
*   **对比方法**：对比了不同上下文组合（仅个体、群体+历史、全上下文）以及不同的少样本选择策略（随机、相位相似、多样性采样）。
*   **评估模式**：分为**单步预测（Single-Step）**（使用真实上下文）和**模拟模式（Simulation）**（使用模型自身生成的预测作为后续上下文）。

#### 4. 资源与算力
*   **硬件**：实验在消费级显卡 **NVIDIA RTX 4060 Ti** 上运行。
*   **性能指标**：推理延迟（TTFT）稳定在 **35ms 以下**，总推理时间随上下文增加呈亚线性增长。
*   **微调细节**：使用 LoRA 适配器（r=16, α=32），训练参数量约 250 万（占总参数 0.10%），训练 3 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模**：包含 447 个有效时间窗口，涵盖了从数据采集到模型训练、测试的全流程。
*   **充分性**：研究设计了详尽的消融实验来验证不同上下文层级的作用，并对比了多种少样本策略。
*   **客观性**：引入了“社会图谱相似度（Weighted Jaccard）”作为核心指标，而非简单的元素级准确率，有效避免了因数据类别不平衡（如对话状态高度自相关）导致的评估偏差。

#### 6. 主要结论与发现
*   **性能突破**：在对话预测上，微调后的 LLM 达到 96% 的相似度，比 LSTM 基准提升了 **3.2 倍**，成功突破了统计模型的“上下文瓶颈”。
*   **模态差异**：LLM 在**对话预测**（语言逻辑强）上表现极佳，但在**共同注意力**预测上完全失败（0% 召回率），说明纯文本 LLM 缺乏处理 3D 空间坐标和视觉语义的能力。
*   **模拟脆弱性**：在自回归模拟模式下，LLM 表现出严重的级联错误，性能下降达 83%-99%，说明其对自身生成的错误上下文非常敏感。
*   **少样本敏感度低**：随机选择示例与复杂的检索策略效果相近（仅差 0.5%），降低了实际部署的复杂度。

#### 7. 优点
*   **跨学科结合**：将团队科学（Team Science）中的 IPO 模型和共享心理模型（SMM）转化为 Prompt 工程的结构，具有很强的理论支撑。
*   **高效性**：证明了在消费级硬件上实现低延迟群体行为预测的可行性。
*   **边界清晰**：客观地指出了文本 LLM 在空间推理方面的局限性，为未来多模态基础模型指明了方向。

#### 8. 不足与局限
*   **空间推理缺失**：由于模型仅处理文本，无法理解复杂的 3D 几何关系，导致视线追踪等任务失效。
*   **错误累积**：在长期模拟任务中稳定性不足，难以进行长程行为仿真。
*   **任务单一性**：实验仅基于“图像排序”这一特定协作任务，其结论在其他类型协作（如体力劳动或高度动态任务）中的泛化性有待验证。

（完）
