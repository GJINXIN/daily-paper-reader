---
title: "SPASM: Stable Persona-driven Agent Simulation for Multi-turn Dialogue Generation"
title_zh: SPASM：用于多轮对话生成的稳定人格驱动智能体模拟
authors: "Han Luo, Guy Laban"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09212v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 基于大语言模型的多轮对话智能体模拟
tldr: 针对大语言模型在多轮对话生成中容易出现的角色漂移、身份混淆和“回声”效应（模仿对方）等稳定性问题，本文提出了SPASM框架。该框架通过模块化的角色创建、对话生成和终止检测流程，并引入了“自我中心上下文投影（ECP）”技术，将对话历史以视角无关的方式存储并动态投影到各代理的视角中。实验表明，SPASM在多种模型上显著提升了长程对话的稳定性，减少了角色漂移并消除了回声现象。
selection_source: fresh_fetch
motivation: 解决LLM在多轮对话模拟中因角色漂移和相互模仿（回声效应）导致的身份一致性失效问题。
method: 提出SPASM框架，包含模块化角色构建流程，并核心采用自我中心上下文投影（ECP）技术来重构对话历史。
result: 在4.5万场对话的测试中，ECP显著降低了角色漂移，并在人工验证下消除了回声效应，保持了强交互几何结构。
conclusion: SPASM为生成高质量、高稳定性的多轮合成对话数据提供了一种无需微调模型的有效方案。
---

## 摘要
大语言模型越来越多地被部署在辅导、支持和咨询等多轮对话场景中，在这些场景下，可靠性取决于在长时程中保持一致的角色、人格和目标。当 LLM 被用于生成合成对话以进行训练和评估时，这一要求变得至关重要，因为 LLM 与 LLM 之间的对话可能会累积与身份相关的失效，例如人格漂移（persona drift）、角色混淆以及“回声”（echoing）现象（即一个智能体逐渐模仿其对话伙伴）。我们提出了 SPASM（用于多轮对话生成的稳定人格驱动智能体模拟），这是一个模块化的、稳定性优先的框架，它将模拟分解为：(i) 通过模式采样、合理性验证和自然语言人格构建进行人格创建；(ii) 客户-响应者（Client--Responder）对话生成；以及 (iii) 用于连贯停止的终止检测。为了在不改变模型权重的情况下提高长时程稳定性，我们提出了自我中心上下文投影（Egocentric Context Projection, ECP）：对话历史以视角无关的表示形式存储，并在生成前确定性地投影到每个智能体的自我中心视角中。通过三种 LLM 主干模型（GPT-4o-mini、DeepSeek-V3.2、Qwen-Plus）和九种客户-响应者配对，我们构建了一个包含 4,500 个人格和 45,000 场对话的数据集（每种配对包含 500 个人格 × 10 场对话）。消融实验表明，ECP 显著减少了人格漂移，并在人工验证下消除了回声现象；嵌入分析恢复了人格结构，并揭示了强烈的响应者驱动的交互几何特征。我们的代码可在 https://github.com/lhannnn/SPASM 获取。

## Abstract
Large language models are increasingly deployed in multi-turn settings such as tutoring, support, and counseling, where reliability depends on preserving consistent roles, personas, and goals across long horizons. This requirement becomes critical when LLMs are used to generate synthetic dialogues for training and evaluation, since LLM--LLM conversations can accumulate identity-related failures such as persona drift, role confusion, and "echoing", where one agent gradually mirrors its partner. We introduce SPASM (Stable Persona-driven Agent Simulation for Multi-turn dialogue generation), a modular, stability-first framework that decomposes simulation into (i) persona creation via schema sampling, plausibility validation, and natural-language persona crafting, (ii) Client--Responder dialogue generation, and (iii) termination detection for coherent stopping. To improve long-horizon stability without changing model weights, we propose Egocentric Context Projection (ECP): dialogue history is stored in a perspective-agnostic representation and deterministically projected into each agent's egocentric view before generation. Across three LLM backbones (GPT-4o-mini, DeepSeek-V3.2, Qwen-Plus) and nine Client--Responder pairings, we construct a dataset of 4,500 personas and 45,000 conversations (500 personas X 10 conversations per pairing). Ablations show ECP substantially reduces persona drift and, under human validation, eliminates echoing; embedding analyses recover persona structure and reveal strong responder-driven interaction geometry. Our code is available at https://github.com/lhannnn/SPASM.

---

## 论文详细总结（自动生成）

### 论文总结：SPASM —— 用于多轮对话生成的稳定人格驱动智能体模拟

#### 1. 核心问题与研究动机
随着大语言模型（LLM）在教育、医疗、心理咨询等长程多轮对话场景中的广泛应用，利用“智能体对战”（LLM-LLM）生成高质量合成对话数据已成为主流。然而，现有的模拟框架在长对话中存在严重的稳定性问题：
*   **人格漂移（Persona Drift）**：智能体逐渐偏离预设的人格特征或目标。
*   **角色混淆（Role Confusion）**：智能体分不清自己是“用户”还是“助手”。
*   **回声效应（Echoing）**：智能体开始模仿对方的语言风格、立场甚至身份（如客户开始像咨询师一样提供建议）。
这些失效模式导致生成的合成数据质量下降，无法真实反映人类交互。

#### 2. 方法论：SPASM 框架与 ECP 技术
论文提出了 **SPASM** 框架，其核心思想是将模拟过程模块化，并引入一种全新的上下文管理机制：

*   **模块化流程**：
    1.  **人格模式采样（Schema Sampling）**：从人口统计学、心理状态、交互模式等维度随机采样。
    2.  **人格验证器（Persona Validator）**：确保采样组合的逻辑合理性（如：18岁学生不应咨询退休金计划）。
    3.  **人格构建器（Persona Crafter）**：将结构化属性转化为自然语言描述。
    4.  **对话模拟（Dialogue Simulation）**：由“客户（Client）”和“响应者（Responder）”进行交互。
    5.  **终止检测器（Termination Detector）**：基于滑动窗口检测对话是否自然结束。

*   **核心技术：自我中心上下文投影（Egocentric Context Projection, ECP）**：
    *   **原理**：不再简单地将对话历史拼接为静态文本，而是将历史存储为“视角无关”的形式（记录绝对发言者身份）。
    *   **投影**：在生成每一轮回复前，根据当前智能体的身份，将历史动态投影为“自我（SELF）”与“伙伴（PARTNER）”的相对视角。
    *   **公式化表达**：定义投影算子 $\Psi_i$，将绝对身份 $s_k$ 映射为相对角色描述符 $Q = \{SELF, PARTNER\}$。这种视角归一化消除了角色歧义，防止模型误将对方的回复当作自己的续写目标。

#### 3. 实验设计
*   **模型主干**：采用了 GPT-4o-mini、DeepSeek-V3.2 和 Qwen-Plus 三种模型，构建了 3×3 的配对矩阵（共 9 种组合）。
*   **数据集规模**：生成了 4,500 个人格，总计 45,000 场对话（每种配对 500 个人格 × 10 场对话）。
*   **评估基准（Benchmark）**：
    *   **漂移测量**：通过定期向智能体投放“人格探测问题”（Probe Questions），计算其回复与初始基准回复的嵌入向量距离。
    *   **回声检测**：结合 LLM 自动评测与严格的人工验证（Human Validation）。
    *   **语义分析**：使用 Silhouette 分数、DBI 指数和 UMAP 可视化分析人格在嵌入空间中的聚类情况。
*   **对比方法**：主要对比了标准的上下文拼接方法（CONCAT）。

#### 4. 资源与算力
*   论文主要基于主流 LLM 的 API（OpenAI, DeepSeek, Alibaba Cloud）进行实验。
*   **算力说明**：文中未明确列出具体的 GPU 型号或训练时长，因为该方法属于 **In-context（上下文内）** 方案，不需要对模型进行微调（Weight-free），其算力消耗主要体现在 API 调用产生的推理开销上。

#### 5. 实验数量与充分性
*   **实验规模**：4.5 万场对话的样本量在同类研究中属于大规模，覆盖了 9 种不同的模型配对，具有很强的代表性。
*   **消融实验**：针对 ECP 机制进行了详尽的消融实验，对比了不同维度（关注点、情绪、动机）下的漂移情况。
*   **客观性**：引入了双盲人工标注来验证“回声效应”，并计算了标注者间的一致性（Cohen’s κ），确保了评估的客观与公平。

#### 6. 主要结论与发现
*   **ECP 的有效性**：ECP 显著降低了长程对话中的人格漂移，并在人工验证下将“回声效应”降至接近 0%。
*   **响应者主导效应**：实验发现，对话的几何结构（聚类质量）主要受“响应者（Responder）”模型的影响。如果响应者模型较强，即使客户模型较弱，也能维持较好的人格一致性。
*   **语义可恢复性**：通过检索实验证明，即使在长对话后，通过 Client 的发言仍能以高准确率（Top-1 接近 90% 以上）检索回其原始人格标签。

#### 7. 优点
*   **稳定性优先**：针对 LLM 模拟中长期存在的“身份坍塌”问题提供了轻量级且极其有效的解决方案。
*   **无需微调**：通过改变上下文投影方式即可实现，易于迁移到任何现有的 LLM 架构中。
*   **模块化设计**：人格验证和终止检测模块提升了合成数据的逻辑严密性和自然度。

#### 8. 不足与局限
*   **语言局限**：实验主要集中在英语环境和指令微调模型上，未测试在多语言或基础模型（Base Model）上的表现。
*   **角色限制**：目前仅限于双人（Client-Responder）交互，未探讨多智能体群聊或动态角色切换的复杂场景。
*   **主观性风险**：尽管有标注协议，但“回声效应”和“人格一致性”的判定仍带有一定的人类主观色彩。

（完）
