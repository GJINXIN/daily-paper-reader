---
title: Propagation Structure-Semantic Transfer Learning for Robust Fake News Detection
title_zh: 面向鲁棒虚假新闻检测的传播结构-语义迁移学习
authors: "Mengyang Chen, Lingwei Wei, Han Cao, Wei Zhou, Zhou Yan, Songlin Hu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23974v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 专注于利用社交媒体上的传播结构和语义特征进行鲁棒的虚假新闻检测。
tldr: 针对社交媒体虚假新闻检测中存在的语义和结构噪声干扰问题，本文提出了一种名为PSS-TL的鲁棒检测框架。该框架采用教师-学生架构，通过双教师模型分别独立学习新闻内容语义和传播结构知识，并利用多通道知识蒸馏（MKD）损失将专业知识迁移至学生模型，有效避免了噪声间的相互干扰。在真实数据集上的实验证明了该方法在提升检测性能和鲁棒性方面的优越性。
selection_source: fresh_fetch
motivation: 现有虚假新闻检测方法在处理非规范语言和不可靠用户交互产生的语义及结构噪声时，容易受到噪声间的相互干扰，导致检测鲁棒性不足。
method: 提出PSS-TL框架，利用双教师模型独立提取语义和结构特征，并通过多通道知识蒸馏机制将纯净知识迁移给学生模型。
result: 在两个真实世界数据集上的广泛实验表明，该方法在虚假新闻检测的准确性和抗噪鲁棒性上均优于现有主流模型。
conclusion: 通过解耦语义与结构知识的学习过程并结合知识蒸馏，可以有效缓解社交媒体噪声对虚假新闻检测的影响。
---

## 摘要
虚假新闻通常是指为了欺骗他人而故意传播的虚假信息，具有有害的社会影响。现有的虚假新闻检测方法主要从新闻内容中学习语义特征，或整合来自传播过程的结构特征。然而，在实际场景中，由于非正式语言的语义歧义以及社交媒体上不可靠的用户交互行为，新闻内容和传播过程中存在固有的语义和结构噪声。尽管最近的一些工作通过混合建模的方式考虑了无关用户交互的影响，但它们仍然受到结构噪声和语义噪声之间相互干扰的影响，导致鲁棒检测的性能有限。为了缓解这一问题，本文提出了一种在教师-学生架构下的新型传播结构-语义迁移学习框架（PSS-TL），用于鲁棒的虚假新闻检测。具体而言，我们设计了双教师模型，分别从含噪的新闻内容和传播结构中独立学习语义知识和结构知识。此外，我们设计了一种多通道知识蒸馏（MKD）损失，使学生模型能够从教师模型中获取专门知识，从而避免相互干扰。在两个真实世界数据集上的广泛实验验证了我们方法的有效性和鲁棒性。

## Abstract
Fake news generally refers to false information that is spread deliberately to deceive people, which has detrimental social effects. Existing fake news detection methods primarily learn the semantic features from news content or integrate structural features from propagation. However, in practical scenarios, due to the semantic ambiguity of informal language and unreliable user interactive behaviors on social media, there are inherent semantic and structural noises in news content and propagation. Although some recent works consider the effect of irrelevant user interactions in a hybrid-modeling way, they still suffer from the mutual interference between structural noise and semantic noise, leading to limited performance for robust detection. To alleviate this issue, this paper proposes a novel Propagation Structure-Semantic Transfer Learning framework (PSS-TL) for robust fake news detection under a teacher-student architecture. Specifically, we design dual teacher models to learn semantics knowledge and structure knowledge from noisy news content and propagation structure independently. Besides, we design a Multi-channel Knowledge Distillation (MKD) loss to enable the student model to acquire specialized knowledge from the teacher models, thereby avoiding mutual interference. Extensive experiments on two real-world datasets validate the effectiveness and robustness of our method.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PSS-TL**（Propagation Structure-Semantic Transfer Learning）的框架，旨在解决社交媒体虚假新闻检测中由于语义和结构噪声导致的检测性能下降问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在社交媒体环境下，虚假新闻检测面临双重噪声：
    1.  **语义噪声**：非正式语言、拼写错误、乱码等。
    2.  **结构噪声**：恶意用户/社交机器人的不可靠交互、评论被删、传播数据采集不全等。
*   **研究动机**：现有的混合建模方法（同时处理文本和传播图）存在**相互干扰**问题。语义噪声会干扰结构权重的计算，而错误的结构关系又会污染特征学习过程，导致误差累积。因此，需要一种能够解耦学习并抑制噪声干扰的鲁棒检测机制。

### 2. 方法论
PSS-TL 采用了**教师-学生（Teacher-Student）架构**，核心思想是通过双教师独立学习纯净知识，再迁移给学生模型。
*   **双教师模型（Dual Teacher Models）**：
    *   **内容教师（Content Teacher）**：利用多层感知机（MLP）从新闻文本中独立学习语义特征，避免受传播结构噪声的影响。
    *   **传播教师（Propagation Teacher）**：基于用户重合度构建全局传播图，并引入**结构修正机制**（根据节点度数调整边权重）。使用位置编码（Positional Encoding）代替文本特征作为输入，从而完全隔离语义噪声。
*   **学生模型（Student Model）**：
    *   设计了**局部-全局传播交互模块（LGPI）**。它首先捕捉单条新闻的局部传播特征，然后在全局图中传播这些特征，以整合新闻间的上下文关系。
*   **多通道知识蒸馏（MKD）损失**：
    *   **教师监督损失（Lsup）**：利用 KL 散度对齐教师和学生模型的预测输出（Logits）。
    *   **目标引导损失（Ltar）**：通过对比学习的思想，对齐教师和学生模型在隐藏层表示上的空间分布，增强知识迁移效率。

### 3. 实验设计
*   **数据集**：使用了两个经典的真实世界数据集 **PolitiFact** 和 **GossipCop**。此外，为了验证跨领域泛化性，引入了 **COAID**（新冠肺炎相关）作为目标域。
*   **对比方法（Benchmarks）**：
    *   基础图神经网络：GCN, GAT, GraphSAGE。
    *   先进传播模型：Bi-GCN, EBGCN（贝叶斯图网络）, UPSR（结构重建）, UPFD（用户偏好感知）, DECOR（度数修正）。
*   **评估指标**：准确率（Accuracy）和 Macro-F1 分数。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA Tesla V100 GPU** 上完成。
*   **软件环境**：PyTorch 1.12.1, PyTorch Geometric 2.3.1。
*   **模型细节**：文本编码使用预训练的 `bert-base-uncased`；隐藏层维度设为 64。
*   **训练时长**：文中未明确提及具体的训练总时长，但给出了学习率设置（教师模型 5e-4/5e-5，学生模型 5e-4）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在两个数据集上配合三种不同的 GNN 后端（GCN, GAT, GraphSAGE）进行了对比。
    *   **消融实验**：分别移除了内容教师、传播教师、两种蒸馏损失及 LGPI 模块，验证了各组件的必要性。
    *   **鲁棒性实验**：在不同比例（0.1 到 0.9）的语义噪声、结构噪声及混合噪声下进行了压力测试。
    *   **泛化性实验**：进行了跨领域（如 PolitiFact -> COAID）的迁移测试。
*   **评价**：实验设计非常充分，涵盖了性能、鲁棒性、泛化性和参数敏感性分析，且每组实验运行五次取平均值，结果具有客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：PSS-TL 在所有数据集上均显著优于现有 SOTA 模型，尤其是在 PolitiFact 数据集上提升明显。
*   **抗噪能力强**：在混合噪声比例高达 0.5 的极端情况下，PSS-TL 的性能下降幅度远小于其他模型，证明了解耦学习能有效抑制噪声干扰。
*   **泛化性优异**：在跨领域检测任务中，PSS-TL 表现出比 DECOR 等模型更强的适应能力。

### 7. 优点
*   **架构创新**：通过双教师架构巧妙地实现了语义与结构的解耦，从根本上解决了噪声相互干扰的问题。
*   **知识蒸馏设计**：MKD 损失不仅关注输出对齐，还通过目标引导损失关注特征空间的对齐，使学生模型学到的特征更纯净。
*   **结构修正**：传播教师模型中的结构修正机制能有效识别并强化可靠的社交关系。

### 8. 不足与局限
*   **全局图构建成本**：构建基于全局用户重合度的传播图在处理超大规模社交网络（如千万级用户）时，可能会面临内存和计算开销过大的挑战。
*   **数据集偏差**：虽然使用了真实数据集，但 PolitiFact 的样本量相对较小（仅 314 条新闻），可能存在一定的过拟合风险。
*   **实时性限制**：由于依赖全局传播图和教师-学生多阶段训练，该方法在处理瞬时爆发的新闻（缺乏足够传播链）时，检测效果可能会受到限制。

（完）
