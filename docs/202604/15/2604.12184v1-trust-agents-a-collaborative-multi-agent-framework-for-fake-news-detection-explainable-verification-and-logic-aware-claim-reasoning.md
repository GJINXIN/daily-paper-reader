---
title: "TRUST Agents: A Collaborative Multi-Agent Framework for Fake News Detection, Explainable Verification, and Logic-Aware Claim Reasoning"
title_zh: TRUST Agents：一种用于虚假新闻检测、可解释验证和逻辑感知断言推理的协作式多智能体框架
authors: "Gautama Shastry Bulusu Venkata, Santhosh Kakarla, Maheedhar Omtri Mohan, Aishwarya Gaddam"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12184v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 用于虚假新闻检测的多智能体框架
tldr: TRUST Agents是一个多智能体协作框架，旨在提升虚假新闻检测的可解释性。它通过声明提取、混合检索、置信度校准验证及报告生成四个核心步骤，将事实核查转变为透明的推理过程。此外，框架引入了逻辑感知分解和多角色评审机制以处理复杂声明。虽然在原始准确率上略逊于微调模型，但在证据透明度和逻辑推理方面具有显著优势。
selection_source: fresh_fetch
motivation: 针对传统虚假新闻检测缺乏可解释性以及难以处理复杂逻辑声明和推理不确定性的问题。
method: 提出一个由提取、检索、验证和解释智能体组成的流水线，并扩展了逻辑分解、多角色评审及逻辑聚合机制。
result: 在LIAR基准测试中，该框架显著提升了证据透明度和复合声明的推理能力，尽管原始性能指标仍受限于检索质量。
conclusion: 多智能体协作能有效增强事实核查的透明度，但检索质量和不确定性校准仍是实现可靠自动核查的主要瓶颈。
---

## 摘要
TRUST Agents 是一个用于可解释事实验证和虚假新闻检测的协作式多智能体框架。该系统并非将验证视为简单的真伪分类任务，而是识别可验证的断言，检索相关证据，将断言与证据进行比对，在不确定性下进行推理，并生成人类可检查的解释。其基准流水线由四个专门的智能体组成：断言提取器利用命名实体识别、依存句法分析和基于大语言模型（LLM）的提取技术来识别事实性断言；检索智能体使用 BM25 和 FAISS 执行混合稀疏与稠密搜索；验证智能体将断言与检索到的证据进行比较，并生成带有校准置信度的判定结果；解释智能体随后生成包含明确证据引用的可读报告。为了更有效地处理复杂断言，我们引入了一个面向研究的扩展版本，包含三个额外组件：受 LoCal 风格断言分解启发的分解智能体；受德尔菲法（Delphi）启发的、具有专门验证者角色的多智能体陪审团；以及一个利用合取、析取、否定和蕴含逻辑来组合原子判定结果的逻辑聚合器。我们在 LIAR 基准数据集上对这两种流水线进行了评估，并与微调后的 BERT、微调后的 RoBERTa 以及零样本 LLM 基准进行了对比。尽管监督式编码器在原始指标上仍然表现更强，但 TRUST Agents 提高了可解释性、证据透明度以及对复合断言的推理能力。结果还表明，检索质量和不确定性校准仍然是可信自动化事实验证的主要瓶颈。

## Abstract
TRUST Agents is a collaborative multi-agent framework for explainable fact verification and fake news detection. Rather than treating verification as a simple true-or-false classification task, the system identifies verifiable claims, retrieves relevant evidence, compares claims against that evidence, reasons under uncertainty, and generates explanations that humans can inspect. The baseline pipeline consists of four specialized agents. A claim extractor uses named entity recognition, dependency parsing, and LLM-based extraction to identify factual claims. A retrieval agent performs hybrid sparse and dense search using BM25 and FAISS. A verifier agent compares claims with retrieved evidence and produces verdicts with calibrated confidence. An explainer agent then generates a human-readable report with explicit evidence citations. To handle complex claims more effectively, we introduce a research-oriented extension with three additional components: a decomposer agent inspired by LoCal-style claim decomposition, a Delphi-inspired multi-agent jury with specialized verifier personas, and a logic aggregator that combines atomic verdicts using conjunction, disjunction, negation, and implication. We evaluate both pipelines on the LIAR benchmark against fine-tuned BERT, fine-tuned RoBERTa, and a zero-shot LLM baseline. Although supervised encoders remain stronger on raw metrics, TRUST Agents improves interpretability, evidence transparency, and reasoning over compound claims. Results also show that retrieval quality and uncertainty calibration remain the main bottlenecks in trustworthy automated fact verification.

---

## 论文详细总结（自动生成）

### 论文总结：TRUST Agents 多智能体协作事实核查框架

#### 1. 论文的核心问题与整体含义（研究动机和背景）
随着虚假信息的规模化传播，自动事实核查系统不仅需要高准确率，更需要**可解释性、证据支撑和处理复杂逻辑断言的能力**。
*   **研究动机**：传统的监督学习模型（如 BERT、RoBERTa）虽然在基准测试中表现良好，但往往是“黑盒”分类，缺乏推理过程，且容易受到数据集统计偏差的影响。
*   **核心挑战**：单一的大语言模型（LLM）在核查时容易产生幻觉、证据支撑薄弱，且难以处理包含多个逻辑连接词（如“既...又...”、“如果...那么...”）的复合断言。

#### 2. 论文提出的方法论
TRUST Agents 提出了一个模块化的多智能体协作框架，分为**基准流水线（Baseline）**和**研究扩展流水线（Research Extension）**。

*   **基准流水线核心步骤**：
    1.  **断言提取（Claim Extractor）**：结合命名实体识别（NER）、依存句法分析和 LLM 提示词，从非结构化文本中提取可验证的事实断言。
    2.  **混合检索（Evidence Retrieval）**：采用 BM25（词法匹配）和 FAISS（语义向量匹配）的混合策略。公式为：$score_{hybrid} = 0.6 \cdot score_{BM25} + 0.4 \cdot score_{dense}$。
    3.  **验证（Verifier）**：逐一对比证据与断言，生成带有置信度的判定（支持、矛盾、信息不足）。
    4.  **解释（Explainer）**：将推理信号转化为包含证据引用的自然语言报告。

*   **研究扩展版关键技术**：
    *   **逻辑分解（Decomposer）**：将复杂断言分解为原子命题，并输出逻辑公式（如 $A \land B$）。
    *   **德尔菲陪审团（Delphi Jury）**：引入多个验证者角色（如“严谨法律人”、“开放实用主义者”、“因果怀疑论者”），通过信任加权投票决定原子命题的真伪。
    *   **逻辑聚合（Logic Aggregator）**：根据分解出的逻辑结构（合取、析取、否定、蕴含）汇总最终判定。

#### 3. 实验设计
*   **数据集**：使用 **LIAR 评估基准**，包含 12,836 条标注了六种真实度等级的政治声明。实验中将其简化为二分类（正类：True/Mostly-true/Half-true；负类：False/Pants-on-fire/Barely-true）。
*   **对比方法（Baselines）**：
    1.  **微调 BERT**：在 LIAR 训练集上进行监督学习。
    2.  **微调 RoBERTa**：同上。
    3.  **零样本（Zero-shot）LLM**：直接让模型判断真伪，不使用检索和多智能体流程。
*   **评估指标**：准确率（Accuracy）和 Macro-F1 分数。

#### 4. 资源与算力
*   **硬件环境**：论文明确提到实验在一台配备 **8 核 CPU 和 32 GB RAM** 的机器上运行。
*   **算力细节**：文中**未明确提及具体的 GPU 型号和数量**，但提到使用了 HuggingFace Transformers 库对 BERT 和 RoBERTa 进行了微调（学习率 $2 \times 10^{-5}$，Batch Size 16，训练 2 个 Epoch）。LLM 调用采用了低采样温度以保证输出稳定性。

#### 5. 实验数量与充分性
*   **实验规模**：主要针对 LIAR 测试集进行了完整评估，对比了基准流水线和研究扩展版在不同“不确定性映射策略”（将“不确定”视为真或假）下的表现。
*   **充分性评价**：
    *   **客观性**：通过对比监督学习模型和零样本模型，客观展示了多智能体系统在原始指标上的差距及其在解释性上的优势。
    *   **局限性**：实验仅限于 LIAR 一个数据集，缺乏对长篇新闻、多语言环境或实时新闻数据的测试。消融实验主要体现在“基准 vs 研究版”的对比上，对单个智能体角色的贡献度分析略显不足。

#### 6. 论文的主要结论与发现
*   **性能权衡**：微调后的 BERT/RoBERTa 在准确率（约 0.65）上仍优于 TRUST Agents（约 0.52）。这是因为监督模型学习了数据集的统计分布，而 TRUST Agents 严格依赖外部证据。
*   **高弃权率**：TRUST Agents 表现出高度的谨慎性，基准版约 70% 的案例返回“不确定”，研究版则高达 82%。
*   **逻辑推理优势**：研究扩展版通过逻辑分解和多角色评审，在处理复合断言时提供了更深层的透明度，Macro-F1 略有提升。
*   **瓶颈所在**：**检索质量**是核心瓶颈。如果检索不到相关证据，再复杂的推理逻辑也会导致系统因谨慎而选择“弃权”。

#### 7. 优点
*   **高度可解释性**：将黑盒分类转变为“提取-检索-验证-解释”的透明流水线，每一步都可审计。
*   **逻辑感知能力**：通过逻辑分解和聚合，解决了传统模型难以处理复合命题和因果假设的问题。
*   **置信度校准**：引入了多角色陪审团和信任分机制，能够识别证据冲突并合理表达不确定性。

#### 8. 不足与局限
*   **基准性能较低**：在标准分类指标上不及经过大量标注数据训练的编码器模型。
*   **证据库限制**：目前主要依赖维基百科等静态库，对时效性极强或冷门领域的断言覆盖不足。
*   **系统复杂性与延迟**：多轮 LLM 调用（分解、多角色验证、聚合）显著增加了推理成本和响应时间。
*   **评估偏差**：将三分类（真、假、不确定）强行映射到二分类基准进行比较，可能掩盖了系统在减少误导性判断方面的真实价值。

（完）
