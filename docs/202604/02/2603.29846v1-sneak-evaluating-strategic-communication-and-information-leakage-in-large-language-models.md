---
title: "SNEAK: Evaluating Strategic Communication and Information Leakage in Large Language Models"
title_zh: SNEAK：评估大语言模型中的策略性通信与信息泄露
authors: "Adar Avsian, Larry Heck"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29846v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 评估多智能体环境中的策略性通信与信息泄露
tldr: 本研究针对大语言模型在多智能体博弈中平衡信息传递与保密性的能力，提出了SNEAK评测基准。该基准要求模型在非对称信息环境下，向盟友传递特定秘密信息的同时避免被对手推断。通过模拟盟友和对手的评估，发现当前模型在策略性沟通方面仍面临巨大挑战，表现远逊于人类。
selection_source: fresh_fetch
motivation: 现有基准缺乏对LLM在非对称信息环境下平衡信息共享与隐私保护这一策略性沟通能力的直接评估。
method: 引入SNEAK基准，要求模型生成能让知情盟友识别但让不知情对手难以推断秘密单词的消息，并定义了效用与泄露量化指标。
result: 实验显示当前大语言模型难以在信息有用性与保密性之间取得平衡，人类在这一任务上的表现远超所有受试模型。
conclusion: 策略性沟通是当前LLM的显著短板，SNEAK为研究多智能体环境下的信息安全与协作提供了新的评估框架。
---

## 摘要
大语言模型（LLMs）正越来越多地部署在多智能体场景中，在这些场景中，通信必须在信息量和保密性之间取得平衡。在此类设定下，智能体可能需要向协作方传递信息，同时防止对手推断出敏感细节。然而，现有的 LLM 基准测试主要评估推理、事实知识或指令遵循等能力，并未直接衡量非对称信息下的策略性通信。我们推出了 SNEAK（Secret-aware Natural language Evaluation for Adversarial Knowledge），这是一个用于评估语言模型中选择性信息共享的基准。在 SNEAK 中，模型被给定一个语义类别、一组候选词和一个秘密词，且必须生成一条消息，在不过于清晰地泄露秘密的前提下表明其知晓该秘密。我们使用两个具有不同信息状态的模拟智能体来评估生成的消息：一个是“盟友”（ally），他知道秘密并必须识别出目标消息；另一个是“变色龙”（chameleon），他不知道秘密并试图从消息中推断出秘密。这产生了两个互补的指标：效用（utility），衡量消息与协作方沟通的效果；泄露（leakage），衡量消息向对手揭示了多少信息。利用该框架，我们分析了现代语言模型在信息量与保密性之间的权衡，并表明非对称信息下的策略性通信对于当前系统而言仍是一项具有挑战性的能力。值得注意的是，人类参与者的表现大幅优于所有评估的模型，得分高出多达四倍。

## Abstract
Large language models (LLMs) are increasingly deployed in multi-agent settings where communication must balance informativeness and secrecy. In such settings, an agent may need to signal information to collaborators while preventing an adversary from inferring sensitive details. However, existing LLM benchmarks primarily evaluate capabilities such as reasoning, factual knowledge, or instruction following, and do not directly measure strategic communication under asymmetric information. We introduce SNEAK (Secret-aware Natural language Evaluation for Adversarial Knowledge), a benchmark for evaluating selective information sharing in language models. In SNEAK, a model is given a semantic category, a candidate set of words, and a secret word, and must generate a message that indicates knowledge of the secret without revealing it too clearly. We evaluate generated messages using two simulated agents with different information states: an ally, who knows the secret and must identify the intended message, and a chameleon, who does not know the secret and attempts to infer it from the message. This yields two complementary metrics: utility, measuring how well the message communicates to collaborators, and leakage, measuring how much information it reveals to an adversary. Using this framework, we analyze the trade-off between informativeness and secrecy in modern language models and show that strategic communication under asymmetric information remains a challenging capability for current systems. Notably, human participants outperform all evaluated models by a large margin, achieving up to four times higher scores.

---

## 论文详细总结（自动生成）

### SNEAK：评估大语言模型中的策略性通信与信息泄露

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）在多智能体协作和策略对话中的应用日益广泛，模型面临一个核心挑战：**如何在非对称信息环境下进行策略性通信**。即在向盟友传递有用信息（Utility）的同时，防止竞争对手或观察者推断出敏感的秘密细节（Leakage）。现有的 LLM 基准测试（如推理、知识或指令遵循）无法衡量这种在“有用性”与“保密性”之间进行权衡的高阶沟通能力。

#### 2. 论文提出的方法论
论文引入了 **SNEAK**（Secret-aware Natural language Evaluation for Adversarial Knowledge）基准，其核心思想源自聚会游戏《变色龙》（The Chameleon）。

*   **任务流程**：
    1.  **输入**：给定一个语义类别（如“动物”）、一组候选词（如猫、狗、狮子等）和一个秘密词（如“狗”）。
    2.  **生成**：模型生成一条短消息（1-5个词），旨在暗示秘密词。
    3.  **评估**：
        *   **盟友（Ally）**：已知秘密词，从一组消息（包含模型生成的消息和干扰项）中识别出正确的消息。
        *   **变色龙（Chameleon）**：不知秘密词，仅根据类别、候选词和消息来推断秘密词。
*   **核心指标**：
    *   **Utility（效用）**：盟友识别出正确消息的概率（经随机猜测基准归一化）。
    *   **Leakage（泄露）**：变色龙猜中秘密词的概率（经随机猜测基准归一化）。
    *   **SoftScore**：$Utility \times (1 - Leakage)$，该指标奖励那些既有用又保密的消息。
    *   **BinaryScore**：严格判定，仅当盟友选对且变色龙选错时计为成功。

#### 3. 实验设计
*   **数据集**：基于 Banks & Connell (2023) 的类别生产规范，包含 117 个语义类别（67个具体，50个抽象），共计 1,394 个测试实例。
*   **对比方法**：
    *   **基准线**：随机词、类别级同义词（低泄露低效用）、秘密词同义词（高效用高泄露）。
    *   **主流模型**：GPT-5.4、Claude Opus 4.6、Gemini 3.1 Pro、DeepSeek-V3.2、Llama-4 Scout、Qwen3-235B 等。
    *   **推理策略**：思维链（CoT）、自我增强测试时缩放（SETS）、递归消息细化（RMR）。
*   **评估器**：使用三个独立的模型（Mixtral-8x7B, Llama-3.3-70B, Qwen-2.5-72B）作为模拟智能体进行客观评分。

#### 4. 资源与算力
论文**未明确说明**具体的训练或推理算力细节（如 GPU 型号、数量或总计算时长）。由于该研究主要侧重于对现有指令微调模型的推理评估，其核心开销在于调用各大模型 API 或运行本地开源模型进行消息生成与多轮模拟评估。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 1,394 个实例，并对多种闭源和开源模型进行了广泛测试。
*   **消融与鲁棒性**：
    *   进行了**人类对比实验**（117个类别），验证了模型评估器与人类判断的一致性。
    *   测试了不同**评估器模型**对结果的影响，发现排名高度一致（相关性 > 0.9）。
    *   分析了**候选集大小**和**干扰项数量**对指标的敏感性。
    *   探讨了**语义领域**（抽象 vs 具体）对任务难度的影响。
*   **评价**：实验设计非常充分且严谨，通过多评估器交叉验证和人类基准对比，确保了评估的客观性与公平性。

#### 6. 论文的主要结论与发现
*   **模型表现远逊于人类**：人类在 SoftScore 上得分约为 59.2，而表现最好的模型（Gemma-3-27B）仅为 34.9。人类在保持高效用的同时，泄露率远低于模型。
*   **效用与泄露的权衡困境**：当前 LLM 往往能生成对盟友非常有用的消息，但这些消息几乎总是会导致严重的泄露。模型在“策略性模糊”方面表现欠佳。
*   **推理策略的影响**：CoT 和 RMR（递归细化）在某些模型上能提升得分，但无法根本性消除与人类的差距。
*   **语义影响**：抽象类别的任务相对容易，因为它们允许更间接的关联；而语义重叠严重的候选集（如相似的动物）会显著增加任务难度。

#### 7. 优点
*   **创新的评估视角**：填补了 LLM 在非对称信息下策略通信评估的空白。
*   **行为化评估框架**：通过模拟智能体的预测行为而非主观打分（LLM-as-judge），提高了评估的可重复性和客观性。
*   **任务设计的简洁性**：将复杂的策略博弈抽象为单轮消息生成，便于大规模自动化测试。

#### 8. 不足与局限
*   **场景单一化**：目前仅限于单轮单词猜测，未能捕捉多轮对话、欺骗或更复杂的谈判动态。
*   **语义范围限制**：数据集主要基于名词类别，缺乏对更广泛上下文、常识或特定领域知识的覆盖。
*   **模拟偏差**：虽然验证了与人类的一致性，但模拟智能体（LLM）的推理逻辑可能与真实人类在博弈中的直觉仍有差异。
*   **应用局限**：该基准主要测试“暗示”能力，对于现实中更复杂的隐私保护通信（如差分隐私或加密通信）参考意义有限。

（完）
