---
title: "HabitatAgent: An End-to-End Multi-Agent System for Housing Consultation"
title_zh: HabitatAgent：一种用于住房咨询的端到端多智能体系统
authors: "Hongyang Yang, Yanxin Zhang, Yang She, Yue Xiao, Hao Wu, Yiyang Zhang, Jiapeng Hou, Rongshan Zhang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00556v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 具有专门角色的LLM驱动多智能体架构
tldr: "HabitatAgent是一个针对住房咨询设计的端到端多智能体系统。针对现有平台在处理复杂约束和事实性保障方面的不足，该系统通过记忆、检索、生成和验证四个专业化智能体协作，实现了可审计且可靠的咨询流程。在真实场景测试中，其准确率从基准的75%提升至95%，显著增强了决策支持的透明度与准确性。"
selection_source: fresh_fetch
motivation: 现有的住房推荐系统在处理多约束条件时推理不透明且事实性保障有限，难以满足高风险的住房决策需求。
method: 提出由记忆、检索（GraphRAG）、生成和验证四个专业智能体构成的多智能体架构，实现端到端的咨询支持。
result: "在100个真实用户咨询场景的评估中，HabitatAgent达到了95%的准确率，远超传统基准系统的75%。"
conclusion: HabitatAgent通过多智能体协作和多层验证机制，为复杂决策支持场景提供了一个可靠且可审计的解决方案。
---

## 摘要
住房选择是一个高风险且在很大程度上不可逆的决策问题。我们将住房咨询视为住房选择的一种决策支持接口。现有的住房平台和许多基于大语言模型（LLM）的助手通常将这一过程简化为排序或推荐，导致推理过程不透明、多约束处理能力脆弱，且对事实性的保证有限。我们提出了 HabitatAgent，这是首个用于端到端住房咨询的、由大语言模型驱动的多智能体架构。HabitatAgent 包含四个专门的智能体角色：记忆（Memory）、检索（Retrieval）、生成（Generation）和验证（Validation）。记忆智能体通过约束提取、记忆融合和验证门控更新等内部阶段来维护多层用户记忆；检索智能体执行混合向量-图检索（GraphRAG）；生成智能体产生引用证据的推荐和解释；验证智能体则应用多层验证和针对性修复。这些智能体共同为端到端住房咨询提供了一个可审计且可靠的工作流。我们在端到端正确性协议下，针对 100 个真实用户咨询场景（300 个多轮问答对）对 HabitatAgent 进行了评估。一个强大的单阶段基准模型（Dense+Rerank）达到了 75% 的准确率，而 HabitatAgent 达到了 95%。

## Abstract
Housing selection is a high-stakes and largely irreversible decision problem. We study housing consultation as a decision-support interface for housing selection. Existing housing platforms and many LLM-based assistants often reduce this process to ranking or recommendation, resulting in opaque reasoning, brittle multi-constraint handling, and limited guarantees on factuality.   We present HabitatAgent, the first LLM-powered multi-agent architecture for end-to-end housing consultation. HabitatAgent comprises four specialized agent roles: Memory, Retrieval, Generation, and Validation. The Memory Agent maintains multi-layer user memory through internal stages for constraint extraction, memory fusion, and verification-gated updates; the Retrieval Agent performs hybrid vector--graph retrieval (GraphRAG); the Generation Agent produces evidence-referenced recommendations and explanations; and the Validation Agent applies multi-tier verification and targeted remediation. Together, these agents provide an auditable and reliable workflow for end-to-end housing consultation.   We evaluate HabitatAgent on 100 real user consultation scenarios (300 multi-turn question--answer pairs) under an end-to-end correctness protocol. A strong single-stage baseline (Dense+Rerank) achieves 75% accuracy, while HabitatAgent reaches 95%.

---

## 论文详细总结（自动生成）

### HabitatAgent: 住房咨询端到端多智能体系统论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
住房选择是一项高风险、高成本且具有不可逆性的决策。传统的住房平台（如贝壳、Zillow等）通常将该过程简化为简单的“搜索与排序”，存在以下核心痛点：
*   **推理不透明**：用户难以理解推荐背后的具体逻辑。
*   **多约束处理能力弱**：难以同时满足地理位置、学区、预算、交通等多维度的硬性与软性约束。
*   **事实性保障不足**：大语言模型（LLM）在处理房产数据时容易产生幻觉（如虚构房价或配套设施）。
*   **用户偏好演变**：用户需求在多轮对话中不断细化，系统需具备一致且动态更新的记忆能力。

#### 2. 方法论：核心思想与技术细节
HabitatAgent 提出了一个由四个专业化智能体构成的闭环多智能体架构：
*   **记忆智能体 (Memory Agent)**：采用**四层记忆结构**（对话、实体、偏好、检索记忆）。核心创新是**验证门控更新机制**：只有通过验证的响应信息才会被提取并更新到长期记忆中，防止错误信息污染用户画像。
*   **检索智能体 (Retrieval Agent)**：实现**自适应混合向量-图检索 (GraphRAG)**。系统通过一个自适应路由器预测查询复杂度，简单查询使用向量检索，复杂查询（涉及空间、关系约束）则调用包含 6,016 个节点和 45,000 条边的房产知识图谱。
*   **生成智能体 (Generation Agent)**：基于任务感知提示词（涵盖 14 种咨询场景），生成带有**证据引用标签 (evidence_ref)** 的响应，确保每个数字和事实均可追溯至检索到的证据快照。
*   **验证智能体 (Validation Agent)**：执行三层验证（事实一致性、实体准确性、合规性）。若验证失败，触发**故障类型感知修复 (Remediation)**，针对性地进行实体重检索、约束放宽或局部修正，而非盲目重新生成。

#### 3. 实验设计
*   **数据集**：基于北京某住房平台的真实匿名交互数据，包含 5,000 条房产列表及关联的学区、交通等结构化数据。
*   **场景**：100 个真实咨询场景，共 300 个多轮问答对（分为简单和复杂两类）。
*   **对比方法 (Baselines)**：
    *   B1: 单体 RAG（无记忆/验证）。
    *   B2: Dense+Rerank（强基准，向量检索+重排序）。
    *   B3: 固定 GraphRAG（全量使用图检索）。
    *   B4: LLM-Ranker（大模型排序）。
    *   B5: Self-RAG（自我纠错）。
    *   B6: Rule-Verifier（基于规则的验证）。

#### 4. 资源与算力
*   **模型使用**：文中提到使用了 **Qwen-7B** 处理简单任务，**DeepSeek** 处理复杂任务。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量、训练时长或推理所需的硬件配置。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 300 组端到端测试，并针对 60 组复杂查询进行了子集分析。
*   **消融实验**：针对“验证门控”、“自适应路由”、“故障修复”和“多层验证”分别进行了消融测试，验证了各模块对最终准确率的贡献。
*   **客观性**：引入了三名专业标注员进行人工评估，并计算了 Cohen’s κ 系数（0.82），显示出较高的一致性和评估的客观性。

#### 6. 主要结论与发现
*   **性能飞跃**：HabitatAgent 的端到端准确率达到 **95%**，远超强基准 Dense+Rerank (75%)。
*   **复杂约束处理**：在处理包含 3 个以上约束的复杂查询时，HabitatAgent 的约束满足率 (CSR@5) 保持在 95%，而纯向量检索方法仅为 8%。
*   **效率平衡**：通过自适应路由，系统在保证准确性的同时，比全量图检索方法降低了约 100ms 的 P95 延迟。
*   **可靠性**：验证门控机制有效防止了多轮对话中的错误累积。

#### 7. 优点
*   **闭环可靠性**：通过“检索-生成-验证-修复”的闭环，极大地降低了高风险决策场景下的幻觉风险。
*   **可审计性**：证据引用标签和证据快照机制使得系统的每一句推荐都有据可查。
*   **架构优雅**：自适应路由机制很好地平衡了图检索的精准度与向量检索的效率。

#### 8. 不足与局限
*   **地域局限性**：实验数据仅限于北京，不同城市的房产政策和市场逻辑差异可能影响系统的泛化能力。
*   **知识图谱维护**：房产数据（如价格、在售状态）更新极快，论文未深入探讨如何实现知识图谱的实时动态更新。
*   **长程记忆压缩**：为了控制延迟，对话记忆仅保留最近 5 轮，虽然有上下文摘要补偿，但在极长周期的咨询中仍可能丢失细节。

（完）
