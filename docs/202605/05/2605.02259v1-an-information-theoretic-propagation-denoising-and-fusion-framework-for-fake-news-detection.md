---
title: An Information-theoretic Propagation Denoising and Fusion Framework for Fake News Detection
title_zh: 一种用于虚假新闻检测的信息论传播去噪与融合框架
authors: "Mengyang Chen, Lingwei Wei, Wei Zhou, Songlin Hu"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02259v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 大语言模型模拟用户交互用于虚假新闻检测
tldr: 针对虚假新闻检测中传播数据不完整的问题，本文提出InfoPDF框架。该框架利用大语言模型生成合成传播数据，并从信息论角度出发，通过将合成图建模为概率潜在分布来实现与真实数据的自适应融合。通过互信息优化目标，InfoPDF能有效抑制噪声、保持数据一致性并提取关键特征，显著提升了检测性能。
selection_source: fresh_fetch
motivation: 现有方法利用大模型生成的合成传播数据存在不可靠和噪声问题，直接融合会导致表示偏差并限制检测效果。
method: 提出一种信息论去噪与融合框架，通过概率潜在分布建模合成传播，并利用互信息目标函数实现去噪、一致性维护和任务特征提取。
result: 在三个真实世界数据集上的实验证明，InfoPDF在多种虚假新闻检测任务中均一致优于现有主流方法。
conclusion: 该研究通过信息论方法有效解决了合成传播数据的不可靠性问题，为利用生成式AI增强虚假新闻检测提供了新思路。
---

## 摘要
不完整的传播数据严重阻碍了鲁棒的虚假新闻检测。最近的方法利用大语言模型通过角色扮演来模拟缺失的用户交互，从而利用合成信号丰富传播过程。然而，此类传播数据本质上是不可靠的，直接融合这些数据会导致表征偏差，从而限制检测性能。在本文中，我们从互信息的角度缓解了合成传播的不可靠性，并提出了一种新颖的信息论传播去噪与融合（InfoPDF）框架，以从真实和合成传播中学习有效的表征。具体而言，我们首先使用大语言模型生成特定属性的合成传播。然后，我们将每个合成传播图建模为概率潜分布，以引导与真实传播的可靠性感知自适应融合。在训练过程中，我们设计了一个基于互信息的目标函数，以学习压缩且任务充分的传播表征。该目标函数共同抑制了特定属性合成传播中的噪声信号，保持了真实与合成传播表征之间的一致性，并确保了虚假新闻检测和属性预测的任务充分性。在三个真实世界数据集上的实验表明，InfoPDF 在各种虚假新闻检测任务中始终取得优异的性能。进一步的分析表明，InfoPDF 能够估计属性级的可靠性，并学习到更具判别性的传播表征。

## Abstract
Incomplete propagation data significantly hinders robust fake news detection. Recent approaches leverage large language models to simulate missing user interactions via role-playing, thereby enriching propagation with synthetic signals. However, such propagation data is intrinsically unreliable, and directly fusing it can lead to biased representations, leading to limited detection performance. In this paper, we alleviate the unreliability of synthetic propagation from the mutual information perspective and propose a novel information-theoretic propagation denoising and fusion (InfoPDF) framework to learn effective representations from both real and synthetic propagation. Specifically, we first generate attribute-specific synthetic propagation using large language models. Then we model each synthetic propagation graph as a probabilistic latent distribution to guide reliability-aware adaptive fusion with real propagation. During training, we design a mutual information-based objective to learn compressed and task-sufficient propagation representations. It jointly suppresses noisy signals across attribute-specific synthetic propagation, maintains consistency between real and synthetic propagation representations, and ensures task sufficiency for fake news detection and attribute prediction. Experiments on three real-world datasets show that InfoPDF consistently achieves superior performance across various fake news detection tasks. Further analysis demonstrates that InfoPDF can estimate attribute-level reliabilities and learn more discriminative propagation representations.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **InfoPDF** 的框架，旨在解决虚假新闻检测中传播数据不完整以及大模型（LLM）生成的合成数据不可靠的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实场景中，虚假新闻的传播数据往往是不完整的（由于平台限制或恶意删除），导致检测性能下降。
*   **研究背景**：虽然现有研究尝试利用 LLM 模拟缺失的用户交互，但这些合成数据存在**不可靠性**（如幻觉、语气不自然、与现实脱节）。
*   **动机**：作者发现 LLM 生成的合成传播质量与用户属性（如年龄、政治倾向等）高度相关。因此，需要一种能够识别并过滤合成数据中噪声、同时有效融合真实与合成信号的机制。

### 2. 方法论（InfoPDF 框架）
InfoPDF 从**信息论**角度出发，通过以下核心步骤实现去噪与融合：
*   **特定属性合成生成**：利用 LLM 针对 8 种特定用户属性（年龄、性别、教育、职业、政治、注册时间、验证状态、地理位置）分别生成合成评论和传播图。
*   **可靠性感知自适应融合**：
    *   使用图神经网络（GNN）分别编码真实和合成传播。
    *   将融合后的表征建模为**随机潜变量（高斯分布）**，通过变分估计计算每个属性视图的方差，以此作为**可靠性得分**。
    *   利用注意力机制根据可靠性得分对不同属性视图进行加权融合。
*   **基于互信息（MI）的训练目标**：
    1.  **任务充分性（Task Sufficiency）**：最大化表征与虚假新闻标签及用户属性标签之间的互信息，确保提取关键特征。
    2.  **信息压缩（Compression）**：通过变分信息瓶颈（VIB）最小化输入与表征间的互信息，抑制合成数据中的冗余噪声。
    3.  **传播对齐（Alignment）**：最大化真实与合成传播表征间的互信息，减少两者间的分布差异。
*   **LLM-free 推理模式**：训练完成后，可以通过预测器从真实表征中重构合成特征，从而在推理阶段无需调用 LLM，提高部署效率。

### 3. 实验设计
*   **数据集**：使用了三个公开的真实世界数据集：**Twitter**、**CED**（微博数据）和 **PHEME**。
*   **基准方法（Benchmarks）**：
    *   **基于内容的方法**：BERT, DeBERTa, EANN, dEFEND, DualEmo, ARG, GenFEND 等。
    *   **基于传播的方法**：GCN, GAT, GraphSAGE, BiGCN, EBGCN, UPSR, RAGCL, EIN 等。
    *   **LLM 增强方法**：DELL, LLM propagation 等。
*   **实验场景**：通用检测、鲁棒性检测（随机丢弃边/掩码特征）、早期检测（仅使用早期传播数据）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA Tesla V100 GPU**。
*   **模型细节**：
    *   LLM 后端：Twitter 和 PHEME 使用 **Mistral-7B-Instruct-v0.2**；CED 使用 **Qwen1.5-7B-Chat**。
    *   优化器：Adam，学习率 2e-4，Batch Size 32，隐藏层维度 128。
*   **训练时长**：文中未明确提及具体的训练总时长，但给出了超参数设置和迭代逻辑。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上进行了主实验对比（包含 20+ 种基准方法）。
    *   进行了详尽的**消融实验**（验证 7 种组件/目标的有效性）。
    *   针对**鲁棒性**和**早期检测**进行了多比例（20%-80%）的对比实验。
    *   包含属性可靠性分析、表征质量聚类分析、超参数敏感性分析等。
*   **评价**：实验设计非常充分，涵盖了从性能提升到内部机制验证的各个方面。通过 t-test 验证了统计显著性，对比实验考虑了不同类型的基准模型，体现了客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：InfoPDF 在所有数据集和指标上均显著优于现有 SOTA 方法，尤其在数据稀疏的早期检测场景下表现突出。
*   **去噪有效性**：通过互信息压缩目标，模型能有效识别并抑制 LLM 生成的低质量信号。
*   **属性差异**：实验证实了不同用户属性对虚假新闻检测的贡献度不同，且模型能准确估计这些属性视图的可靠性。
*   **推理效率**：LLM-free 模式在保持高性能的同时，极大降低了实际应用中的计算开销。

### 7. 优点与亮点
*   **视角新颖**：首次从信息论（互信息）的角度系统性地处理 LLM 合成传播数据的不可靠性。
*   **自适应性强**：通过概率建模动态评估数据质量，而非盲目信任 LLM 的输出。
*   **实用性高**：设计的 LLM-free 推理模式解决了生成式模型在线推理慢、成本高的痛点。

### 8. 不足与局限
*   **训练成本**：虽然推理是 LLM-free 的，但训练阶段仍需调用 LLM 生成大量合成数据，这在超大规模数据集上可能存在预处理成本较高的问题。
*   **属性依赖**：模型性能依赖于预定义的 8 种属性，如果原始数据中完全缺失这些属性的相关线索，LLM 生成的质量可能会受到限制。
*   **语言偏差**：实验主要集中在英文和中文社交媒体，对于其他语种或特定文化背景下的虚假新闻模拟效果尚待验证。

（完）
