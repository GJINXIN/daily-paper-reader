---
title: "When Embedding-Based Defenses Fail: Rethinking Safety in LLM-Based Multi-Agent Systems"
title_zh: 当基于嵌入的防御失效时：重新思考基于大语言模型的多智能体系统的安全性
authors: "Lingxi Zhang, Guangtao Zheng, Hanjie Chen"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01133v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 大语言模型驱动的多智能体系统中的虚假信息传播
tldr: 本研究探讨了基于大语言模型的多智能体系统（MAS）在恶意攻击下的安全性。研究发现，现有的基于嵌入（Embedding）的防御机制容易被精心构造的恶意消息绕过。为此，作者提出了三种新型攻击手段，并揭示了嵌入防御忽略Token级置信度信号的局限性。通过引入置信度分数来过滤或降权通信消息，显著提升了系统在多种攻击场景下的鲁棒性，并强调了早期干预的重要性。
selection_source: fresh_fetch
motivation: 现有的基于嵌入的防御机制无法有效识别与良性消息嵌入空间接近的恶意攻击，导致多智能体系统面临安全风险。
method: 提出利用Token级别的置信度分数（Logits）作为防御信号，在多智能体通信过程中对可疑消息进行剪枝或降权处理。
result: 实验证明，该方法在不同模型、数据集和通信拓扑结构下均能有效提升系统的抗攻击能力。
conclusion: 置信度信号是提升多智能体系统安全性的关键，且其效能随通信轮次增加而衰减，因此早期干预至关重要。
---

## 摘要
由大语言模型（LLM）驱动的多智能体系统（MAS）使智能体能够进行通信和共享信息，从而在复杂任务上取得强大的性能。然而，这种通信也产生了一个攻击面，恶意智能体可以传播错误信息并操纵群体决策，从而破坏 MAS 的安全性。现有的基于嵌入的防御旨在检测并剔除可疑智能体，但其有效性取决于恶意消息和良性消息的文本嵌入之间是否存在清晰的分离。攻击者可以通过精心构造嵌入与良性消息接近的消息来规避此类防御。我们从理论上分析了这种失效模式，并通过三种攻击（慢速漂移、良性包装和混沌播种）进行了实证验证。我们的分析进一步揭示了基于嵌入的防御的一个根本局限性：由于它们仅依赖于文本嵌入，因此忽略了诸如 Logits 之类的 Token 级置信度信号，而当嵌入在攻击下无法区分时，这些信号仍能提供信息。我们建议在 MAS 通信过程中使用置信度分数来剔除或降低消息的权重。实验表明，该方法在不同模型、数据集和通信拓扑结构中均提高了鲁棒性。此外，我们发现置信度信号的有效性会随着通信轮次的增加而衰减，这突显了早期干预的重要性。这些见解可以为未来关于 MAS 攻击和防御的研究提供参考和启发。

## Abstract
Large language model (LLM)-powered multi-agent systems (MAS) enable agents to communicate and share information, achieving strong performance on complex tasks. However, this communication also creates an attack surface where malicious agents can propagate misinformation and manipulate group decisions, undermining MAS safety. Existing embedding-based defenses aim to detect and prune suspicious agents, but their effectiveness depends on a clear separation between the text embeddings of malicious and benign messages. Attackers can circumvent such defenses by crafting messages whose embeddings lie close to benign ones. We analyze this failure mode theoretically and validate it empirically with three attacks, Slow Drift, Benign Wrapper, and Chaos Seeding. Our analysis further reveals a fundamental limitation of embedding-based defenses: because they rely solely on the text embeddings, they ignore token-level confidence signals such as logits, which can remain informative when embeddings are not distinguishable under attack. We propose using confidence scores to prune or down-weight messages during MAS communication. Experiments show improved robustness across models, datasets, and communication topologies. Moreover, we find that the effectiveness of confidence signals decays over communication rounds, highlighting the importance of early intervention. This insights can inform and inspire future work on MAS attacks and defenses.

---

## 论文详细总结（自动生成）

这是一份关于论文《When Embedding-Based Defenses Fail: Rethinking Safety in LLM-Based Multi-Agent Systems》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：随着大语言模型驱动的多智能体系统（MAS）广泛应用，智能体间的通信安全成为关键。恶意智能体可能通过传播虚假信息操纵群体决策。
*   **核心问题**：现有的 MAS 防御机制（如 G-Safeguard, GUARDIAN）主要依赖**文本嵌入（Embedding）**来检测异常。作者发现，这种防御假设恶意消息在嵌入空间中是明显的离群点。然而，如果攻击者精心构造“近良性（Near-benign）”的消息，使其嵌入向量与良性消息极度接近，现有的防御就会失效。
*   **研究动机**：揭示嵌入防御的理论缺陷，验证新型攻击的有效性，并寻找除嵌入之外的更可靠的内部防御信号。

### 2. 论文提出的方法论
论文从攻击和防御两个维度提出了新方法：

#### A. 理论分析与新型攻击
*   **理论证明**：利用 Lipschitz 连续性证明，基于嵌入的评分函数会学习到一个过宽的“良性接受区”，攻击者只要将恶意消息控制在良性支持集的 $\gamma/L$ 邻域内即可规避检测。
*   **三种近良性攻击**：
    1.  **Slow Drift（慢速漂移）**：在多轮对话中逐渐改变嵌入方向，避免突发性异样。
    2.  **Benign Wrapper（良性包装）**：将短小的恶意指令隐藏在长篇的良性文本中，使整体嵌入受良性部分主导。
    3.  **Chaos Seeding（混沌播种）**：诱导良性智能体产生分歧，扩大良性消息的嵌入分布范围，从而掩盖恶意消息。

#### B. 置信度引导的防御（Confidence-Guided Defense）
*   **核心思想**：利用模型生成 Token 时的内部信号（如 Logits 或熵）来衡量可靠性。
*   **技术细节**：
    1.  **置信度计算**：计算消息中 Token 的熵（Entropy），取前 $k$ 个最高熵的平均值作为不确定性度量，再映射为置信度分数。
    2.  **策略一：置信度剪枝（Pruning）**：直接拦截低于阈值的消息。
    3.  **策略二：置信度降权（Down-weighting）**：将置信度作为元数据随消息发送，提示接收方智能体在推理时降低对低置信度内容的权重。

### 3. 实验设计
*   **数据集**：MMLU（知识）、GSM8K（数学推理）、BBH（逻辑推理）。
*   **模型（Backbones）**：开源模型 LLaMA-3.1-8B、Qwen3-4B；闭源 API 模型 GPT-4o-mini。
*   **Benchmark 与对比方法**：
    *   **无防御状态**（No defense）。
    *   **G-Safeguard**：基于图神经网络（GNN）的嵌入防御。
    *   **GUARDIAN**：基于时序图建模的嵌入防御。
*   **通信拓扑**：星型（Star）、链式（Chain）、随机稀疏图（Random Sparse）。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **vLLM** 框架在本地运行开源模型以获取 Token 级的 Logits。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及具体的训练/推理总时长。这在学术论文中属于对硬件环境描述较简略的部分。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个主流数据集和 3 种不同规模/类型的 LLM。
    *   对比了 3 种新型攻击方式与传统的“明显攻击（Overt Attack）”。
    *   测试了多种通信拓扑结构对防御效果的影响。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅验证了防御的有效性，还深入探讨了信号随轮次衰减的动态过程（消融实验），并分析了拓扑密度与攻击传播速度的关系，提供了多维度的视角。

### 6. 论文的主要结论与发现
1.  **嵌入防御的脆弱性**：在近良性攻击下，基于嵌入的防御准确率大幅下降，甚至可能误伤良性智能体。
2.  **置信度信号的互补性**：Token 级的置信度在嵌入失效时仍能提供有效的辨别信号，显著提升系统鲁棒性。
3.  **信号衰减效应**：随着通信轮次增加，恶意信息会被良性智能体“回声”传播，导致置信度和嵌入信号都会发生坍缩（Self-mixing），使防御难度增大。
4.  **拓扑结构的影响**：更密集的通信拓扑会加速错误信息的污染，缩短防御的有效窗口期。

### 7. 优点
*   **视角独特**：挑战了当前 MAS 安全领域主流的“嵌入检测”范式，提出了更具威胁的攻击模型。
*   **理论与实证结合**：不仅有 Lipschitz 稳定性的理论推导，还有针对性的攻击算法设计。
*   **实用性强**：提出的置信度防御不需要额外训练模型，易于在现有 MAS 框架中部署。

### 8. 不足与局限
*   **算力细节缺失**：未提供详细的硬件开销和推理延迟数据。
*   **置信度获取限制**：对于完全不开放 Logits 或 Token 概率的极少数黑盒 API，该防御方法可能难以实施（尽管 GPT-4o-mini 等已提供部分概率信息）。
*   **防御策略单一**：目前主要依赖熵作为置信度指标，未来可能需要结合自一致性（Self-consistency）等更复杂的内部信号。

（完）
