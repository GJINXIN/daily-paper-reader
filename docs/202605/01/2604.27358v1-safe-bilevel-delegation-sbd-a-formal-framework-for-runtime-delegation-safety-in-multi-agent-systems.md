---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
title_zh: 安全双层委派 (SBD)：多智能体系统中运行时委派安全的形式化框架
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 多智能体系统中的运行时委派安全
tldr: 针对高风险环境下大语言模型智能体任务委派的安全性问题，本文提出了安全双层委派（SBD）形式化框架。该框架通过双层优化机制，在运行时根据上下文动态调整安全与效率的权衡，并引入连续委派度实现从人工干预到完全自主的平滑过渡。研究在理论上证明了安全性单调性、策略收敛性及责任传播边界，为多智能体系统的安全运行提供了坚实的理论支撑。
selection_source: fresh_fetch
motivation: 现有研究多在设计阶段或基于经验准则处理多智能体架构，缺乏在执行过程中动态平衡安全与效率的运行时机制。
method: 采用双层优化模型，外层网络学习环境相关的安全权重，内层循环在满足概率安全约束的前提下优化委派策略。
result: 建立了安全性单调性、内层策略线性收敛性以及多跳委派链中责任传播边界等三项核心理论结果。
conclusion: SBD框架为医疗、金融和教育等高风险领域的智能体委派提供了具备理论保障的安全控制与责任追溯方案。
---

## 摘要
随着大语言模型 (LLM) 智能体被部署在关键任务环境中，如何安全地将子任务委派给专门的子智能体变得至关重要。现有工作在设计阶段解决多智能体架构选择问题，或提供广泛的经验性指导，但两者都未能提供一种在执行过程中随任务上下文变化动态调整安全与效率权衡的运行时机制。我们提出了安全双层委派 (SBD)，这是一种用于分层多智能体系统中运行时委派安全的形式化框架。SBD 将任务委派建模为一个双层优化问题：外层元权重网络 phi 学习依赖于上下文的安全-效率权重 lambda(s) ∈ [0,1]；内层循环在满足概率安全约束 P(safe) >= 1-delta 的前提下优化委派策略 pi。连续委派度 alpha ∈ [0, 1] 控制转移给每个子智能体的决策权限，在完全人工干预 (alpha=0) 和完全自主执行 (alpha=1) 之间进行平滑插值。我们确立了三项理论结果：(1) 安全单调性——更高的外层安全权重会产生弱更安全的内层策略；(2) 内层策略收敛性——在标准平滑性假设下，内层问题的投影梯度下降呈线性收敛；(3) 问责传播界限，该界限在多跳委派链中分配责任，并具有可证明的单智能体上限。我们在三个关键领域实例化了 SBD：医疗 AI (MIMIC-III)、金融风险控制 (S&P 500) 和教育智能体监管 (ASSISTments)，并明确了数据集、安全约束集、基准模型和评估协议。本手稿完整展示了形式化框架和理论结果；按照文中所述协议进行的实证验证已在计划中，并将在后续修订版本中报告。

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在医疗、金融等高风险领域的广泛应用，如何安全地将子任务委派给专门的子智能体成为核心挑战。
*   **研究动机**：现有的多智能体安全机制大多在“设计时”固定（如预设权限、静态架构），无法根据执行过程中的实时风险上下文（如病人病情突然恶化）动态调整。
*   **核心问题**：如何建立一种运行时（Runtime）机制，能够根据任务环境的变化，在“安全性”与“效率”之间找到最优的动态平衡，并提供形式化的安全保障。

### 2. 方法论：核心思想与技术细节
论文提出了 **Safe Bilevel Delegation (SBD)** 框架，其核心是将委派问题建模为一个**双层优化（Bilevel Optimization）**问题：

*   **核心思想**：引入一个连续的委派度 $\alpha \in [0, 1]$。$\alpha=0$ 代表完全人工干预，$\alpha=1$ 代表完全自主执行。系统通过学习来决定在特定状态下应该转移多少决策权。
*   **双层结构**：
    *   **外层（元权重学习）**：学习一个元权重网络 $\phi(s)$，它根据当前状态 $s$ 输出一个权重 $\lambda$。当环境风险高时，$\lambda$ 增大，迫使系统更看重安全性。
    *   **内层（策略优化）**：在给定 $\lambda$ 的情况下，优化委派策略 $\pi$，同时必须满足概率安全约束 $P(safe) \geq 1 - \delta$（即不安全事件的概率低于阈值 $\delta$）。
*   **关键技术**：
    *   **超梯度计算**：使用隐式微分（Implicit Differentiation）处理外层对内层最优解的依赖。
    *   **投影梯度下降（PGD）**：内层优化通过将策略投影到安全可行域来确保硬性安全边界。
    *   **问责传播（Accountability Propagation）**：定义了多跳委派链中的责任权重分配公式，确保责任可追溯。

### 3. 实验设计
论文预注册（Pre-registered）了一个跨三个高风险领域的评估协议：
*   **数据集/场景**：
    1.  **医疗 AI**：使用 MIMIC-III 数据集，根据病人病情严重程度（APACHE-II 评分）决定委派权限。
    2.  **金融风险控制**：使用 S&P 500 数据，在 2020 年新冠市场崩溃等极端波动期间测试系统的风险规避能力。
    3.  **教育监管**：使用 ASSISTments 数据集，针对“风险学生”调整教学智能体的自主权。
*   **对比方法（Baselines）**：
    *   **Fixed-$\alpha$**：固定委派比例。
    *   **Safe-RL (CPO)**：传统的受限策略优化。
    *   **MARL-vanilla**：无安全机制的多智能体强化学习。
    *   **MaAS**：静态架构搜索方法。
    *   **SBD-NoOuter**：消融实验，移除外层动态权重学习。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 (80GB)** GPU。
*   **训练时长**：在设置内层迭代 $T_{in}=50$、外层迭代 $T_{out}=500$ 的情况下，每个领域的完整运行时间**少于 7 分钟**。
*   **模型规模**：$\phi$ 和 $\pi$ 均采用 3-4 层的 MLP 网络，宽度为 256。

### 5. 实验数量与充分性
*   **实验组数**：计划在 3 个不同领域进行实验，每个领域包含 5 个基准对比和 4 组消融实验，并使用 3 个随机种子。
*   **充分性与客观性**：
    *   **理论验证充分**：论文对三个核心定理（单调性、收敛性、问责界限）提供了详尽的数学证明。
    *   **实证尚不完整**：**注意**，这是该论文的 V1 版本，作者明确指出目前仅包含形式化框架、理论证明和“预注册”的实验协议，**实际的实验结果数据尚未在本文档中展示**（计划在修订版中报告）。
    *   **公平性**：通过预注册假说和明确的“证伪标准”（Falsifiability criteria），确保了后续实验结果的客观性。

### 6. 主要结论与发现
1.  **安全单调性**：证明了外层安全权重越高，内层生成的策略在统计上就越安全。
2.  **线性收敛性**：在标准平滑性假设下，内层策略优化能够快速线性收敛。
3.  **责任界限**：证明了在长委派链中，没有任何单一智能体可以承担全部责任，责任会随委派度 $\alpha$ 衰减。
4.  **动态适应性**：SBD 能够自动在低风险时追求效率，在高风险时收回权限。

### 7. 优点
*   **形式化严谨**：将模糊的“委派安全”转化为严格的双层优化数学问题。
*   **运行时适应**：突破了静态权限设置的限制，实现了上下文感知的动态安全控制。
*   **连续控制**：相比于二元的“委派/不委派”，连续的 $\alpha$ 允许更精细的权限平滑过渡。
*   **可解释的问责制**：为多智能体系统的法律和伦理审计提供了数学基础。

### 8. 不足与局限
*   **实证缺失**：当前版本缺少实际运行的实验数据图表，仅有协议。
*   **强假设限制**：理论证明依赖于内层损失函数的“强凸性”假设，这在深度神经网络中可能并不总是成立。
*   **安全约束来源**：系统假设安全约束集 $C$ 是预先给定的（由专家定义），但在复杂现实中，定义完美的约束集本身就很困难。
*   **单层安全**：目前只关注“委派决策”的安全，未建模子智能体在执行过程中的内部故障。

（完）
