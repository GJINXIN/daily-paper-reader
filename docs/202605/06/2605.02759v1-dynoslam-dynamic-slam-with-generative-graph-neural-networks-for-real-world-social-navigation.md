---
title: "DynoSLAM: Dynamic SLAM with Generative Graph Neural Networks for Real-World Social Navigation"
title_zh: DynoSLAM：基于生成式图神经网络的动态 SLAM，用于真实世界社交导航
authors: "Danil Tokhchukov, Veronika Morozova, Gonzalo Ferrer"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02759v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 用于现实社交导航的社交感知图神经网络
tldr: 针对传统SLAM在动态行人环境中表现不佳的问题，本文提出DynoSLAM框架。该框架将生成式图神经网络（GNN）与因子图优化紧密耦合，通过随机世界模型捕捉行人互动的多模态不确定性。通过蒙特卡洛采样将行人运动预测嵌入SLAM图，不仅提升了动态环境下的定位与跟踪精度，还为机器人社交导航提供了概率安全包络。
selection_source: fresh_fetch
motivation: 传统SLAM依赖静态环境假设，难以处理现实世界中行人等动态实体带来的复杂交互和运动不确定性。
method: 将社交感知GNN集成为因子图中的随机世界模型，利用蒙特卡洛模拟捕捉行人运动的多模态不确定性并嵌入动态马氏距离因子。
result: 实验证明该随机公式能有效防止确定性预测导致的优化失败，并在保持高精度跟踪的同时为导航规划提供数学严谨的概率安全包络。
conclusion: DynoSLAM通过融合生成式学习与几何优化，实现了在拥挤动态环境下的鲁棒定位与预见性、无碰撞的社交机器人导航。
---

## 摘要
传统的同步定位与建图（SLAM）算法严重依赖静态环境假设，这极大地限制了它们在行人等移动实体密集的真实世界空间中的适用性。在这项工作中，我们提出了 DynoSLAM，这是一种紧耦合的动态 GraphSLAM 架构，它将社交感知图神经网络（GNN）直接集成到因子图优化中。与使用僵化的等速启发式方法或确定性单智能体神经先验的传统方法不同，我们的框架将行人运动预测建模为一个随机世界模型（World Model）。通过利用训练好的 GNN 进行蒙特卡洛展开（Monte Carlo rollouts），我们捕捉了人类交互的多模态认知不确定性，并通过动态马氏距离（Mahalanobis distance）因子将其嵌入到 SLAM 图中。我们通过广泛的仿真实验证明，这种随机公式不仅保持了高精度的回顾性跟踪，还防止了由确定性“最大值问题”（argmax problem）引起的优化失败。最终，提取未来行人状态的经验均值和协方差矩阵，为下游局部规划器提供了一个数学严谨的概率安全包络，从而在密集拥挤的环境中实现具有预见性且无碰撞的机器人导航。

## Abstract
Traditional Simultaneous Localization and Mapping (SLAM) algorithms rely heavily on the static environment assumption, which severely limits their applicability in real-world spaces populated by moving entities, such as pedestrians. In this work, we propose DynoSLAM, a tightly-coupled Dynamic GraphSLAM architecture that integrates socially-aware Graph Neural Networks (GNNs) directly into the factor graph optimization. Unlike conventional approaches that use rigid constant-velocity heuristics or deterministic single-agent neural priors, our framework formulates pedestrian motion forecasting as a stochastic World Model. By utilizing Monte Carlo rollouts from a trained GNN, we capture the multimodal epistemic uncertainty of human interactions and embed it into the SLAM graph via a dynamic Mahalanobis distance factor. We demonstrate through extensive simulated experiments that this stochastic formulation not only maintains highly accurate retrospective tracking but also prevents the optimization failures caused by the deterministic "argmax problem". Ultimately, extracting the empirical mean and covariance matrices of future pedestrian states provides a mathematically rigorous, probabilistic safety envelope for downstream local planners, enabling anticipatory and collision-free robot navigation in densely crowded environments.

---

## 论文详细总结（自动生成）

以下是对论文《DynoSLAM: Dynamic SLAM with Generative Graph Neural Networks for Real-World Social Navigation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统 SLAM 算法（如 GraphSLAM）建立在“静态环境”假设之上。在行人密集的真实社交场景中，动态物体会引入错误的观测因子，导致轨迹漂移或定位完全失效。
*   **现有方案的缺陷**：目前的动态 SLAM 多采用简单的等速模型（CVM）或确定性神经网络。前者无法处理复杂的非线性避障行为；后者则面临**“Argmax 问题”**——在面对多模态意图（如行人可能从左绕行也可能从右绕行）时，确定性模型往往会输出两种可能性的平均值，导致生成不符合物理规律的直线轨迹，进而误导 SLAM 优化器。
*   **研究目标**：提出一种能感知社交上下文、捕捉运动不确定性，并将其紧耦合进因子图优化的动态 SLAM 框架。

### 2. 方法论：核心思想与技术细节
DynoSLAM 的核心是将行人运动预测建模为一个**随机世界模型（Stochastic World Model）**。
*   **联合状态优化**：将机器人位姿和动态地标（行人）状态整合进联合状态向量 $y_i$，在因子图中同时优化机器人轨迹和行人运动路径。
*   **社交感知图注意力网络（GAT）**：利用 GAT 提取行人间的空间交互特征。通过编码行人的历史轨迹，计算注意力权重以捕捉邻近行人对目标个体未来路径的影响。
*   **蒙特卡洛展开（Monte Carlo Rollouts）**：为了解决确定性预测的局限，系统在预测阶段对 GAT 输出的预测速度注入微小的高斯扰动，并进行多次自回归采样。
*   **动态马氏距离因子**：通过采样得到的多条轨迹计算经验均值（$\mu$）和协方差矩阵（$\Sigma$）。在 SLAM 优化中，使用马氏距离作为运动先验因子（Kinematic Prior）。
    *   **动态刚度调节**：当行人意图明确时，协方差小，先验因子“硬”，约束力强；当交互复杂、预测不确定时，协方差增大，先验因子“软”，优化器会自动降低对预测的依赖，更多参考观测数据。

### 3. 实验设计
*   **数据集与场景**：使用 `pyminisim` 2D 社交导航模拟器。行人运动由“带朝向的社交力模型”（HSFM）驱动，能产生复杂的非线性避障行为。
*   **数据规模**：收集了 6,000 个场景片段（5,500 用于训练，500 用于测试），行人数量从 1 到 15 人不等。
*   **对比方法（Benchmarks）**：
    1.  **No $L_{kin}$**：无运动先验，仅靠观测。
    2.  **CVM Baseline**：传统的等速模型。
    3.  **Single-Agent (MLP)**：不考虑社交上下文的独立神经预测器。
    4.  **Deterministic GAT**：确定性图神经网络。
    5.  **Stochastic GAT (DynoSLAM)**：本文提出的随机模型。
*   **评价指标**：机器人/地标 RMSE、绝对轨迹误差（ATE）、相对位姿误差（RPE）、安全距离误差（SDE）及预测 RMSE。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **开源情况**：作者提供了 GitHub 代码链接，表明其算法具有可复现性，且 GAT 被描述为“轻量级世界模型”，暗示其对实时性有较好支持。

### 5. 实验数量与充分性
*   **实验规模**：6,000 组实验片段在仿真研究中属于较大规模，覆盖了从简单到极度拥挤的多种交互场景。
*   **消融实验**：文章对运动先验因子进行了详细的消融分析（从无先验到线性先验，再到非线性单智能体及多智能体先验），清晰地展示了各组件的贡献。
*   **客观性**：实验对比了目前主流的启发式方法和深度学习基准，评价指标涵盖了定位精度、预测精度和导航安全性，较为全面。

### 6. 主要结论与发现
*   **随机性是关键**：随机 GAT 解决了确定性模型的“均值塌陷”问题。即使预测均值与等速模型相似，其提供的**协方差信息**也能显著提升 SLAM 优化器的鲁棒性。
*   **定位精度**：DynoSLAM 保持了极高的回顾性跟踪精度（机器人 RMSE 为 0.39m），同时在预测未来轨迹方面显著优于 CVM 和 MLP。
*   **赋能下游任务**：提取的概率安全包络（均值+协方差椭圆）为 MPC 或 PPO 等规划器提供了数学严谨的输入，使机器人能提前做出避障决策。

### 7. 优点
*   **紧耦合架构**：将深度学习预测直接嵌入因子图优化，而非简单的后处理，实现了感知与预测的深度融合。
*   **不确定性量化**：通过蒙特卡洛采样将“认知不确定性”转化为可计算的协方差，解决了社交场景中意图模糊的难题。
*   **通用性**：该框架可以作为现有静态 SLAM 系统的插件并行运行，过滤动态噪声并增强地图质量。

### 8. 不足与局限
*   **环境局限性**：目前实验仅在 2D 仿真环境中完成，尚未在复杂的 3D 真实世界传感器数据（如 LiDAR 或深度相机）上进行验证。
*   **随机性来源**：目前的随机性依赖于手动注入的扰动，而非模型内生的生成概率分布（如 Flow Matching 或扩散模型）。
*   **计算开销**：多次蒙特卡洛采样虽然提升了鲁棒性，但在计算资源受限的嵌入式平台上可能会带来实时性挑战（文中未给出详细推理耗时）。

（完）
