---
title: COVID-19 Infodemic. Understanding content features in detecting fake news using a machine learning approach
title_zh: COVID-19 信息疫情：理解机器学习方法在虚假新闻检测中的内容特征
authors: "Vimala Balakrishnan, Lee Zing Hii, Eric Laporte"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06435v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 利用语言和内容特征进行虚假新闻检测
tldr: 本研究针对新冠肺炎疫情期间的“信息疫情”，探讨了文本和语言特征在虚假新闻检测中的应用。通过构建包含二元分词和词性分布等特征的模型，并利用随机森林、支持向量机等五种传统机器学习算法进行实验。研究发现，随机森林在检测任务中表现最佳，证明了内容特征在区分真假新闻方面的有效性，为利用传统机器学习手段检测虚假信息提供了实证支持。
selection_source: fresh_fetch
motivation: 旨在研究文本和语言等内容特征在新冠肺炎虚假新闻检测中的贡献，以填补该领域在特征利用方面的研究空白。
method: 采用随机森林、支持向量机等传统机器学习算法，在新冠疫情数据集上对比分析了二元分词和词性标注等特征的检测效果。
result: 随机森林算法获得了最佳检测性能，且文本和语言特征在单独使用时均能显著提升虚假新闻的识别准确率。
conclusion: 实验证明传统机器学习方法结合内容特征可以成功检测虚假新闻，为非深度学习的检测方案提供了有效路径。
---

## 摘要
尽管实证证据表明内容特征有助于区分真实新闻和虚假新闻，但利用内容特征（特别是文本和语言特征）进行虚假新闻检测的研究仍不够充分。为此，本研究调查了一系列选定的内容特征，如词二元组（word bigrams）、词性分布（part of speech distribution）等，以改进虚假新闻检测。我们在 COVID-19 大流行期间收集的一个新数据集上进行了一系列实验，并使用了决策树（Decision Tree）、K-最近邻（K-Nearest Neighbor）、逻辑回归（Logistic Regression）、支持向量机（Support Vector Machine）和随机森林（Random Forest）。在所有实验设置中，随机森林获得了最佳结果，支持向量机紧随其后。总的来说，研究发现文本和语言特征在单独使用时都能改进虚假新闻检测，然而，将它们组合成一个单一模型并没有显著提高检测效果。研究还注意到使用二元组和词性标签之间的差异。本研究表明，与深度学习相比，传统的机器学习方法可以成功地利用文本和语言特征来检测虚假新闻。

## Abstract
The use of content features, particularly textual and linguistic for fake news detection is under-researched, despite empirical evidence showing the features could contribute to differentiating real and fake news. To this end, this study investigates a selection of content features such as word bigrams, part of speech distribution etc. to improve fake news detection. We performed a series of experiments on a new dataset gathered during the COVID-19 pandemic and using Decision Tree, K-Nearest Neighbor, Logistic Regression, Support Vector Machine and Random Forest. Random Forest yielded the best results, followed closely by Support Vector Machine, across all setups. In general, both the textual and linguistic features were found to improve fake news detection when used separately, however, combining them into a single model did not improve the detection significantly. Differences were also noted between the use of bigrams and part of speech tags. The study shows that textual and linguistic features can be used successfully in detecting fake news using the traditional machine learning approach as opposed to deep learning.

---

## 论文详细总结（自动生成）

这篇论文对 COVID-19 期间的虚假新闻检测进行了深入研究，重点探讨了文本和语言特征在传统机器学习框架下的有效性。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在 COVID-19 大流行期间，虚假信息（Misinformation）的传播形成了“信息疫情”（Infodemic），严重威胁公共卫生安全。
*   **核心问题**：尽管深度学习在 NLP 领域占据主导，但利用**文本和语言内容特征**（如词性分布、二元分词等）在传统机器学习算法中区分真假新闻的潜力尚未得到充分挖掘。
*   **研究目的**：通过提取特定的内容特征，构建有效的机器学习模型，以自动检测与 COVID-19 相关的虚假新闻，并理解哪些特征对检测贡献最大。

### 2. 论文提出的方法论
研究构建了一个包含五个阶段的架构：
*   **数据收集**：从马来西亚事实核查网站（sebenarnya.my）抓取虚假新闻，从主流媒体（The Star）抓取真实新闻。
*   **预处理**：包括将马来语翻译为英语（由专家校验）、词性标注（POS Tagging）、去除停用词、词干提取和词形还原。
*   **特征提取**（核心技术）：提取了 9 种特征，分为三类：
    1.  **词性统计（POS）**：属于浅层语法特征，计算各类词性（如名词、动词、形容词）的分布。
    2.  **二元分词（Bigrams）**：利用词袋模型（BoW）提取连续的词对，捕捉语义信息。
    3.  **杂项特征（Misc）**：涉及复杂度和风格，包括大写字母比例、标点符号比例、数字比例、停用词比例、类符/形符比（TTR）、句子长度及每句平均词数。
*   **检测模型**：使用了五种传统机器学习算法：决策树（DT）、随机森林（RF）、逻辑回归（LR）、支持向量机（SVM）和 K-最近邻（KNN）。

### 3. 实验设计
*   **数据集**：共 1,512 篇新闻稿件（699 篇虚假，813 篇真实），样本相对平衡。
*   **实验场景（Benchmark）**：
    *   **Baseline**：仅基于原始文本。
    *   **各特征组消融实验**：分别测试 Bigram、POS、Misc 特征对 Baseline 的提升。
    *   **All**：融合所有特征。
*   **评估指标**：采用 10 折交叉验证（10-fold cross-validation），使用准确率（Accuracy）、召回率（Recall）、精确率（Precision）和 F1 分数。

### 4. 资源与算力
*   **硬件/算力**：论文**未明确说明**具体的硬件配置（如 GPU/CPU 型号）。
*   **软件环境**：明确提到使用 Python 语言及 `scikit-learn` 工具包进行建模和评估。由于使用的是传统机器学习算法而非大规模深度学习模型，推测其对算力要求较低，普通工作站即可完成。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种算法分别运行了 5 种特征组合（Baseline, POS, Bigram, Misc, All），总计至少 25 组核心对比实验。
*   **充分性评价**：实验设计较为**客观且公平**。通过 10 折交叉验证减少了数据划分带来的偏差；对比了多种主流机器学习算法，并对特征进行了分类讨论。虽然数据集规模（1,512 条）在机器学习领域不算大，但对于特定主题（COVID-19）且经过人工核实的虚假新闻研究来说，具有一定的代表性。

### 6. 主要结论与发现
*   **算法表现**：**随机森林（RF）**表现最优，其次是**支持向量机（SVM）**，两者在多数设置下的准确率和 F1 分数均超过 95%。
*   **特征贡献**：文本和语言特征在单独使用时均能提升检测效果。
*   **特征组合**：令人意外的是，将所有特征组合在一起（All 模式）并没有比单一特征组显著提高性能，甚至在 SVM 和 LR 中导致性能下降，这可能与特征冗余或小样本下的维度灾难有关。
*   **语言学观察**：真实新闻通常更长、句子更多、大写字母使用更规范；虚假新闻则表现出更高的类符/形符比（TTR），意味着词汇变化更多，且句子往往较短。

### 7. 优点
*   **特征工程深入**：不仅关注分类结果，还深入分析了 POS 分布和 Bigram 词频，揭示了真假新闻在写作风格上的本质差异。
*   **实用性强**：证明了在特定领域（如疫情）下，传统机器学习结合精选特征可以达到甚至超过深度学习的精度，且计算成本更低。
*   **数据可靠**：使用了经过官方事实核查机构验证的真实/虚假新闻数据集。

### 8. 不足与局限
*   **翻译偏差风险**：部分原始数据为马来语，通过 Google 翻译转为英语，尽管有专家校验，但翻译过程可能损失了原始的语言风格特征。
*   **数据集局限**：样本量较小，且仅限于 COVID-19 主题，模型的泛化能力（迁移到其他类型的虚假新闻）有待验证。
*   **分类维度单一**：仅进行了二分类（真 vs 假），未区分虚假新闻的具体类型（如讽刺、误导性标题、宣传等）。
*   **特征融合策略简单**：仅采用了简单的特征拼接，未探索更高级的特征选择或加权融合方法。

（完）
