---
title: Towards multi-modal forgery representation learning for AI-generated video detection and localization
title_zh: 迈向用于AI生成视频检测与定位的多模态伪造表示学习
authors: "Dat Le, Khoa Nguyen, Xin Wang, Shu Hu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07232v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: AI生成视频检测与定位
tldr: 随着生成式AI的发展，AI生成视频带来的语义扭曲和滥用风险日益增加。针对现有检测器在多模态建模和细粒度时间定位方面的局限，本文提出一种多模态伪造表示学习架构。该架构整合了LMM语义分支、时空视觉分支和多尺度局部伪造音频分支，实现了对部分篡改视频的同步检测与精确时间定位，在多项实验中显著超越了现有最先进方法。
selection_source: fresh_fetch
motivation: 现有AI生成视频检测工具多局限于单一或部分模态建模，且缺乏对部分篡改内容的细粒度时间定位能力。
method: 提出一种集成大语言模型（LMM）语义、时空视觉和多尺度局部伪造音频分支的多模态核心架构。
result: 实验结果表明，该方法在检测和细粒度时间定位任务上均优于现有的最先进技术。
conclusion: 结合多模态特征的表示学习能有效提升AI生成视频伪造检测的可靠性与定位精度。
---

## 摘要
生成式人工智能的最新进展使大规模视频创作变得普及。AI生成的视频（包括跨视觉和音频通道的部分篡改片段）带来了日益严重的语义扭曲和滥用风险，这促使了对可靠检测工具的需求。大多数现有的AI生成视频检测器仍受限于单一或部分模态的数据建模，且缺乏细粒度的时间伪造定位。为了应对这些挑战，我们的主要创新在于引入了一个核心架构，该架构共同集成了LMM语义分支、时空（ST）视觉分支以及多尺度部分伪造（PS）音频分支。这种多模态方法能够同时实现对部分篡改的AI生成视频伪造的检测和细粒度时间定位。大量实验表明，该方法优于现有的最先进方法。

## Abstract
Recent advances in generative AI have democratized video creation at scale. AI-generated videos, including partially manipulated clips across visual and audio channels, pose escalating risks of semantic distortion and misuse, which motivates the need for reliable detection tools. Most existing AI-generated video detectors remain limited by single- or partial-modality of data modeling and the lack of fine-grained temporal forgery localization. To address these challenges, our primary novelty introduces a core architecture that jointly integrates an LMM semantic branch with a spatio-temporal (ST) visual branch and a multi-scale partial-spoof (PS) audio branch. This multi-modal approach enables simultaneous detection and fine-grained temporal localization of partially manipulated AI-generated video forgeries. Extensive experiments show that this approach outperforms existing state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种用于AI生成视频检测与定位的多模态伪造表示学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着生成式AI（如Sora、Veo等）的普及，创作高质量视频的门槛大幅降低，但也带来了严重的伪造风险。
*   **现有挑战**：目前的检测方法大多局限于单一模态（仅视觉或仅音频），或者虽然涉及多模态但仅能进行粗粒度的二分类（判断全段真假），无法应对“部分篡改”（Partial Manipulation）的威胁。
*   **研究动机**：在实际场景中，攻击者可能只修改视频中的几秒钟或某个特定语义片段。因此，迫切需要一种能够整合视觉、音频及语义信息，并能实现**细粒度时间定位**（Localization）的检测工具。

### 2. 论文提出的方法论
论文提出了一个包含三个互补分支的多分支框架，通过动态融合实现检测与定位：

*   **LMM 语义分支（LMM Branch）**：
    *   利用大语言模型（LLaVA架构）提取高层语义特征。
    *   通过特定的法证指令（Forensic Instruction）引导模型描述光照、纹理、一致性等，捕捉语义层面的不一致性。
*   **时空视觉分支（ST Branch）**：
    *   **重构引导**：使用 VQ-VAE 重构帧，通过原始帧与重构帧的差异放大生成痕迹。
    *   **IAFA 机制**：采用“帧内与跨帧注意力”（In-and-Across Frame Attention）的混合 ViT，同时捕捉局部空间伪造和全局时间不一致。
*   **多尺度局部伪造音频分支（PS Branch）**：
    *   基于 Wav2Vec 2.0 提取特征，并使用 gMLP 评分头在多个时间分辨率（160ms, 320ms, 640ms）上计算伪造得分，以捕捉极短的合成语音片段。
*   **融合与定位机制**：
    *   **动态融合**：将各分支的逻辑值（Logits）通过线性模块融合，输出帧级置信度。
    *   **定位策略**：视觉分支采用滑动窗口，LMM分支采用稀疏采样加插值，音频分支原生支持细粒度输出，最终实现对伪造片段的精确对齐。
*   **训练策略**：采用 **One-Cycle 学习率策略**，以加快收敛并帮助优化器跳出损失平坦区。

### 3. 实验设计
*   **数据集**：
    *   **AV-Deepfake1M++**：主要基准，包含大量极短（平均320ms）的局部篡改片段，极具挑战性。
    *   **FakeAVCeleb**：用于跨数据集的零样本（Zero-shot）泛化能力测试。
*   **Benchmark 与对比方法**：
    *   对比了 **MM-Det**（文本+视觉）、**UMMAFormer**（视觉+音频）、**BA-TFD/BA-TFD+**（视觉+音频）等最先进的多模态模型。
*   **评价指标**：视频级 AUC、片段级 AUC（Seg）、平均精度（AP）和平均召回率（AR）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到了使用了预训练的 CLIP 视觉编码器、Wav2Vec 2.0 和 LLaVA 权重，并开源了代码。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模基准测试（AV-Deepfake1M++）。
    *   **消融实验**：分别验证了文本（LMM）、视觉、音频分支以及 One-Cycle 调度器的贡献，证明了各组件的必要性。
    *   **跨数据集验证**：在 FakeAVCeleb 上进行了测试，验证了模型的通用性。
*   **充分性评价**：实验设计较为全面，涵盖了检测、定位、泛化性和组件分析。通过可视化不同伪造场景（如仅音频伪造、仅视觉伪造），客观地展示了多模态融合在处理“单模态失效”时的鲁棒性。

### 6. 论文的主要结论与发现
*   **多模态协同至关重要**：整合 LMM 语义特征能显著提升对复杂伪造的识别能力，尤其是在视觉痕迹不明显时。
*   **定位精度领先**：该方法在 AP@0.5 指标上超过了之前的 SOTA 方法（BA-TFD+）约 34%，证明了其在细粒度时间定位上的卓越性能。
*   **处理短片段能力强**：即使是 320ms 的极短篡改，该模型也能通过多尺度音频分析和时空视觉捕捉有效识别。

### 7. 优点
*   **多维度建模**：不仅关注像素级和声学级特征，还引入了 LMM 进行语义推理，使检测结果更具解释性。
*   **细粒度定位**：解决了传统方法只能做“全段二分类”的痛点，适应实际应用中局部篡改的检测需求。
*   **鲁棒性强**：通过动态融合机制，即使某一模态被规避（如视觉极其逼真），也能通过其他模态（如音频或语义矛盾）发现破绽。

### 8. 不足与局限
*   **两阶段训练**：目前视觉和音频分支是分开训练后再融合的，并非完全的端到端（End-to-End）训练，这可能限制了模态间深层交互的优化。
*   **计算复杂度**：引入 LMM 和多尺度音频分支虽然提升了精度，但也增加了推理时的计算开销，对于实时检测场景可能存在挑战。
*   **硬件细节缺失**：未提供训练资源消耗的具体数据，不便于其他研究者评估复现成本。

（完）
