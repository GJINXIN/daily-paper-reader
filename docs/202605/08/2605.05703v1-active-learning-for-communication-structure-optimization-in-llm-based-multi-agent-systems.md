---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 基于大语言模型的多智能体系统通信结构优化
tldr: 针对大语言模型多智能体系统（LLM-MAS）通信结构优化中随机采样任务效率低、不稳定的问题，本文提出一种基于主动学习的任务选择框架。该框架利用集成卡尔曼反演估计任务的信息量，通过识别最具价值的任务来优化通信拓扑。实验证明，该方法在有限预算下能显著提升系统性能并增强鲁棒性。
selection_source: fresh_fetch
motivation: 现有的通信结构优化方法依赖随机采样，忽略了不同任务在难度和领域上的差异，导致在有限预算下优化效果不稳定。
method: 提出一种基于集成卡尔曼反演的信息论任务选择框架，结合嵌入表示选择和批量汤普森采样，主动识别对优化通信结构最有价值的任务。
result: 在良性环境和存在智能体攻击的场景下，该方法均能以更低的计算成本实现更优的通信结构优化。
conclusion: 主动学习能有效提升LLM多智能体系统通信结构的优化效率和稳定性，是解决黑盒复杂系统优化问题的有效途径。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 Token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相等，这使得在有限的训练预算下，优化过程往往不稳定且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论驱动的任务选择框架。该方法通过评估候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（Ensemble Kalman Inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒且具有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型和批量汤普森采样相结合。我们在良性设置和存在智能体攻击的设置下验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

### 基于大语言模型的多智能体系统通信结构优化的主动学习：论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：优化大语言模型多智能体系统（LLM-MAS）的通信结构（即智能体之间“谁与谁通信”）可以显著提升任务性能并降低 Token 成本。
*   **核心问题**：现有的通信结构优化方法通常依赖于从任务池中**随机采样**训练任务。然而，不同任务在难度和领域上差异巨大，对优化通信结构的贡献（信息量）并不相等。在有限的训练预算下，随机采样会导致优化过程不稳定、鲁棒性差，且容易受到特定训练集偏差的影响。
*   **研究动机**：探索是否可以通过**主动学习（Active Learning）**识别出对通信结构优化最具“信息量”的任务，从而在极低预算下实现高效、稳定的结构优化。

#### 2. 方法论
该论文提出了一种基于集成的、无导数的信息论主动学习框架，主要包含以下关键技术：
*   **核心思想**：利用**信息增益（Information Gain）**来衡量任务的价值。即：如果一个任务能显著改变通信结构参数（Logits）的后验分布，则该任务具有高信息量。
*   **集成卡尔曼反演（EKI）**：由于 LLM-MAS 是不可微的黑盒系统，论文引入 EKI 作为贝叶斯更新的近似。通过维护一个参数粒子集成（Ensemble），利用前向预测的协方差来近似后验分布，从而避免了复杂的梯度计算。
*   **两阶段选择策略**：
    1.  **代表性选择（Representative Selection）**：利用文本嵌入（Embedding）和贪婪最远点采样，从原始大任务池中筛选出具有多样性的候选子集。
    2.  **信息量选择（Informative Selection）**：在候选子集中，利用 EKI 估计每个任务的信息增益。
*   **可扩展性优化**：引入**代理模型（Surrogate Modeling）**和**批量汤普森采样（Batch Thompson Sampling）**。通过高斯过程（GP）拟合任务嵌入与信息增益之间的关系，减少昂贵的 MAS 实际运行次数。

#### 3. 实验设计
*   **数据集**：
    *   **MMLU**：大规模多任务语言理解基准（涵盖法律、医学、历史等）。
    *   **GSM8K**：小学数学应用题数据集。
*   **实验场景**：
    *   **良性设置（Benign）**：所有智能体正常协作。
    *   **对抗设置（Adversarial/Attack）**：系统中存在恶意智能体（Fake Agents），故意提供错误推理和误导信息。
*   **对比方法（Baselines）**：
    *   **No Train**：全连接的初始结构。
    *   **Random**：随机选择任务进行训练（主流方法）。
    *   **EGL/EMC**：基于梯度长度或模型变化的传统主动学习指标。
    *   **Fisher Coreset**：基于费舍尔信息矩阵的协同集选择方法。

#### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4.1-nano** 作为智能体和决策者的基础模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于主要基于 API 调用和推理，其核心成本体现在 Token 使用量上。论文通过“Token 成本 vs. 准确率”曲线展示了方法的经济性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了多次独立运行（Run Counts 见附录，通常每组实验重复 10-20 次以上）。
    *   提供了 **Bootstrap 95% 置信区间**，增强了结果的可信度。
*   **消融实验**：对“代表性选择”和“信息量选择”分别进行了消融，验证了各模块的贡献。
*   **敏感性分析**：研究了集成规模（Ensemble Size）和 EKI 迭代步数对结果的影响。
*   **评价**：实验设计较为充分，涵盖了从标准性能到极端对抗环境的测试，对比基准选择合理，统计分析较为严谨。

#### 6. 主要结论与发现
*   **稳定性提升**：在有限预算下，主动学习显著降低了优化结果的方差。在 MMLU 的最差 25% 运行中，准确率比随机采样高出 **1.30%**（良性）和 **4.25%**（攻击场景）。
*   **性能增强**：在对抗环境下，该方法能更有效地识别并切断恶意智能体的通信路径，平均准确率提升显著（MMLU +1.45%，GSM8K +0.93%）。
*   **成本效益**：在相同的 Token 预算下，主动学习选出的任务能训练出更优的通信拓扑，证明了“任务质量胜过任务数量”。

#### 7. 优点
*   **无导数优化**：针对 LLM-MAS 的黑盒特性，巧妙地应用 EKI 解决了无法直接求导的问题。
*   **鲁棒性强**：特别是在对抗环境下，能够精准识别出有助于“去伪存真”的关键任务。
*   **模块化设计**：该框架可以作为插件应用于现有的各种 MAS 结构优化算法之上。

#### 8. 不足与局限
*   **标签依赖**：目前的方法假设训练池中的任务都有标准答案（用于计算性能得分），在完全无标签的场景下适用性受限。
*   **集成规模限制**：虽然实验证明小规模集成（J=6）有效，但在参数维度极高或后验分布极度复杂时，EKI 的近似精度可能下降。
*   **计算开销**：虽然减少了训练任务数，但评估任务信息量本身需要多次 MAS 运行（前向评估），存在一定的初始选样成本。

（完）
