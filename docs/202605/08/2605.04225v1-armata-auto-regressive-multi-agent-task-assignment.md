---
title: "ARMATA: Auto-Regressive Multi-Agent Task Assignment"
title_zh: ARMATA：自回归多智能体任务分配
authors: "Yazan Youssef, Aboelmagd Noureldin, Sidney Givigi"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04225v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 用于多智能体任务分配与协作的自回归框架
tldr: "针对空间分布的多智能体系统协调难题，本文提出 ARMATA 框架。传统方法通常将区域分配与路径规划解耦，导致忽略阶段间依赖或缺乏全局视野。ARMATA 采用集中式端到端自回归架构，通过创新的多阶段解码机制，在单次推理中统一生成分配决策与路由序列。该方法能隐式平衡工作负载与路径效率，避免局部最优。实验表明，ARMATA 在解质量上比 Google OR-Tools 等工业求解器提升达 20%，且将计算时间从数小时缩短至秒级，实现了效率与质量的双重突破。"
selection_source: fresh_fetch
motivation: 解决多智能体任务分配中分配与路径规划阶段解耦或缺乏全局上下文导致的效率低下问题。
method: 提出一种集中式的端到端自回归框架，通过多阶段解码机制在单次推理中同时生成分配决策和访问序列。
result: "在实验中比 Google OR-Tools 和 CPLEX 等工业求解器提升了高达 20% 的解质量，且计算时间从小时级缩短至秒级。"
conclusion: 该研究证明了通过集中式全局状态统一处理分配与路径规划，能有效避免局部最优并显著提升大规模多智能体系统的协调效率。
---

## 摘要
在空间分布区域协调多智能体系统需要解决一个复杂的层次化问题：首先在智能体之间分配区域（分配），随后确定最优访问顺序（路径规划）。现有方法通常将这些阶段解耦，忽略了阶段间的依赖关系，或者依赖于缺乏全局上下文的去中心化启发式算法。在这项工作中，我们提出了一个中心化的、完全端到端的自回归框架，能够联合生成分配决策和路径序列。我们方法的核心贡献是一个多阶段解码机制，它在保持中心化全局状态的同时，在单次自回归过程中统一了高层分配和底层路径规划。这使得模型能够隐式地平衡工作负载分布与路径规划效率，从而避免去中心化方法中常见的局部最优。大量实验表明，我们的方法显著优于各种基准模型，与 Google OR-Tools、IBM CPLEX 和 LKH-3 等工业求解器相比，解的质量提升高达 20%，同时将计算时间从数小时缩短至数秒。

## Abstract
Coordinating multi-agent systems over spatially distributed areas requires solving a complex hierarchical problem: first distributing areas among agents (allocation) and subsequently determining the optimal visitation order (routing). Existing methods typically decouple these stages ignoring inter-stage dependencies or rely on decentralized heuristics that lack global context. In this work, we propose a centralized, fully end-to-end auto-regressive framework that jointly generates allocation decisions and routing sequences. The core contribution of our approach is a multi-stage decoding mechanism that unifies high-level allocation and low-level routing in a single autoregressive pass while maintaining a centralized global state. This enables the model to implicitly balance workload distribution with routing efficiency, avoiding local optima common in decentralized methods. Extensive experiments demonstrate that our method significantly outperforms diverse baselines, achieving up to a 20\% improvement in solution quality over industrial solvers such as Google OR-Tools, IBM CPLEX, and LKH-3, while reducing computation time from hours to seconds.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ARMATA**（Auto-Regressive Multi-Agent Task Assignment）的新型框架，旨在解决多智能体系统在空间分布任务中的分配与路径规划联合优化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多智能体任务分配与路由（MATAR）是一个复杂的层次化 NP-hard 问题，包含“任务分配”（谁去哪）和“路径规划”（按什么顺序去）两个阶段。
*   **研究动机**：
    *   **阶段解耦缺陷**：传统方法通常将分配与路由分开处理，忽略了两者间的紧密耦合，容易陷入局部最优。
    *   **计算效率瓶颈**：精确求解器（如 CPLEX）在处理大规模问题时耗时极长（数小时甚至数天）；启发式算法虽然快，但缺乏全局上下文且难以利用历史经验。
    *   **去中心化局限**：现有的学习方法多采用去中心化架构，受限于局部观测，难以实现全局最优的工作负载平衡。

### 2. 方法论：ARMATA 框架
ARMATA 采用**中心化、完全端到端、自回归**的架构，在单次推理中同时完成分配与路由。
*   **核心思想**：将任务分配建模为序列生成问题，通过自回归方式逐个为智能体生成决策，并利用全局状态信息维持协调。
*   **关键技术细节**：
    *   **双重编码器（Encoder）**：
        *   **节点编码器**：使用门控图卷积网络（Gated GCN）提取待扫描区域的几何特征和空间拓扑关系。
        *   **智能体编码器**：同样使用 Gated GCN 提取智能体之间的相对位置特征，增强模型对智能体分布的理解。
    *   **三阶段解码器（Decoder）**：通过三个连续的 Transformer 网络进行决策：
        1.  **网络 I**：决定当前智能体分配哪一个区域。
        2.  **网络 II**：确定进入该区域的起始点（角点）。
        3.  **网络 III**：选择在该区域内的扫描移动模式。
    *   **训练算法**：使用强化学习（RL）进行训练，主要采用 **REINFORCE** 算法，并对比了 **PPO** 算法。目标函数是最小化所有智能体的总行驶距离。

### 3. 实验设计
*   **应用场景**：以搜索与救援（SAR）任务为代表，智能体需扫描多个地理区域。
*   **数据集**：在单位正方形内随机生成智能体位置和区域坐标。
    *   **训练集**：每轮（Epoch）生成 128,000 个样本，涵盖 5-10 个智能体和 35-100 个区域。
    *   **测试集**：分为“范围内”（In-range，如 5 智能体/40 区域）和“范围外”（Out-of-range，如 12 智能体/180 区域）以测试泛化性。
*   **Benchmark（基准方法）**：
    *   **工业求解器**：Google OR-Tools, IBM CPLEX。
    *   **先进元启发式算法**：LKH-3（Lin-Kernighan-Helsgaun-3）。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA GeForce RTX-3090 GPU** 和 Intel i9 11900KF CPU 的工作站。
*   **软件框架**：PyTorch 2.1，环境基于 OpenAI Gym 开发。
*   **训练时长**：模型训练了 100 个 Epoch，每个 Epoch 包含 12.8 万个样本。虽然文中未给出总训练小时数，但从样本量和模型参数量（约 36 万）推断，训练强度较大。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度的对比实验，包括不同规模的任务实例（从 24 区域到 180 区域）。
*   **消融实验**：专门测试了“禁用智能体编码器”的影响，证明了智能体特征对减少最优性差距（约 20%）的关键作用。
*   **算法对比**：对比了 REINFORCE 和 PPO 在固定/变化起始位置下的表现。
*   **充分性评价**：实验设计较为全面，不仅对比了结果质量，还对比了计算时间，并验证了模型在未见过的更大规模问题上的泛化能力，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **解质量突破**：ARMATA 在解的质量上比 Google OR-Tools、CPLEX 和 LKH-3 提升了 **14% 至 26%**。
*   **计算效率飞跃**：计算时间实现了 **4 个数量级** 的缩减。在 CPLEX 需要 400 秒、LKH-3 需要 150 秒的复杂实例中，ARMATA 仅需 **0.0165 秒** 即可生成高质量解。
*   **强泛化性**：模型在超出训练分布的大规模实例（如 180 个区域）上依然保持了优异的性能，解决了以往学习方法难以扩展的问题。

### 7. 优点与亮点
*   **端到端联合优化**：打破了分配与路由的壁垒，通过多阶段解码机制实现了真正的联合决策。
*   **实时性极强**：亚秒级的推理速度使其非常适合动态环境下的实时重规划。
*   **自回归冲突规避**：通过顺序决策机制，模型在生成过程中自然地避免了任务分配冲突，无需复杂的后处理掩码。

### 8. 不足与局限
*   **解码顺序固定**：自回归解码通常依赖于预定义的智能体处理顺序，这可能在某些极端非对称场景下限制搜索空间。
*   **仿真与现实差距**：实验基于单位正方形的简化几何环境，未充分考虑现实中的障碍物、通信延迟或动力学约束（如无人机的转弯半径）。
*   **任务同质性**：实验假设所有区域的扫描任务性质相似，未探讨具有不同优先级或异构任务类型的复杂场景。

（完）
