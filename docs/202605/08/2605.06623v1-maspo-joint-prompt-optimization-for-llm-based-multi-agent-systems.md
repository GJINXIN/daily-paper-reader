---
title: "MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems"
title_zh: MASPO：基于大语言模型的多智能体系统联合提示词优化
authors: "Zhexuan Wang, Xuebo Liu, Li Wang, Zifei Shan, Yutong Wang, Zhenxi Song, Min Zhang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06623v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: LLM多智能体系统的联合提示词优化
tldr: 针对基于大语言模型的多智能体系统（MAS）中提示词优化困难的问题，本文提出MASPO框架。该框架通过联合评估机制，不仅关注单个智能体的局部表现，更强调其对下游智能体及全局目标的贡献。结合数据驱动的进化束搜索算法，MASPO能自动且迭代地优化整个系统的提示词，有效解决了局部与全局目标不一致的挑战，显著提升了复杂协作任务的性能。
selection_source: fresh_fetch
motivation: 多智能体系统中局部智能体目标与全局系统目标之间存在不一致，导致难以协同优化各角色的提示词。
method: 引入联合评估机制来衡量提示词对下游任务的贡献，并采用数据驱动的进化束搜索在提示词空间中进行高效迭代。
result: 在6个不同任务的实验中，MASPO一致优于现有最先进的提示词优化方法，平均准确率提升了2.9。
conclusion: MASPO证明了通过联合优化多智能体提示词可以有效弥合局部交互与全局结果之间的差距，提升系统整体协作能力。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在处理复杂协作任务方面展现出巨大潜力，其中智能体通常通过特定角色的提示词进行编排。虽然这些提示词的质量至关重要，但在相互协作的智能体之间进行联合优化仍是一项艰巨的挑战，这主要是由于局部智能体目标与整体系统目标之间的不一致。为解决这一问题，我们提出了 MASPO，这是一个旨在自动且迭代地优化整个系统提示词的新型框架。MASPO 的核心创新在于其联合评估机制，该机制不仅根据提示词的局部有效性进行评估，还根据其促进后续智能体下游成功的能力进行评估。这在不依赖地面真值（ground-truth）标签的情况下，有效地弥合了局部交互与全局结果之间的差距。此外，MASPO 采用数据驱动的进化束搜索，以高效地在多维提示词空间中进行搜索。在 6 个不同任务上的广泛实证评估表明，MASPO 一致优于最先进的提示词优化方法，平均准确率提升了 2.9。我们在 https://github.com/wangzx1219/MASPO 发布了代码。

## Abstract
Large language model (LLM)-based Multi-agent systems (MAS) have shown promise in tackling complex collaborative tasks, where agents are typically orchestrated via role-specific prompts. While the quality of these prompts is pivotal, jointly optimizing them across interacting agents remains a non-trivial challenge, primarily due to the misalignment between local agent objectives and holistic system goals. To address this, we introduce MASPO, a novel framework designed to automatically and iteratively refine prompts across the entire system. A core innovation of MASPO is its joint evaluation mechanism, which assesses prompts not merely by their local validity, but by their capacity to facilitate downstream success for successor agents. This effectively bridges the gap between local interactions and global outcomes without relying on ground-truth labels. Furthermore, MASPO employs a data-driven evolutionary beam search to efficiently navigate the high-dimensional prompt space. Extensive empirical evaluations across 6 diverse tasks demonstrate that MASPO consistently outperforms state-of-the-art prompt optimization methods, achieving an average accuracy improvement of 2.9. We release our code at https://github.com/wangzx1219/MASPO.

---

## 论文详细总结（自动生成）

这是一份关于论文《MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多智能体系统（MAS）通过编排多个大模型智能体协作来解决复杂任务，但其性能高度依赖于每个智能体的提示词（Prompt）。
*   **核心挑战**：
    *   **局部与全局失配（Local-Global Misalignment）**：中间智能体可能完美执行了局部指令，但生成的输出却误导了下游智能体，导致系统整体失败。
    *   **信用分配难题（Credit Assignment）**：中间步骤缺乏地面真值（Ground-truth）标签，难以评估单个智能体对最终结果的贡献。
    *   **非平稳性（Non-stationarity）**：一个智能体提示词的改变会改变下游智能体的输入分布，导致联合优化极其困难。
*   **研究目的**：开发一个能够自动、迭代且联合优化整个 MAS 提示词的框架，解决协作中的交互瓶颈。

### 2. 方法论
MASPO 框架由三个核心支柱组成：
*   **多粒度联合评估机制（Joint Evaluation）**：
    *   不再只看局部输出，而是通过三个维度打分：**局部有效性**（是否符合角色约束）、**前瞻潜力**（Lookahead Potential，是否对直接下游智能体有帮助）和**全局对齐**（是否提升了最终系统输出）。
    *   公式化表示为这三者的加权组合，利用一个强模型（如 Gemini）作为评估器进行偏好判断。
*   **失配驱动的生成式搜索（Misalignment-Aware Sampling）**：
    *   专门挖掘“局部成功但全局失败”的案例（即失配案例），将其作为“硬负样本”注入优化器的上下文。
    *   引导优化器诊断并修复特定的交互崩溃，而非盲目修改。
*   **自适应进化束搜索（Evolutionary Beam Search with Beam Refresh）**：
    *   采用**坐标上升法**：按拓扑顺序逐个优化智能体。
    *   **束刷新机制（Beam Refresh）**：当上游智能体更新后，下游智能体保留的候选提示词分数会“过期”。MASPO 会重新评估这些候选词，以应对输入分布的偏移（协变量偏移）。

### 3. 实验设计
*   **数据集/场景**：涵盖三大领域共 6 个基准测试：
    *   **数学**：MATH-500, AGIEval-MATH, AQuA。
    *   **复杂推理**：GPQA-Diamond。
    *   **代码生成**：MBPP, HumanEval-ET。
*   **基准架构**：对比了**顺序结构（Sequential）**和**分层结构（Hierarchical）**两种 MAS 拓扑。
*   **对比方法**：
    *   **基础方法**：Vanilla, CoT, Self-Refine, AgentDropout。
    *   **优化算法**：TPE（用于 MIPRO/MASS 等）、SPO（单智能体提示词优化器）。
*   **模型配置**：智能体骨干模型为 **Qwen3-8B**，优化器和评估器使用 **Gemini-2.5-Pro**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理成本控制**：论文提到优化过程仅需几十个无标签样本（样本池大小为 50，每轮采样 10 个），旨在降低计算开销和数据标注需求。
*   **API 使用**：由于使用了 Gemini 作为优化和评估引擎，主要的资源消耗体现在 API 调用成本上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个数据集上进行了主实验。
    *   进行了 **8 个维度的消融实验**（包括搜索策略、调度策略、联合评估、失配采样等）。
    *   进行了**敏感性分析**（如权重分配、前瞻深度、计算预算）。
    *   进行了**跨模型迁移性测试**（将优化后的提示词迁移至 DeepSeek-V3, GLM-4.6, Claude-Sonnet-4 等）。
*   **充分性评价**：实验设计非常全面且严谨。通过控制变量法（如在相同 Gemini 预算下对比 SPO）确保了公平性，跨模型迁移实验证明了该方法的鲁棒性和客观性。

### 6. 主要结论与发现
*   **性能提升**：MASPO 在所有任务上均优于现有最先进方法，平均准确率提升 **2.9**。
*   **失配修复**：实验证明显式注入失配案例能显著降低系统的“失配率”，有效解决了协作瓶颈。
*   **拓扑无关性**：无论是在顺序还是分层架构中，MASPO 都能提供稳定的增益。
*   **提示词迁移性**：在小模型（Qwen3-8B）上优化出的提示词，直接迁移到大模型（如 Claude 或 Gemini）上依然有效，证明了其捕捉到了通用的协作逻辑。

### 7. 优点
*   **创新的评估视角**：引入“前瞻潜力”，解决了 MAS 中长期存在的信用分配难题。
*   **针对性强**：失配驱动的采样机制比随机搜索更高效，能精准定位系统薄弱环节。
*   **动态适应**：束刷新机制有效应对了多智能体环境中的非平稳性问题。
*   **实用性高**：不需要地面真值标签，仅需少量无标签数据即可运行。

### 8. 不足与局限
*   **依赖强模型**：优化和评估过程高度依赖高性能的骨干模型（如 Gemini），在完全本地化或资源受限的环境下可能受限。
*   **搜索空间限制**：目前仅针对自然语言提示词进行离散优化，尚未涉及模型参数或更复杂的通信协议优化。
*   **计算延迟**：迭代式的束搜索和多维度评估虽然样本量小，但在优化阶段仍存在较高的推理延迟和 API 开销。

（完）
