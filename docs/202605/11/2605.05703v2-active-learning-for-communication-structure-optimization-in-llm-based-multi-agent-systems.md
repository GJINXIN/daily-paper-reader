---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v2"
tags: ["query:mas-ss"]
score: 8.0
evidence: 基于大语言模型的多智能体系统通信结构优化
tldr: 针对大语言模型多智能体系统（LLM-MAS）通信结构优化中随机采样任务效率低、不稳定的问题，本文提出一种基于主动学习的任务选择框架。该框架利用集成卡尔曼反演估计任务的信息量，通过识别最具价值的任务来优化通信拓扑。实验证明，该方法在有限预算下能显著提升系统性能并增强鲁棒性。
selection_source: fresh_fetch
motivation: 现有的通信结构优化方法依赖随机采样，忽略了不同任务在难度和领域上的差异，导致在有限预算下优化效果不稳定。
method: 提出一种基于集成卡尔曼反演的信息论任务选择框架，结合嵌入表示选择和批量汤普森采样，主动识别对优化通信结构最有价值的任务。
result: 在良性环境和存在智能体攻击的场景下，该方法均能以更低的计算成本实现更优的通信结构优化。
conclusion: 主动学习能有效提升LLM多智能体系统通信结构的优化效率和稳定性，是解决黑盒复杂系统优化问题的有效途径。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相同，这使得在有限训练预算下的优化往往不稳定，且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论的任务选择框架。该方法通过衡量候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（ensemble Kalman inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒且含有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型和批量汤普森采样相结合。我们在良性设置和存在智能体攻击的设置下验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

以下是对论文《Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型多智能体系统（LLM-MAS）的通信结构优化**。
*   **背景**：通过优化智能体之间的通信图（哪些智能体该说话、什么时候说），可以显著提升系统性能并减少 Token 消耗。
*   **核心问题**：现有的优化方法通常依赖于从任务池中**随机采样**训练任务。然而，不同任务的难度和领域差异巨大，随机采样在有限预算下会导致优化过程极不稳定，且对特定训练集高度敏感。
*   **研究动机**：能否通过**主动学习（Active Learning）**，在有限的计算预算内，识别出对优化通信结构“信息量最大”的任务，从而提高优化的效率、稳定性和鲁棒性？

### 2. 方法论：核心思想与关键技术
论文提出了一种基于集成的、信息论的主动学习框架，主要包含以下三个阶段：

*   **代表性选择（Representative Selection）**：
    *   利用文本嵌入（Embeddings）将任务向量化。
    *   采用贪婪最远点采样（Greedy Farthest-Point Selection）从原始任务池中筛选出一个较小的“代表性任务池”，确保任务的多样性。
*   **信息量评估（Informative Selection - 核心创新）**：
    *   **核心指标**：使用**信息增益（Information Gain）**，即任务观测前后通信图参数分布的 KL 散度。
    *   **关键技术**：引入**集成卡尔曼反演（Ensemble Kalman Inversion, EKI）**。由于 LLM-MAS 是黑盒、非奇异且含有噪声的系统，直接计算梯度非常困难。EKI 提供了一种无导数（Derivative-free）的近似方法，通过维护一个参数集成（Ensemble）来模拟贝叶斯后验更新，从而高效估算每个任务的信息增益。
*   **代理模型与批量采样（Surrogate Modeling）**：
    *   为了避免评估所有候选任务，使用高斯过程（GP）作为代理模型来预测任务的信息增益。
    *   结合**汤普森采样（Thompson Sampling）**进行序列化决策，进一步降低评估成本。

### 3. 实验设计
*   **数据集**：
    *   **MMLU**：大规模多任务语言理解基准（涵盖法律、医学、人文等）。
    *   **GSM8K**：小学数学应用题数据集（侧重逻辑推理）。
*   **实验场景**：
    *   **良性设置（Benign）**：所有智能体正常协作。
    *   **对抗设置（Adversarial/Attack）**：部分智能体被设定为“攻击者”，故意提供错误推理和误导信息。
*   **对比方法（Baselines）**：
    *   **Random Training**：随机选择任务进行训练（主要基准）。
    *   **EGL (Expected Gradient Length)**：基于梯度长度的任务选择。
    *   **EMC (Expected Model Change)**：基于模型参数变化量的选择。
    *   **Fisher-based Coreset**：基于费舍尔信息矩阵的协同集选择。

### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4.1-nano** 作为智能体和决策者的底层模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **成本衡量**：论文主要通过 **Token 消耗量**（百万级别）来衡量计算成本，并展示了在相同 Token 预算下，主动学习比随机训练能获得更高的准确率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个设置进行了多次独立运行（每组实验通常包含 10 到 23 次独立运行），并提供了标准差和四分位数（Q1）分析。
    *   进行了详细的**消融实验**，验证了“代表性选择”和“信息量选择”各自的贡献。
    *   进行了**敏感性分析**，探讨了集成规模（Ensemble Size）和 EKI 迭代步数对结果的影响。
*   **充分性评价**：实验设计较为充分，不仅关注平均准确率，还特别分析了“最差 25% 运行”的表现，证明了该方法在提升系统**稳定性**和**尾部鲁棒性**方面的显著作用。

### 6. 主要结论与发现
*   **稳定性提升**：在有限预算下，随机训练会导致优化结果波动巨大。主动学习显著降低了这种不确定性，减少了优化失败的概率。
*   **性能优越**：在对抗场景下，主动学习的表现尤为突出。在 MMLU 攻击设置中，平均准确率比随机训练提升了 **1.45%**，在 GSM8K 中提升了 **0.93%**。
*   **鲁棒性增强**：优化后的通信结构能有效识别并抑制攻击智能体的影响（例如切断从攻击智能体到正常智能体的通信路径）。
*   **成本效益**：虽然主动学习增加了前期的任务选择成本，但通过优化后的结构在推理阶段节省的 Token 以及提升的准确率，使得总成本效益更高。

### 7. 优点与亮点
*   **无导数优化**：巧妙地将 EKI 引入 LLM-MAS 领域，解决了复杂黑盒系统难以进行信息论评估的痛点。
*   **关注稳定性**：不同于多数论文只看平均分，本文深入探讨了优化过程的“不稳定性”问题，具有很强的工程实践意义。
*   **两阶段策略**：结合了嵌入表示的“多样性”和信息论的“精确性”，兼顾了可扩展性和评估精度。

### 8. 不足与局限
*   **集成规模限制**：由于计算成本限制，EKI 的集成规模较小（J=6），在处理极高维度的参数空间或极其复杂的后验分布时，可能存在近似偏差。
*   **标签依赖**：目前的方法假设候选任务的正确答案是已知的。如果是在完全无标签的环境下进行任务选择，则需要引入更复杂的期望信息增益（EIG）计算。
*   **泛化性验证**：实验主要集中在 6 个智能体的规模上，对于更大规模（如数十个智能体）的系统，其计算开销和优化难度尚待进一步验证。

（完）
