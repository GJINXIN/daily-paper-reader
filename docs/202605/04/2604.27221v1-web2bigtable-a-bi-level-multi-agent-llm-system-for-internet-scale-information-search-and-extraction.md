---
title: "Web2BigTable: A Bi-Level Multi-Agent LLM System for Internet-Scale Information Search and Extraction"
title_zh: Web2BigTable：一种用于互联网规模信息搜索与提取的双层多智能体大语言模型系统
authors: "Yuxuan Huang, Yihang Chen, Zhiyuan He, Yuxiang Chen, Ka Yiu Lee, Huichi Zhou, Weilin Luo, Meng Fang, Jun Wang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27221v1"
tags: ["query:mas-ss"]
score: 8.0
evidence: 用于信息搜索的双层多智能体系统
tldr: Web2BigTable是一个双层多智能体框架，旨在解决互联网规模下的深度推理和广度结构化信息提取难题。它通过上层协调器分解任务与下层执行器并行处理，结合闭环验证、外部存储和共享工作区，实现了高效的搜索与数据聚合。该系统在WideSearch和XBench-DeepSearch基准测试中均取得SOTA性能，显著提升了复杂Web搜索任务的成功率和准确性。
selection_source: fresh_fetch
motivation: 现有Web搜索智能体在处理需要跨实体一致性的广度聚合任务和长路径深度推理任务时表现不佳。
method: 提出一种双层架构，利用上层协调器分解任务，下层智能体通过共享工作区和闭环验证机制并行执行并动态优化。
result: "在WideSearch基准上，成功率达到38.50%（是次优系统的7.5倍），并在深度搜索任务中达到73.0%的准确率。"
conclusion: Web2BigTable通过多智能体协同和自进化机制，有效解决了大规模互联网信息搜索与结构化提取的广度与深度挑战。
---

## 摘要
智能体网络搜索日益面临两种截然不同的需求：针对单一目标的深度推理，以及跨多个实体和异构来源的结构化聚合。当前系统在这两个方面都面临挑战。面向广度的任务要求具有广泛覆盖范围和跨实体一致性的模式对齐输出，而面向深度的任务则需要对长且具有分支的搜索轨迹进行连贯推理。我们推出了 Web2BigTable，这是一个支持上述两种模式的“网络到表格”搜索多智能体框架。Web2BigTable 采用双层架构，其中上层编排器将任务分解为子问题，下层工作智能体并行解决这些问题。通过“运行-验证-反思”的闭环过程，该框架利用持久且人类可读的外部存储器，随着时间的推移共同改进任务分解和执行，并对每个单一智能体进行自我进化更新。在执行过程中，工作智能体通过共享工作区进行协作，使部分发现可见，从而减少冗余探索、调和冲突证据，并适应新出现的覆盖缺口。Web2BigTable 在 WideSearch 上刷新了最先进水平，Avg@4 成功率达到 38.50（是排名第二的 5.10 的 7.5 倍），行 F1 分数为 63.53（比第二名高出 25.03），项目 F1 分数为 80.12（比第二名高出 14.42）。它还泛化到了 XBench-DeepSearch 上的深度导向搜索，达到了 73.0 的准确率。代码可在 https://github.com/web2bigtable/web2bigtable 获取。

## Abstract
Agentic web search increasingly faces two distinct demands: deep reasoning over a single target, and structured aggregation across many entities and heterogeneous sources. Current systems struggle on both fronts. Breadth-oriented tasks demand schema-aligned outputs with wide coverage and cross-entity consistency, while depth-oriented tasks require coherent reasoning over long, branching search trajectories. We introduce \textbf{Web2BigTable}, a multi-agent framework for web-to-table search that supports both regimes. Web2BigTable adopts a bi-level architecture in which an upper-level orchestrator decomposes the task into sub-problems and lower-level worker agents solve them in parallel. Through a closed-loop run--verify--reflect process, the framework jointly improves decomposition and execution over time via persistent, human-readable external memory, with self-evolving updates to each single-agent. During execution, workers coordinate through a shared workspace that makes partial findings visible, allowing them to reduce redundant exploration, reconcile conflicting evidence, and adapt to emerging coverage gaps. Web2BigTable sets a new state of the art on WideSearch, reaching an Avg@4 Success Rate of \textbf{38.50} ($7.5\times$ the second best at 5.10), Row F1 of \textbf{63.53} (+25.03 over the second best), and Item F1 of \textbf{80.12} (+14.42 over the second best). It also generalises to depth-oriented search on XBench-DeepSearch, achieving 73.0 accuracy. Code is available at https://github.com/web2bigtable/web2bigtable.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Web2BigTable** 的双层多智能体系统，旨在解决大规模互联网信息的搜索与结构化提取难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的智能体网络搜索面临两类需求：**深度搜索**（针对单一复杂问题的多步推理）和**广度搜索**（跨大量实体和异构来源的结构化聚合，即“Web-to-Table”）。
*   **核心挑战**：
    *   **上下文饱和**：单智能体在处理数百行数据的提取时，上下文窗口会迅速溢出。
    *   **错误累积**：初始规划往往无法适应搜索过程中发现的新情况。
    *   **冗余与缺口**：多智能体协作时，缺乏有效的协调机制来避免重复劳动或填补信息空白。
*   **整体含义**：论文提出了一种能够通过外部存储器自我进化、且具备异步协作能力的双层架构，旨在同时兼顾搜索的深度与广度。

### 2. 方法论：核心思想与关键技术
Web2BigTable 采用了**双层架构（Bi-Level Architecture）**和**外部存储驱动的自进化机制**：
*   **双层架构**：
    *   **上层协调器（Orchestrator）**：负责任务分解。它将复杂的查询拆分为多个独立的子任务（如按实体、按时间或按类别拆分）。
    *   **下层工作智能体（Workers）**：并行执行子任务。每个工作智能体利用工具（搜索、浏览、代码执行）获取证据并填充表格。
*   **关键技术细节**：
    *   **共享工作板（Shared Workboard）**：一个基于 Markdown 的共享空间，通过文件锁和标签分区实现异步协作。工作智能体可以读取他人的进度，从而避免重复搜索，并根据全局状态调整自己的策略。
    *   **自进化闭环（Run-Verify-Reflect）**：系统在训练阶段通过“运行-验证-反思”流程，将错误报告转化为人类可读的“技能脚本”（SKILL.md）。这些技能存储在外部存储器中，供推理阶段直接调用，无需微调底层大模型。
    *   **技能库（Skill Banks）**：分为协调器技能（分解策略）和工作者技能（执行工具/脚本），实现了长期语义记忆的积累。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **WideSearch**：侧重广度，包含 200 个任务（中英各半），要求提取数百行、多列的结构化数据。
    *   **XBench-DeepSearch**：侧重深度，要求进行多跳推理和跨源验证。
*   **对比方法**：
    *   **单智能体**：如 Claude 4.5/Sonnet、GPT-5 High/mini、Gemini 3 Pro/Flash、DeepSeek-R1 等。
    *   **端到端系统**：OpenAI、Google 和 Anthropic 的原生搜索/研究产品。
    *   **多智能体框架**：基于上述模型的标准层级编排框架。

### 4. 资源与算力
*   **模型使用**：协调器主要使用 **GPT-5 mini**，工作智能体使用 **Gemini 3 Flash**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该系统基于大模型 API 和外部存储器的“非参数化”进化，其核心开销在于 API 调用成本和推理延迟，而非传统的模型训练算力。

### 5. 实验数量与充分性
*   **实验规模**：在 WideSearch 的 200 个任务上进行了每项 4 次独立运行（取平均值和最大值），并在 XBench 上进行了完整测试。
*   **消融实验**：针对“学习到的协调技能”、“共享工作板”和“工作者技能进化”分别进行了消融，验证了各组件的贡献。
*   **客观性**：实验对比了当前市面上几乎所有主流的闭源和开源前沿模型，并使用了统一的 Item-F1、Row-F1 等细粒度指标，评估较为客观、公平。

### 6. 主要结论与发现
*   **性能突破**：在 WideSearch 上，Web2BigTable 的成功率（SR）达到 **38.50%**，是次优系统（5.10%）的 **7.5 倍**；行 F1 提升了 25.03%，项目 F1 提升了 14.42%。
*   **泛化能力**：在深度搜索基准 XBench 上达到 73.0 的准确率，超过了 Minimax-M2 和 MiroFlow 等专门的深度研究系统。
*   **框架优于模型**：实验证明，使用较弱的模型（如 GPT-5 mini + Gemini 3 Flash）配合该框架，表现远超直接使用最强模型（如 GPT-5 High）的单智能体或普通框架。

### 7. 优点
*   **自进化机制**：通过人类可读的外部存储积累经验，避免了昂贵的模型微调，且易于人工干预和纠错。
*   **高效协作**：共享工作板解决了多智能体系统常见的“信息孤岛”问题，显著减少了冗余开销。
*   **结构化输出能力强**：专门针对 Web-to-Table 场景优化，解决了长尾实体覆盖不足的问题。

### 8. 不足与局限
*   **延迟与成本**：多智能体并行及多次反思迭代会带来较高的 API 调用成本和端到端响应延迟。
*   **依赖外部工具**：系统的上限仍受限于底层搜索引擎的索引质量和网页解析工具的鲁棒性。
*   **应用限制**：虽然在表格提取上表现优异，但对于非结构化的开放式创意写作或纯逻辑推导任务，该框架的优势可能不明显。

（完）
