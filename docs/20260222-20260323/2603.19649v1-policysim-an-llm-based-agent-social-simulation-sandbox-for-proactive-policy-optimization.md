---
title: "PolicySim: An LLM-Based Agent Social Simulation Sandbox for Proactive Policy Optimization"
title_zh: PolicySim：一种用于前瞻性策略优化的基于 LLM 智能体的社交模拟沙盒
authors: "Renhong Huang, Ning Tang, Jiarong Xu, Yuxuan Cao, Qingqian Tu, Sheng Guo, Bo Zheng, Huiyuan Liu, Yang Yang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19649v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 基于大模型智能体的社会模拟用于政策优化
tldr: 针对社交平台干预政策（如推荐和过滤）可能引发的回声室和极化风险，本文提出了PolicySim，一个基于大语言模型的社会模拟沙盒。该系统通过SFT和DPO优化的用户智能体以及结合上下文老虎机与消息传递的自适应干预模块，实现了对平台生态的真实模拟。PolicySim能够在政策部署前主动评估并优化干预效果，为降低社会风险提供了有效的预演工具。
selection_source: fresh_fetch
motivation: 传统的在线A/B测试在识别政策风险时具有滞后性和高成本，亟需一种能在部署前主动评估社交平台干预政策影响的模拟工具。
method: 提出PolicySim沙盒，利用经过SFT和DPO微调的用户智能体模拟真实行为，并结合上下文老虎机与消息传递机制构建自适应干预模块。
result: 实验证明PolicySim能在微观和宏观层面准确模拟平台生态系统，并能有效支持干预政策的优化。
conclusion: PolicySim为社交平台政策的主动评估与优化提供了一个切实可行的框架，有助于在实际部署前识别并缓解潜在的社会风险。
---

## 摘要
社交平台是信息交换的中心枢纽，用户行为和平台干预共同塑造了舆论。然而，推荐和内容过滤等干预策略可能会无意中放大回声壁效应和极化现象，从而带来重大的社会风险。因此，前瞻性地评估此类策略的影响至关重要。现有方法主要依赖于反应性的在线 A/B 测试，风险仅在部署后才能被识别，导致风险识别滞后且成本高昂。基于 LLM 的社交模拟提供了一种极具前景的部署前替代方案，但当前方法在真实模拟平台干预以及整合平台反馈方面仍存在不足。弥补这些差距对于构建可操作的框架以评估和优化平台策略至关重要。为此，我们提出了 PolicySim，这是一个基于 LLM 的社交模拟沙盒，用于干预策略的前瞻性评估和优化。PolicySim 通过两个关键组件建模用户行为与平台干预之间的双向动态关系：(1) 一个通过有监督微调 (SFT) 和直接偏好优化 (DPO) 精炼的用户智能体模块，以实现特定平台的行为真实性；(2) 一个采用带有消息传递的上下文老虎机 (contextual bandit) 的自适应干预模块，以捕捉动态网络结构。实验表明，PolicySim 能够在微观和宏观层面准确模拟平台生态系统，并支持有效的干预策略。

## Abstract
Social platforms serve as central hubs for information exchange, where user behaviors and platform interventions jointly shape opinions. However, intervention policies like recommendation and content filtering, can unintentionally amplify echo chambers and polarization, posing significant societal risks. Proactively evaluating the impact of such policies is therefore crucial. Existing approaches primarily rely on reactive online A/B testing, where risks are identified only after deployment, making risk identification delayed and costly. LLM-based social simulations offer a promising pre-deployment alternative, but current methods fall short in realistically modeling platform interventions and incorporating feedback from the platform. Bridging these gaps is essential for building actionable frameworks to assess and optimize platform policies. To this end, we propose PolicySim, an LLM-based social simulation sandbox for the proactive assessment and optimization of intervention policies. PolicySim models the bidirectional dynamics between user behavior and platform interventions through two key components: (1) a user agent module refined via supervised fine-tuning (SFT) and direct preference optimization (DPO) to achieve platform-specific behavioral realism; and (2) an adaptive intervention module that employs a contextual bandit with message passing to capture dynamic network structures. Experiments show that PolicySim can accurately simulate platform ecosystems at both micro and macro levels and support effective intervention policy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PolicySim** 的创新性社交模拟沙盒系统，旨在通过大语言模型（LLM）智能体，在政策正式部署前主动评估并优化社交平台的干预策略。

### 1. 核心问题与整体含义
*   **研究动机**：社交平台（如 Twitter、微博）的干预政策（如推荐算法、内容过滤）虽然旨在提升用户体验，但往往会无意中放大“回声室效应”和“舆论极化”。
*   **核心痛点**：传统的政策评估依赖于 **A/B 测试**，这是一种“反应性”模式，即风险只有在部署并产生负面影响后才能被发现，且测试成本高、反馈周期长、具有不可逆的社会风险。
*   **整体含义**：论文提出了一种“前瞻性”范式，利用 LLM 模拟真实用户行为，构建一个可控的沙盒环境，在政策上线前进行模拟演练和自动优化。

### 2. 方法论：核心思想与技术细节
PolicySim 由两个核心模块组成：
*   **用户智能体模块 (User Agent Module)**：
    *   **画像与行为**：从真实数据中提取身份、兴趣、发帖风格和互动行为四个维度的画像。支持发帖、转发、评论、点赞、关注/取关等多种行为。
    *   **记忆机制**：分为短期记忆（临时存储）和长期记忆（基于语义相关性和时间衰减进行检索）。
    *   **训练范式（关键创新）**：首次提出结合 **有监督微调 (SFT)** 和 **直接偏好优化 (DPO)** 的社交智能体训练方法。SFT 用于学习社交媒体的语言风格和基础行为逻辑；DPO 则利用真实用户偏好数据进一步对齐，使智能体行为更具真实感。
    *   **立场建模**：利用指数移动平均 (EMA) 处理智能体在模拟过程中的立场演变，减少 LLM 幻觉带来的噪声。
*   **自适应干预模块 (Adaptive Intervention Module)**：
    *   **核心算法**：采用 **上下文多臂老虎机 (Contextual Bandit)** 框架。
    *   **消息传递机制**：在构建上下文嵌入时，引入类似图神经网络 (GNN) 的消息传递，捕捉社交网络中的邻居影响。
    *   **优化流程**：系统根据模拟环境的反馈（如极化程度、毒性得分）计算奖励（Reward），通过平衡“探索”与“利用”来动态调整干预策略（如调整推荐列表或曝光权重）。

### 3. 实验设计
*   **数据集**：主要使用 **TwiBot-20** (Twitter 数据集，包含 22.9 万用户和 3350 万推文) 和 **Weibo** 数据集。
*   **实验场景**：
    *   **微观层面**：评估内容生成质量、行为对齐准确度、自我一致性及社交能力（Engagement, Robustness, Suitability）。
    *   **宏观层面**：模拟“反堕胎立法”等热点事件下的舆论演化和极化现象。
*   **对比方法 (Benchmarks)**：
    *   **模型底座**：GLM4-light, Llama-3-8B, Qwen2.5 系列 (0.5B, 3B, 7B)。
    *   **干预基准**：$\epsilon$-greedy 算法、UCB 算法。
    *   **系统对比**：与现有的社交模拟框架（如 HiSim）进行对比。

### 4. 资源与算力
*   **硬件资源**：使用了 **12 台 NVIDIA A100-PCIE-40GB GPU**。
*   **模型配置**：以 **Qwen2.5-3B-Instruct** 作为智能体底座，使用 LoRA（rank=64）进行微调。
*   **训练细节**：SFT 和 DPO 阶段各训练最多 10 个 epoch 直至收敛。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从微观行为指标到宏观群体动力学的全方位评估。
*   **消融实验**：针对用户画像、SFT、DPO 等组件分别做了消融实验，验证了各模块的必要性。
*   **充分性评价**：实验设计较为全面，不仅验证了模拟的“真实性”，还验证了干预的“有效性”和系统的“可扩展性”（测试了高达 1000 个智能体的并行模拟）。跨平台（Twitter 到 Weibo）的验证增强了结论的客观性。

### 6. 主要结论与发现
*   **训练优于提示词**：通过 SFT+DPO 训练的智能体在行为对齐和内容质量上显著优于仅依赖提示词工程（Prompt Engineering）的智能体。
*   **干预有效性**：PolicySim 能够有效识别并优化干预策略。例如，在不增加社区毒性的前提下，成功提升了跨立场互动的比例。
*   **线性扩展性**：系统运行时间随智能体数量呈线性增长，具备支持大规模模拟的潜力。
*   **极化预警**：模拟显示，未经优化的推荐算法会显著加速立场极化，验证了沙盒的预警功能。

### 7. 优点与亮点
*   **闭环优化**：不仅是“模拟”，更实现了“模拟-反馈-优化”的闭环，使干预策略具有自适应性。
*   **行为真实性**：通过 DPO 解决 LLM 智能体在社交场景下行为模式单一的问题，使其更像真实网民。
*   **图结构感知**：在老虎机模型中引入消息传递，考虑了社交网络拓扑结构对信息传播的影响。

### 8. 不足与局限
*   **算力成本**：尽管具有线性扩展性，但大规模 LLM 推理（如模拟百万级用户）仍面临巨大的计算开销。
*   **幻觉风险**：尽管使用了 EMA 平滑立场，但 LLM 固有的幻觉仍可能导致模拟产生偏离现实的极端案例。
*   **场景覆盖**：实验主要集中在政治和虚假信息领域，对于更复杂的商业营销或长周期社会变迁的模拟尚待验证。
*   **模型依赖**：模拟效果高度依赖于底层 LLM 的能力，较小参数的模型在复杂社交推理上表现有限。

（完）
