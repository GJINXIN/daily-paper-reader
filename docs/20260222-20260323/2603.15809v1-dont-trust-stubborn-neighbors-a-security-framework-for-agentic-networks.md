---
title: "Don't Trust Stubborn Neighbors: A Security Framework for Agentic Networks"
title_zh: 不要信任固执的邻居：针对代理网络的安全性框架
authors: "Samira Abedini, Sina Mavali, Lea Schönherr, Martin Pawelczyk, Rebekka Burkholz"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15809v1"
tags: ["query:mas-ss"]
score: 10.0
evidence: 利用社会模型研究智能体网络中的虚假信息传播
tldr: 本研究针对基于大语言模型的多智能体系统（MAS）面临的恶意操纵风险，借鉴社会科学中的Friedkin-Johnsen观点形成模型，提出了一个通用的安全理论框架。研究发现，单个固执且具说服力的智能体即可引发说服级联并控制系统动态。为此，作者提出了一种信任自适应防御机制，通过动态调整智能体间的信任度，在不损害协作性能的前提下有效抵御恶意攻击，显著增强了MAS的安全性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中恶意智能体通过通信渠道传播误导信息并操纵集体决策的安全风险。
method: 引入社会科学的Friedkin-Johnsen模型构建理论框架，并提出一种动态调整智能体间信任度的自适应防御机制。
result: 理论与实验均证明，单个固执的攻击者能显著改变集体观点，而所提防御机制能有效限制其影响力。
conclusion: 通过动态信任管理可以平衡多智能体系统的协作效率与安全性，为构建鲁棒的智能体网络提供了新思路。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）正越来越多地被部署用于代理任务，例如网页自动化、行程规划和协作问题解决。然而，它们的交互性质引入了新的安全风险：恶意或受损的智能体可以利用通信渠道传播虚假信息并操纵集体结果。在本文中，我们借鉴社会科学中的 Friedkin-Johnsen 观点形成模型，提出了一个研究 LLM-MAS 的通用理论框架，以探讨此类操纵是如何产生和传播的。值得注意的是，该模型能够紧密捕捉 LLM-MAS 的行为，这一点我们在跨越不同网络拓扑以及攻击和防御场景的广泛实验中得到了验证。从理论和实证角度，我们发现单个高度固执且具有说服力的智能体就能接管 MAS 的动态，这通过触发重塑集体观点的说服级联，凸显了系统对攻击的高度敏感性。我们的理论分析揭示了提高系统安全性的三种机制：a) 增加良性智能体的数量，b) 增加智能体固有的固执程度或对同伴的抵抗力，或 c) 降低对潜在对手的信任。由于扩展规模在计算上成本高昂，且高固执度会降低网络达成共识的能力，我们提出了一种新的机制，通过信任自适应防御来减轻威胁，该防御动态调整智能体间的信任，以在保持协作性能的同时限制对抗性影响。广泛的实验证实，该机制能有效防御操纵。

## Abstract
Large Language Model (LLM)-based Multi-Agent Systems (MASs) are increasingly deployed for agentic tasks, such as web automation, itinerary planning, and collaborative problem solving. Yet, their interactive nature introduces new security risks: malicious or compromised agents can exploit communication channels to propagate misinformation and manipulate collective outcomes.   In this paper, we study how such manipulation can arise and spread by borrowing the Friedkin-Johnsen opinion formation model from social sciences to propose a general theoretical framework to study LLM-MAS. Remarkably, this model closely captures LLM-MAS behavior, as we verify in extensive experiments across different network topologies and attack and defense scenarios. Theoretically and empirically, we find that a single highly stubborn and persuasive agent can take over MAS dynamics, underscoring the systems' high susceptibility to attacks by triggering a persuasion cascade that reshapes collective opinion. Our theoretical analysis reveals three mechanisms to increase system security: a) increasing the number of benign agents, b) increasing the innate stubbornness or peer-resistance of agents, or c) reducing trust in potential adversaries. Because scaling is computationally expensive and high stubbornness degrades the network's ability to reach consensus, we propose a new mechanism to mitigate threats by a trust-adaptive defense that dynamically adjusts inter-agent trust to limit adversarial influence while maintaining cooperative performance. Extensive experiments confirm that this mechanism effectively defends against manipulation.

---

## 论文详细总结（自动生成）

### 论文总结：不要信任固执的邻居——针对代理网络的安全性框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的多智能体系统（MAS）在网页自动化、行程规划和协作决策等领域的广泛应用，系统安全性面临严峻挑战。论文指出，MAS 的交互本质引入了新的攻击面：恶意或受损的智能体可以利用通信渠道传播虚假信息，引发“说服级联”（Persuasion Cascade），从而操纵集体决策结果。研究的核心问题是：**在去中心化的代理网络中，虚假信息是如何传播并最终导致系统崩溃的？我们该如何建立理论框架来量化这种风险并设计有效的防御机制？**

#### 2. 方法论：核心思想与技术细节
论文的核心思想是将社会科学中的 **Friedkin-Johnsen (FJ) 观点形成模型** 引入 LLM-MAS 的分析中。

*   **FJ 模型建模**：将智能体的观点演化建模为线性动力学系统。每个智能体 $i$ 的观点更新受三个因素影响：
    1.  **固有偏见（Stubbornness, $\gamma$）**：智能体对初始私有信念 $s_i$ 的坚持程度。
    2.  **信念保留（Resistance, $\alpha$）**：智能体对上一时刻观点的保留程度。
    3.  **同伴影响（Peer Influence, $w$）**：智能体对邻居观点的信任权重。
    *   **更新公式**：$b_i(t+1) = \gamma_i s_i + (1-\gamma_i)[\alpha_i b_i(t) + (1-\alpha_i) \sum w_{ij} b_j(t)]$。
*   **攻击模型**：定义了“级联攻击”，即攻击者通过表现得极度固执（$\gamma=1$）且具有说服力，诱导良性智能体偏离正确答案。
*   **防御机制（信任自适应防御）**：
    *   **T-W (Trust Warmup)**：通过预先的测试题评估智能体可靠性，初始化信任权重。
    *   **T-S (Trust Sparse)**：在运行过程中随机抽取 20% 的题目进行在线信任更新。
    *   **T-WS (混合模式)**：结合预热和稀疏更新，以应对“先表现良好再发动攻击”的自适应攻击者。

#### 3. 实验设计
*   **数据集/场景**：
    *   **CommonsenseQA (CSQA)**：常识推理任务，具有模糊性，易受干扰。
    *   **ToolBench**：工具选择任务，模拟实际的 API 调用决策场景。
*   **网络拓扑**：对比了星型网络（攻击者为中心节点或叶子节点）和全连接网络。
*   **对比基准 (Benchmark)**：
    *   **模型族**：涵盖了 Gemini-3-Flash, GPT-5 mini, GPT-OSS-120B, MiniMax-M2.5, Qwen3-235B, Ministral-3-14B 等 6 种主流模型。
    *   **指标**：攻击成功率（ASR），即原本能答对的良性智能体被误导为错误答案的比例。
    *   **对比方法**：无防御状态 vs. 不同参数设置下的信任防御机制。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA H100 GPU**。
*   **推理框架**：使用 `vLLM` 进行本地部署。
*   **模型配置**：大型模型（如 Qwen3-235B）采用了 4 路张量并行（Tensor Parallelism）和 FP8 量化；较小模型（如 Ministral-3-14B）在单卡运行。
*   **训练/推理时长**：文中未明确给出总耗时，但提到每组实验包含 10 轮迭代对话。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨越 6 个模型家族、2 个数据集、3 种网络拓扑、多种智能体性格特征（固执度、说服力组合）以及 10 轮对话的详尽实验。
*   **充分性**：实验设计非常充分。不仅验证了 FJ 模型对 LLM 行为的拟合度（R² > 0.95），还专门设计了“自适应攻击者”场景，模拟攻击者在预热阶段伪装成良性智能体的情况，增强了实验的客观性和现实意义。

#### 6. 主要结论与发现
*   **拓扑决定论**：星型网络中的中心节点（Hub）如果被攻破，其破坏力最大，ASR 几乎是全连接网络的数倍。
*   **单点崩溃**：理论和实验均证明，**只需一个极度固执的攻击者**，就足以让整个全连接的良性智能体群体放弃正确答案。
*   **防御有效性**：增加良性智能体数量（N）或提高其固有固执度可以提升鲁棒性，但会降低达成共识的效率。
*   **信任的力量**：动态信任机制（尤其是 T-WS）能有效识别并隔离攻击者，即使面对自适应攻击，也能显著降低 ASR。

#### 7. 优点：亮点与创新
*   **跨学科建模**：成功将社会心理学的观点动力学模型（FJ 模型）应用于 AI 安全领域，提供了坚实的数学理论基础。
*   **高拟合度**：证明了复杂的 LLM 交互行为可以用相对简单的线性动力学方程精确描述，这为未来的 MAS 治理提供了可解释的工具。
*   **防御实用性**：提出的信任自适应机制不依赖于复杂的重训练，仅通过 Prompt 注入信任权重即可实现，具有极强的工程落地价值。

#### 8. 不足与局限
*   **任务类型局限**：实验主要集中在多选题（Multiple Choice）和工具选择上，对于开放式生成或长程规划任务的适用性尚待验证。
*   **信任评估依赖**：防御机制依赖于系统拥有部分“地面真值”（Ground Truth）来评估智能体，在完全无监督的开放环境中可能难以实施。
*   **中心化风险**：目前的信任管理是中心化的，这在完全去中心化的代理网络中可能成为新的单点故障或攻击目标。

（完）
