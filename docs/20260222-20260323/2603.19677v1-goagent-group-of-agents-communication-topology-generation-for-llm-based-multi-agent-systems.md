---
title: "GoAgent: Group-of-Agents Communication Topology Generation for LLM-based Multi-Agent Systems"
title_zh: GoAgent：面向基于大语言模型的多智能体系统的智能体组通信拓扑生成
authors: "Hongjiang Chen, Xin Zheng, Yixin Liu, Pengfei Jiao, Shiyuan Li, Huan Liu, Zhidong Zhao, Ziqi Xu, Ibrahim Khalil, Shirui Pan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19677v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 基于大语言模型的多智能体系统通信拓扑
tldr: "针对大语言模型多智能体系统（MAS）中通信拓扑构建效率低下的问题，本文提出GoAgent框架。该方法打破了传统的以节点为中心的构建模式，将协作小组视为基本构建单元。通过LLM枚举候选小组并自回归地连接它们，同时引入条件信息瓶颈（CIB）来压缩组间通信冗余。实验证明，GoAgent在六个基准测试中达到了93.84%的平均准确率，并显著降低了17%的Token消耗。"
selection_source: fresh_fetch
motivation: 现有的多智能体通信拓扑多以节点为中心，导致协作小组结构不明确且通信开销大。
method: 提出GoAgent框架，将协作小组作为原子单元进行枚举和连接，并利用条件信息瓶颈优化组间通信。
result: "在六个基准测试中取得93.84%的SOTA准确率，同时减少了约17%的Token消耗。"
conclusion: 显式建模小组结构并结合信息压缩机制，能有效提升多智能体系统的协作效率和性能。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在解决复杂任务方面展现了卓越的能力，但其有效性在很大程度上取决于协调智能体交互的底层通信拓扑。在这些系统中，成功解决问题通常需要特定任务的组结构来对子任务进行分治。然而，大多数现有方法以“以节点为中心”的方式生成通信拓扑，使得组结构从局部连接决策中隐式产生，而非显式建模，这往往导致协调效果不佳和不必要的通信开销。为了解决这一局限性，我们提出了 GoAgent（Group-of-Agents），这是一种将协作组显式视为 MAS 构建原子单元的通信拓扑生成方法。具体而言，GoAgent 首先通过 LLM 枚举与任务相关的候选组，然后自回归地选择并连接这些组作为原子单元来构建最终的通信图，从而共同捕捉组内凝聚力和组间协调。为了减轻拓扑扩展中固有的通信冗余和噪声传播，我们进一步引入了条件信息瓶颈（CIB）目标来压缩组间通信，在保留任务相关信号的同时过滤掉冗余的历史噪声。在六个基准测试上的广泛实验表明，GoAgent 达到了最先进的性能，平均准确率达 93.84%，同时减少了约 17% 的 token 消耗。

## Abstract
Large language model (LLM)-based multi-agent systems (MAS) have demonstrated exceptional capabilities in solving complex tasks, yet their effectiveness depends heavily on the underlying communication topology that coordinates agent interactions. Within these systems, successful problem-solving often necessitates task-specific group structures to divide and conquer subtasks. However, most existing approaches generate communication topologies in a node-centric manner, leaving group structures to emerge implicitly from local connectivity decisions rather than modeling them explicitly, often leading to suboptimal coordination and unnecessary communication overhead. To address this limitation, we propose GoAgent (Group-of-Agents), a communication topology generation method that explicitly treats collaborative groups as the atomic units of MAS construction. Specifically, GoAgent first enumerates task-relevant candidate groups through an LLM and then autoregressively selects and connects these groups as atomic units to construct the final communication graph, jointly capturing intra-group cohesion and inter-group coordination. To mitigate communication redundancy and noise propagation inherent in expanding topologies, we further introduce a conditional information bottleneck (CIB) objective that compresses inter-group communication, preserving task-relevant signals while filtering out redundant historical noise. Extensive experiments on six benchmarks demonstrate the state-of-the-art performance of GoAgent with 93.84% average accuracy while reducing token consumption by about 17%.

---

## 论文详细总结（自动生成）

### 论文总结：GoAgent —— 面向大语言模型多智能体系统的组通信拓扑生成

#### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的多智能体系统（MAS）在构建通信拓扑时，大多采用“以节点为中心（Node-centric）”的模式，即逐个预测智能体及其连接。
*   **研究动机**：
    *   **缺乏宏观协作结构**：复杂任务通常需要特定的小组（如“提出者-验证者-修正者”）协作，节点中心模式难以显式建模这种高阶结构，导致协作效率低下。
    *   **通信冗余与噪声**：无约束的节点连接会产生大量冗余 Token 消耗，并导致任务无关的“历史噪声”在网络中传播，干扰智能体决策。
*   **整体含义**：本文提出 **GoAgent** 框架，将“协作小组（Group）”视为构建 MAS 的原子单元，通过显式建模组间协调和组内凝聚力，提升系统的解决问题能力和通信效率。

#### 2. 方法论
GoAgent 采用“以小组为中心（Group-centric）”的范式，主要包含以下四个阶段：
*   **任务编码与小组发现**：
    *   利用预训练编码器将任务查询 $Q$ 转化为向量 $z_Q$。
    *   利用 LLM（如 GPT-4）预先生成一个包含 $K$ 个候选协作小组的池子。每个小组包含特定角色（如分析师、程序员）和固定的组内拓扑（如链式、全连接）。
*   **自回归拓扑生成**：
    *   模型以自回归方式逐个选择小组并预测其与已生成小组之间的有向边。
    *   使用 GRU 聚合历史生成状态，并通过动态门控机制融合任务向量 $z_Q$。
*   **条件信息瓶颈（CIB）**：
    *   **核心思想**：在预测下一个小组或边时，引入 CIB 层对历史特征进行压缩。
    *   **技术细节**：以任务 $z_Q$ 为条件，通过最小化预测损失（确保保留关键信号）和最小化与任务相关先验的 KL 散度（过滤冗余噪声），实现信息的精简。
*   **训练策略**：
    *   通过启发式探索收集能够成功解决任务的“最小可行拓扑”作为真值轨迹。
    *   使用教师强制（Teacher Forcing）和 KL 预热（Warm-up）策略进行监督学习。

#### 3. 实验设计
*   **数据集/场景**：涵盖了 6 个主流基准测试：
    *   **通用推理**：MMLU。
    *   **数学推理**：GSM8K、MultiArith、SVAMP、AQuA。
    *   **代码生成**：HumanEval。
*   **对比方法（Baselines）**：
    *   **单智能体**：Vanilla、CoT、Self-Consistency。
    *   **固定拓扑 MAS**：Chain、Tree、Complete、Random、LLM-Debate。
    *   **可学习拓扑 MAS**：AgentPrune、AgentDropout、G-Designer、EIB-LEARNER、ARG-Designer（最先进的节点中心自回归方法）。

#### 4. 资源与算力
*   **模型调用**：主要使用 `gpt-4o-2024-08-06` API 进行智能体交互。
*   **生成器参数**：使用轻量级组件，隐藏层维度 $d=256$，小组编码器使用 `all-MiniLM-L6-v2`。
*   **训练规模**：每个数据集仅需 40-60 个查询（Queries）进行微调，显示出极高的样本效率。
*   **算力说明**：文中未明确提及具体的 GPU 型号和训练时长，但由于生成器（GRU/MLP）非常轻量，且训练样本极少，预计在单张消费级 GPU 上即可快速完成训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的标准数据集上进行了测试，实验覆盖面广。
*   **消融实验**：分别验证了“小组范式”和“CIB 机制”的有效性，证明两者缺一不可。
*   **效率分析**：专门对比了 Token 消耗量，证明了方法的经济性。
*   **鲁棒性测试**：进行了针对提示词注入（Prompt Injection）的攻击实验。
*   **评价**：实验设计全面、客观，通过与多种 SOTA 方法对比，充分验证了 GoAgent 在准确率、效率和鲁棒性方面的优势。

#### 6. 主要结论与发现
*   **性能领先**：GoAgent 在所有基准测试中均取得 SOTA 结果，平均准确率达到 93.84%。
*   **高效率**：相比于之前的 SOTA 方法（ARG-Designer），GoAgent 在保持更高准确率的同时，减少了约 **17%** 的 Token 消耗。
*   **强鲁棒性**：得益于 CIB 的噪声过滤功能，GoAgent 在面对恶意提示词攻击时，性能下降远小于节点中心的方法。
*   **结构优势**：显式的小组建模能构建出更简洁、更符合逻辑的协作流，避免了节点中心模式下常见的冗余连接。

#### 7. 优点
*   **范式创新**：打破了节点中心论，提出了更符合人类组织学的小组中心生成范式。
*   **信息压缩**：引入条件信息瓶颈（CIB），有效解决了 MAS 中长期存在的通信噪声和冗余问题。
*   **样本经济性**：仅需极少量训练数据即可学习到有效的拓扑生成模式。

#### 8. 不足与局限
*   **候选池依赖**：依赖于预定义的候选小组池，如果任务需要的专业知识超出了池子的范围，系统无法在推理时动态合成全新的角色或小组结构。
*   **任务类型限制**：目前主要集中在静态推理任务，尚未在具身智能（Embodied AI）或高度动态的交互式环境（如多智能体强化学习场景）中进行验证。
*   **离线生成**：拓扑是在任务开始前生成的，缺乏在执行过程中根据中间结果动态调整拓扑的能力。

（完）
