---
title: "In Trust We Survive: Emergent Trust Learning"
title_zh: 信任中生存：涌现信任学习
authors: "Qianpu Chen, Giulio Barbero, Mike Preuss, Derya Soydaner"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17564v1"
tags: ["query:mas-ss"]
score: 9.0
evidence: 多智能体环境中的涌现信任与合作
tldr: 本研究提出涌现信任学习（ETL），一种轻量级的信任控制算法，旨在解决竞争性博弈中共享资源的合作难题。该算法通过维护内部信任状态来调节智能体的决策，仅需局部观测和个体奖励。实验表明，ETL在资源竞争、社会困境及迭代囚徒困境中均能有效减少冲突、防止资源枯竭，并在面对不同策略时维持稳健的合作，为多智能体协作提供了高效且低开销的新方案。
selection_source: fresh_fetch
motivation: 针对竞争性博弈中智能体因追求个体利益而导致资源枯竭和社会困境的问题，探索如何通过信任机制促进合作。
method: 引入一种可插入现有智能体的轻量级ETL算法，利用内部信任状态动态调节记忆、探索和动作选择。
result: 实验证明ETL在多种复杂环境下均能显著降低冲突、提升生存率，并能有效应对背叛者同时与互惠者保持合作。
conclusion: 信任机制是实现多智能体系统涌现合作的关键，ETL以极低的计算和通信开销实现了高效的资源管理和战略合作。
---

## 摘要
我们引入了涌现信任学习（Emergent Trust Learning, ETL），这是一种轻量级的、基于信任的控制算法，可以嵌入到现有的 AI 智能体中。它使这些智能体能够在共享资源的竞争性博弈环境中达成合作。每个智能体维持一个紧凑的内部信任状态，用于调节记忆、探索和动作选择。ETL 仅需要个体奖励和局部观测，且计算和通信开销微乎其微。我们在三种环境中评估了 ETL：在基于网格的资源世界中，基于信任的智能体减少了冲突并防止了长期的资源枯竭，同时实现了具有竞争力的个体收益。在具有强烈社会困境和随机楼层分配的分层塔（Tower）环境中，ETL 维持了高生存率，甚至在经历了长期的强制贪婪阶段后仍能恢复合作。在迭代囚徒困境中，该算法泛化为一种策略性元博弈，在与互惠对手保持合作的同时，避免被背叛者长期剥削。代码将在论文发表后发布。

## Abstract
We introduce Emergent Trust Learning (ETL), a lightweight, trust-based control algorithm that can be plugged into existing AI agents. It enables these to reach cooperation in competitive game environments under shared resources. Each agent maintains a compact internal trust state, which modulates memory, exploration, and action selection. ETL requires only individual rewards and local observations and incurs negligible computational and communication overhead.   We evaluate ETL in three environments: In a grid-based resource world, trust-based agents reduce conflicts and prevent long-term resource depletion while achieving competitive individual returns. In a hierarchical Tower environment with strong social dilemmas and randomised floor assignments, ETL sustains high survival rates and recovers cooperation even after extended phases of enforced greed. In the Iterated Prisoner's Dilemma, the algorithm generalises to a strategic meta-game, maintaining cooperation with reciprocal opponents while avoiding long-term exploitation by defectors. Code will be released upon publication.

---

## 论文详细总结（自动生成）

### 论文总结：In Trust We Survive: Emergent Trust Learning (ETL)

#### 1. 核心问题与整体含义（研究动机和背景）
在多智能体强化学习（MARL）中，智能体往往追求个体利益最大化，这在共享资源的竞争环境中常导致“公地悲剧”：如过度消耗资源导致系统崩溃、社会困境中的相互背叛等。现有的协作方法通常依赖于共享奖励函数或中心化训练（CTDE），但在去中心化、匿名或竞争性的现实游戏场景中，这些假设往往不成立。
**研究动机**：开发一种轻量级的、可插拔的组件，使自私的智能体在不改变原始奖励函数和核心逻辑的情况下，通过“信任”机制自发涌现出协作行为，以维持系统的长期稳定和个体收益。

#### 2. 方法论：核心思想与关键技术
论文提出了**涌现信任学习（ETL）**，其核心是将“信任”作为一个内部状态变量引入智能体的决策过程。
*   **核心思想**：智能体根据局部观测到的信号（如资源剩余量、收益波动）推断环境和他人的可靠性，并据此调节自身的探索策略和动作选择。
*   **关键组件**：
    *   **记忆机制**：分为短期记忆（STM，追踪近期交互模式）和长期记忆（LTM，积累历史成功经验），用于平衡快速适应与策略稳定性。
    *   **信任更新公式**：$\Delta T_{ij} = \alpha s_t^i - \beta d_t^i + \gamma (\bar{T}_{ij}^t - T_{ij}^t)$。其中 $s$ 是支持信号（如获得资源），$d$ 是不满信号（如资源枯竭或受损），$\bar{T}$ 是长期信任均值。信任值被限制在 $[-1, 1]$ 之间。
    *   **动态探索机制**：探索率 $\epsilon$ 根据信任的方差动态调整。当信任不稳定（方差大）时增加探索以寻找新策略；当信任稳定且处于高位时，减少探索以执行协作动作。
    *   **动作选择**：当信任值和环境稳定性评分超过阈值时，智能体倾向于执行已知的协作策略，否则退回到随机探索。

#### 3. 实验设计
论文在三个具有代表性的场景中进行了评估：
*   **网格资源环境（Grid-based Resource World）**：多个智能体竞争可再生的资源点。对比基准是不带信任机制的同类学习算法。
*   **塔式环境（Tower Environment）**：受电影《饥饿站台》启发，食物从塔顶降下，上层智能体贪婪会导致下层饿死。对比基准是标准的**表格型 Q-Learning** 和 **蒙特卡洛（MC）控制**。
*   **迭代囚徒困境（IPD）**：经典的博弈论模型。对比基准包括 **Always Cooperate、Always Defect、Random、Tit-for-Tat (TFT)** 以及专门设计的延迟反应剥削策略。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号或训练时长。
*   由于实验主要基于表格型强化学习（Tabular RL）和简单的 2D 环境（如 Pygame 开发的 Tower），且算法设计强调“轻量级”和“低计算开销”，推测其对算力的需求极低，普通的 CPU 即可完成训练。

#### 5. 实验数量与充分性
*   **实验规模**：在网格环境中进行了 30 组独立运行以确保统计显著性；在 IPD 中进行了全循环赛（Round-robin tournament），每场比赛包含 500 轮交互。
*   **鲁棒性测试**：在 Tower 环境中设计了“强制贪婪阶段”实验，验证智能体在经历长期背叛后能否恢复合作。
*   **充分性评价**：实验覆盖了资源管理、社会困境和战略博弈三个维度，对比了经典基准算法，实验设计较为客观、公平，能够有力支撑“信任能促进涌现合作”的论点。

#### 6. 主要结论与发现
*   **减少冲突与保护资源**：在网格环境中，ETL 显著降低了冲突频率，并使环境中的剩余资源量保持在较高水平，防止了资源枯竭。
*   **高生存率与恢复力**：在 Tower 环境中，ETL 维持了 90% 以上的生存率，且在经历强制贪婪期后能迅速找回协作平衡，表现优于 Q-Learning 和 MC。
*   **抗剥削性**：在 IPD 中，ETL 的综合胜率超过了 TFT。它能识别互惠者并达成长期合作，同时在面对纯背叛者时能及时降低信任以避免被持续剥削。
*   **无需显式奖励**：协作行为是作为生存和优化个体收益的副产品“涌现”出来的，而非通过人工设计的协作奖励诱导。

#### 7. 优点
*   **轻量级与低开销**：不需要智能体之间的显式通信，也不需要追踪他人的身份（ID），计算和存储负担极小。
*   **可插拔性强**：可以作为现有 RL 算法的附加模块，易于集成到不同的游戏 AI 系统中。
*   **动态适应性**：通过信任方差调节探索率，使智能体在多变的环境中具有极强的鲁棒性和恢复能力。

#### 8. 不足与局限
*   **规模扩展性**：目前的实验主要针对小规模群体（如 4 个智能体），在大规模（如数百个智能体）复杂环境中的表现尚待验证。
*   **环境复杂度**：实验环境相对抽象（2D 网格或矩阵博弈），尚未在具有高维状态空间（如 3D 动作游戏）的深度强化学习架构中进行大规模测试。
*   **参数敏感性**：信任更新公式中的超参数（$\alpha, \beta, \gamma$）可能需要针对不同环境进行微调，论文未详细讨论参数敏感性分析。

（完）
